/** Tab-scoped session credentials (Phase 7). */
export const SESSION_ID_KEY = "agent-talkie.dashboard.sessionId";
export const RECONNECT_SECRET_KEY = "agent-talkie.dashboard.reconnectSecret";

/** Baseline relay generation for health / stale detection (Plan 2–3). */
export const RELAY_GENERATION_KEY = "agent-talkie.dashboard.relayGeneration";
