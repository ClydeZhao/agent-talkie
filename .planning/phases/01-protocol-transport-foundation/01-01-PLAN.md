---
phase: "01-protocol-transport-foundation"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - packages/protocol/package.json
  - packages/protocol/tsconfig.json
  - packages/protocol/src/envelope.ts
  - packages/protocol/src/errors.ts
  - packages/protocol/src/idempotency.ts
  - packages/protocol/src/index.ts
  - packages/protocol/src/envelope.test.ts
  - packages/protocol/src/idempotency.test.ts
  - packages/protocol/scripts/write-envelope-json-schema.ts
  - packages/protocol/json-schema/envelope.schema.json
  - docs/protocol-upgrades.md
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02
  - PROTO-04
must_haves:
  truths:
    - "A JSON object with all D-01 wire fields plus idempotency_key validates via the exported parser when schema_version is 1."
    - "schema_version outside 1..1 yields error code SCHEMA_VERSION_UNSUPPORTED with supported_min 1, supported_max 1, and upgrade_doc_url ending in docs/protocol-upgrades.md."
    - "The same idempotency_key run twice through the in-memory guard does not execute the side effect twice."
  artifacts:
    - path: "packages/protocol/json-schema/envelope.schema.json"
      provides: "Committed JSON Schema for the envelope"
    - path: "docs/protocol-upgrades.md"
      provides: "Human upgrade path for schema versions"
  key_links:
    - from: "packages/protocol/src/envelope.ts"
      to: "packages/protocol/src/errors.ts"
      via: "schema version range check after Zod parse"
---

<objective>
Deliver the monorepo scaffold, Zod-defined wire envelope (PROTO-01), idempotency guard contract (PROTO-02 application layer), strict schema-version rejection with documented upgrade path (PROTO-04), and generated JSON Schema (D-02).

Purpose: Establishes the single source of truth for the message contract before subjects and NATS integration.

Output: Runnable `npm test` / `vitest` for protocol package, `envelope.schema.json`, and `docs/protocol-upgrades.md`.
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-protocol-transport-foundation/01-CONTEXT.md
@.planning/phases/01-protocol-transport-foundation/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

## Locked decisions (traceability)

- **D-01:** Wire envelope fields: `schema_version`, `message_id`, `idempotency_key`, `thread_id`, `sender_session_id`, `space_id`, `type` (`control` | `conversation`), `timestamp`, `payload`.
- **D-02:** Zod plus `z.toJSONSchema()`; committed artifact under `packages/protocol/json-schema/`.
- **D-05 / D-06:** Integers; unsupported versions rejected with structured error, not silent drop.
- **Wire casing:** **snake_case** JSON keys matching D-01 names exactly (per 01-RESEARCH open question resolution).

## Constants (concrete targets)

- `SUPPORTED_SCHEMA_VERSION_MIN = 1`, `SUPPORTED_SCHEMA_VERSION_MAX = 1`.
- `SCHEMA_VERSION_UNSUPPORTED` string literal as `code` on rejection payload.
- `upgrade_doc_url` value string must contain `docs/protocol-upgrades.md` (grep-verifiable in `errors.ts`).
- `space_id` subject-safe pattern: `^[a-zA-Z0-9_-]{1,128}$`.
- Package name: `@agent-talkie/protocol`.
- Zod **4.3.6**, TypeScript **5.9.3**, Vitest **4.1.4**, tsx **4.19.3**, `@types/node` **22.15.3** (exact pins in package.json).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root and protocol package scaffold</name>
  <read_first>
    - .planning/phases/01-protocol-transport-foundation/01-CONTEXT.md
    - .planning/phases/01-protocol-transport-foundation/01-RESEARCH.md
    - AGENTS.md
  </read_first>
  <files>package.json, tsconfig.json, vitest.config.ts, packages/protocol/package.json, packages/protocol/tsconfig.json, packages/protocol/src/index.ts</files>
  <action>
    Create npm workspaces monorepo at repo root: root package.json with "private": true, "workspaces": ["packages/*"], scripts "test": "vitest run", "test:protocol": "vitest run packages/protocol", "generate:schema": "npx tsx packages/protocol/scripts/write-envelope-json-schema.ts".

    Root devDependencies exact versions: typescript@5.9.3, vitest@4.1.4, @types/node@22.15.3, tsx@4.19.3.

    Root tsconfig.json compilerOptions: target ES2022, module NodeNext, moduleResolution NodeNext, strict true, declaration true, declarationMap true, composite true; exclude node_modules.

    vitest.config.ts at root: include packages/protocol/src/**/*.test.ts, environment node, passWithNoTests true.

    packages/protocol/package.json: name @agent-talkie/protocol, version 0.0.0, type module, dependency zod 4.3.6; exports or vitest/vite resolution so tests import from src without prebuild.

    packages/protocol/tsconfig.json extends root; include src/**/*.ts and scripts/**/*.ts.

    packages/protocol/src/index.ts: export const PROTOCOL_PACKAGE_VERSION = "0.0.0"; Task 2 adds envelope exports.

    Run npm install at repo root after writing files.
  </action>
  <acceptance_criteria>
    - Command `grep -q "\"workspaces\"" package.json` exits 0.
    - Command `grep -q "@agent-talkie/protocol" packages/protocol/package.json` exits 0.
    - Command `grep -q "\"zod\": \"4.3.6\"" packages/protocol/package.json` exits 0.
    - Command `test -f vitest.config.ts` exits 0.
    - Command `grep -q passWithNoTests vitest.config.ts` exits 0.
    - Command `npm test` run from repo root exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm test</automated>
  </verify>
  <done>Monorepo installs cleanly; Vitest runs with passWithNoTests until Task 3 adds tests.</done>
</task>

<task type="auto">
  <name>Task 2: Envelope Zod schema, errors, and parser</name>
  <read_first>
    - packages/protocol/package.json
    - packages/protocol/tsconfig.json
    - .planning/phases/01-protocol-transport-foundation/01-CONTEXT.md
    - packages/protocol/src/index.ts
  </read_first>
  <files>packages/protocol/src/envelope.ts, packages/protocol/src/errors.ts, packages/protocol/src/index.ts</files>
  <action>
    Implement packages/protocol/src/errors.ts: export SCHEMA_VERSION_UNSUPPORTED_CODE = "SCHEMA_VERSION_UNSUPPORTED"; export type SchemaVersionUnsupported with fields code, supported_min number, supported_max number, upgrade_doc_url string; export function schemaVersionUnsupported() returning object with code SCHEMA_VERSION_UNSUPPORTED_CODE, supported_min 1, supported_max 1, upgrade_doc_url "docs/protocol-upgrades.md".

    Implement packages/protocol/src/envelope.ts: export SUPPORTED_SCHEMA_VERSION_MIN = 1, SUPPORTED_SCHEMA_VERSION_MAX = 1; Zod object EnvelopeSchema snake_case keys schema_version z.number().int().positive(), message_id z.string().uuid(), idempotency_key z.string().min(1), thread_id z.string().min(1), sender_session_id z.string().min(1), space_id z.string().regex(/^[a-zA-Z0-9_-]{1,128}$/), type z.enum(["control","conversation"]), timestamp use Zod 4 ISO datetime (z.iso.datetime() if available else z.string().datetime({ offset: true })), payload z.unknown(); export type MessageEnvelope as z.infer output of EnvelopeSchema; export parseEnvelope(input: unknown) implementing safeParse then range check then VALIDATION_ERROR branch as specified in plan 01-CONTEXT.

    Update packages/protocol/src/index.ts to re-export envelope and errors modules; keep PROTOCOL_PACKAGE_VERSION export.
  </action>
  <acceptance_criteria>
    - Command `grep -q SCHEMA_VERSION_UNSUPPORTED packages/protocol/src/errors.ts` exits 0.
    - Command `grep -q docs/protocol-upgrades.md packages/protocol/src/errors.ts` exits 0.
    - Command `grep -q idempotency_key packages/protocol/src/envelope.ts` exits 0.
    - Command `grep -q SUPPORTED_SCHEMA_VERSION_MIN packages/protocol/src/envelope.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && test -f packages/protocol/src/envelope.ts && npx vitest run packages/protocol</automated>
  </verify>
  <done>Parser and error types compile; exports available from package index.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests, idempotency guard, JSON Schema artifact, upgrade doc</name>
  <read_first>
    - packages/protocol/src/envelope.ts
    - packages/protocol/src/errors.ts
    - packages/protocol/src/index.ts
  </read_first>
  <files>packages/protocol/src/idempotency.ts, packages/protocol/src/idempotency.test.ts, packages/protocol/src/envelope.test.ts, packages/protocol/scripts/write-envelope-json-schema.ts, packages/protocol/json-schema/envelope.schema.json, docs/protocol-upgrades.md</files>
  <behavior>
    - parseEnvelope: valid minimal fixture with schema_version 1 passes.
    - parseEnvelope: schema_version 2 returns ok false and error.code SCHEMA_VERSION_UNSUPPORTED with supported_min 1 and supported_max 1.
    - parseEnvelope: missing message_id fails with VALIDATION_ERROR.
    - createIdempotencyGuard: first call for key k1 runs fn; second call for k1 returns cached result without re-invoking fn (counter mock).
  </behavior>
  <action>
    Add packages/protocol/src/idempotency.ts exporting createIdempotencyGuard returning an object with method run that accepts key string and synchronous callback fn returning T, returns T, backed by a string-keyed Map (document that async callers must wrap).

    Add packages/protocol/scripts/write-envelope-json-schema.ts: import EnvelopeSchema from ../src/envelope.ts, z.toJSONSchema(EnvelopeSchema), writeFileSync packages/protocol/json-schema/envelope.schema.json pretty-printed JSON.

    Add docs/protocol-upgrades.md with first line exactly "# Protocol upgrades" and body stating schema_version 1 only for v1.

    Implement envelope.test.ts and idempotency.test.ts covering the four behavior bullets.

    Run npm run generate:schema; commit generated envelope.schema.json.
  </action>
  <acceptance_criteria>
    - Command `grep -q SCHEMA_VERSION_UNSUPPORTED packages/protocol/src/envelope.test.ts` exits 0.
    - Command `grep -q createIdempotencyGuard packages/protocol/src/idempotency.test.ts` exits 0.
    - Command `test -f packages/protocol/json-schema/envelope.schema.json` exits 0.
    - Command `grep -q schema_version packages/protocol/json-schema/envelope.schema.json` exits 0.
    - Command `head -n 1 docs/protocol-upgrades.md | grep -q "Protocol upgrades"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run generate:schema && npx vitest run packages/protocol</automated>
  </verify>
  <done>All protocol unit tests green; JSON Schema file exists; upgrade doc exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unknown JSON to parser | Untrusted wire input enters via parseEnvelope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | parseEnvelope | mitigate | Zod safeParse; reject invalid shapes |
| T-01-02 | D | oversized JSON | mitigate | Document 256KiB default recommendation in docs/protocol-upgrades.md |
| T-01-03 | I | schema version spoofing | mitigate | Integer range check 1..1 |
</threat_model>

<verification>
- npx vitest run packages/protocol passes.
- npm run generate:schema regenerates envelope.schema.json deterministically.
</verification>

<success_criteria>
PROTO-01, PROTO-02 (application idempotency field plus guard), and PROTO-04 satisfied by tests and committed artifacts per REQUIREMENTS.md.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-transport-foundation/01-01-SUMMARY.md`
</output>
