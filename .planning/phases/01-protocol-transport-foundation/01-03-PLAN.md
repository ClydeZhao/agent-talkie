---
phase: "01-protocol-transport-foundation"
plan: "03"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - packages/relay/package.json
  - packages/relay/tsconfig.json
  - packages/relay/src/schema.ts
  - packages/relay/src/server.ts
  - packages/relay/src/relay.ingest.test.ts
  - vitest.config.ts
  - package.json
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02
  - PROTO-03
  - PROTO-04
must_haves:
  truths:
    - "A minimal WebSocket relay accepts JSON text frames, runs parseEnvelope, rejects unsupported schema_version with SCHEMA_VERSION_UNSUPPORTED shape, and persists idempotency_key in SQLite."
    - "Publishing the same idempotency_key twice yields a duplicate indication on the second accepted frame without inserting a second row."
    - "Vitest proves the above without Docker or NATS (npm install only)."
  artifacts:
    - path: "packages/relay/src/server.ts"
      provides: "Embedded relay WebSocket server with ingress validation"
    - path: "packages/relay/src/schema.ts"
      provides: "SQLite DDL and open helper for idempotency store"
  key_links:
    - from: "packages/relay/src/server.ts"
      to: "packages/protocol/src/envelope.ts"
      via: "parseEnvelope on each inbound message"
---

<objective>
Implement an embedded local relay: WebSocket JSON ingress, Zod-backed envelope validation (PROTO-01, PROTO-04), SQLite-backed idempotency deduplication (PROTO-02, D-00a/c), using relay route keys from plan 02 for classification logging or hooks (PROTO-03).

Purpose: Replaces JetStream/NATS integration with the zero-external-services default while keeping dedup and schema rejection testable.

Output: packages/relay with ws + better-sqlite3, integration-style Vitest against an ephemeral port and temp DB file.
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-protocol-transport-foundation/01-CONTEXT.md
@packages/protocol/src/envelope.ts
@packages/protocol/src/errors.ts
@packages/protocol/src/relay_routing.ts

## Dependency pins (concrete)

- npm package name: @agent-talkie/relay (workspace)
- runtime deps: ws 8.20.0, better-sqlite3 11.10.0 (use exact versions in packages/relay/package.json)
- devDependency: @types/ws 8.18.1

## Ingress limits (concrete)

- MAX_INGRESS_BYTES = 65536 (number literal 65536 in server.ts)
- SQLite table name: relay_idempotency with columns idempotency_key TEXT PRIMARY KEY, first_message_id TEXT NOT NULL, route_key TEXT NOT NULL, received_at TEXT NOT NULL (ISO strings via new Date().toISOString())

## Inbound client frame (concrete)

- Client sends a single JSON object per message that is a full envelope (same keys as EnvelopeSchema); not wrapped in { kind: "envelope" } for this test server (TalkieTransport framing can wrap later).

## Server response frames (concrete JSON shapes)

- Success first insert: {"ok":true,"duplicate":false,"route_key":"<relayRouteKeyFromEnvelope result>"}
- Success duplicate idempotency_key: {"ok":true,"duplicate":true,"route_key":"<same as first>"}
- Schema/version error: {"ok":false,"error":<ParseEnvelopeError value from parseEnvelope — object spread so code and supported_min appear>}
- Oversize: {"ok":false,"error":{"code":"PAYLOAD_TOO_LARGE","max_bytes":65536}}
- Invalid JSON: {"ok":false,"error":{"code":"INVALID_JSON"}}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relay workspace, SQLite schema, and WebSocket server</name>
  <read_first>
    - package.json
    - packages/protocol/package.json
    - packages/protocol/tsconfig.json
    - packages/protocol/src/envelope.ts
    - packages/protocol/src/relay_routing.ts
    - packages/protocol/src/errors.ts
  </read_first>
  <files>packages/relay/package.json, packages/relay/tsconfig.json, packages/relay/src/schema.ts, packages/relay/src/server.ts, package.json</files>
  <action>
    Create packages/relay/package.json: name "@agent-talkie/relay", type "module", dependencies: "@agent-talkie/protocol" workspace link use `"*"` or `"workspace:*"` per existing monorepo convention, "ws": "8.20.0", "better-sqlite3": "11.10.0", devDependencies "@types/node", "@types/ws": "8.18.1", "typescript" align with root 5.9.3; add exports "." -> "./src/server.ts" matching protocol package style.

    Create packages/relay/tsconfig.json mirroring packages/protocol/tsconfig.json with compilerOptions.noEmit set to true (boolean true) so `tsc --noEmit` validates types without emitting files.

    Create packages/relay/src/schema.ts exporting function openIdempotencyDb(path: string) returning better-sqlite3 Database — run DDL CREATE TABLE IF NOT EXISTS relay_idempotency ( idempotency_key TEXT PRIMARY KEY, first_message_id TEXT NOT NULL, route_key TEXT NOT NULL, received_at TEXT NOT NULL ); prepare statements for INSERT OR IGNORE and SELECT by idempotency_key.

    Create packages/relay/src/server.ts exporting async function startRelayServer(opts: { dbPath: string; port?: number }): Promise<{ url: string; close: () => Promise<void> }> — use ws WebSocketServer; listen on opts.port ?? 0; for each connection buffer UTF-8 chunks until a full message where total buffered bytes <= 65536 else send PAYLOAD_TOO_LARGE frame and close with code 1009; parse JSON with try/catch; on failure send INVALID_JSON; call parseEnvelope from @agent-talkie/protocol; if !parsed.ok send {"ok":false,"error":parsed.error}; on ok compute routeKey = relayRouteKeyFromEnvelope(parsed.envelope); attempt INSERT OR IGNORE with idempotency_key, first_message_id = envelope.message_id, route_key = routeKey, received_at ISO; if changes === 1 send success duplicate false with route_key; else SELECT existing row and send success duplicate true with stored route_key (must match relayRouteKeyFromEnvelope for same envelope).

    Add root package.json script "test:relay" with value: `npx vitest run packages/relay` (exact command string).
  </action>
  <acceptance_criteria>
    - Command `grep -q '"name": "@agent-talkie/relay"' packages/relay/package.json` exits 0.
    - Command `grep -q 'better-sqlite3' packages/relay/package.json` exits 0.
    - Command `grep -q '65536' packages/relay/src/server.ts` exits 0.
    - Command `grep -q relay_idempotency packages/relay/src/schema.ts` exits 0.
    - Command `grep -q parseEnvelope packages/relay/src/server.ts` exits 0.
    - Command `grep -q relayRouteKeyFromEnvelope packages/relay/src/server.ts` exits 0.
    - Command `grep -q 'test:relay' package.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm install && npx tsc --noEmit -p packages/relay/tsconfig.json</automated>
  </verify>
  <done>Relay package installs; server module loads; schema defines relay_idempotency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest integration for schema rejection, dedup, and route_key</name>
  <read_first>
    - packages/relay/src/server.ts
    - packages/protocol/src/envelope.ts
    - packages/protocol/src/errors.ts
    - vitest.config.ts
    - packages/protocol/src/envelope.test.ts
  </read_first>
  <files>packages/relay/src/relay.ingest.test.ts, vitest.config.ts</files>
  <behavior>
    - First WebSocket message with valid envelope (schema_version 1, unique message_id UUIDs, same idempotency_key "idem-a") returns JSON with ok true, duplicate false, route_key exactly talkie:v1:control:sp1 for type control.
    - Second message same idempotency_key "idem-a" but new message_id UUID returns JSON ok true, duplicate true.
    - Message with schema_version 999 returns ok false and error.code === "SCHEMA_VERSION_UNSUPPORTED" and error.upgrade_doc_url === "docs/protocol-upgrades.md".
    - Raw payload string of length 65537 bytes returns ok false and error.code === "PAYLOAD_TOO_LARGE" (send as one WS message; build string in test).
  </behavior>
  <action>
    Add packages/relay/src/relay.ingest.test.ts using Vitest; import WebSocket from ws; in beforeAll startRelayServer with dbPath = path.join(os.tmpdir(), `talkie-relay-test-${process.pid}.sqlite`); afterAll await close and unlink db if exists; use wss.url from server.

    Implement four tests matching behavior bullets; use envelope fields: space_id sp1, sender_session_id s1, thread_id th1, timestamp 2026-01-01T00:00:00.000Z, type control, payload null for valid cases; for duplicate test reuse idempotency_key idem-a.

    Ensure root vitest.config.ts (create if missing) includes packages/relay in test include patterns so `npx vitest run packages/relay` discovers relay.ingest.test.ts — if vitest.config.ts exists, append include only as needed without breaking protocol tests.
  </action>
  <acceptance_criteria>
    - Command `grep -q SCHEMA_VERSION_UNSUPPORTED packages/relay/src/relay.ingest.test.ts` exits 0.
    - Command `grep -q PAYLOAD_TOO_LARGE packages/relay/src/relay.ingest.test.ts` exits 0.
    - Command `grep -q 'idem-a' packages/relay/src/relay.ingest.test.ts` exits 0.
    - Command `grep -q talkie:v1:control:sp1 packages/relay/src/relay.ingest.test.ts` exits 0.
    - Command `test -f packages/relay/src/relay.ingest.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm install && npx vitest run packages/relay</automated>
  </verify>
  <done>relay ingest tests pass locally without Docker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket client → relay | Untrusted JSON and size; same machine in tests, network in future |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | T | JSON ingress | mitigate | parseEnvelope + Zod; reject unknown schema with SCHEMA_VERSION_UNSUPPORTED |
| T-01-10 | D | large frames | mitigate | MAX_INGRESS_BYTES 65536; close connection on exceed |
| T-01-11 | T | idempotency replay | mitigate | SQLite PRIMARY KEY on idempotency_key; second insert returns duplicate flag |
| T-01-12 | E | SQLite file path | accept | Test uses temp path; production path selection deferred to later phases |
</threat_model>

<verification>
- npm install at repo root succeeds; npx vitest run packages/relay exits 0; npx vitest run packages/protocol still exits 0.
</verification>

<success_criteria>
PROTO-02: durable dedup via SQLite + duplicate flag; PROTO-04: unsupported schema returns documented error; PROTO-01/03: envelope parse and route_key tie to relay routing module.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-transport-foundation/01-03-SUMMARY.md`
</output>
