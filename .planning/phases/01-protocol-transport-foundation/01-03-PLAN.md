---
phase: "01-protocol-transport-foundation"
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - docker-compose.yml
  - .env.example
  - packages/protocol/package.json
  - packages/protocol/src/jetstream-dedup.integration.test.ts
  - package.json
autonomous: true
requirements:
  - PROTO-02
must_haves:
  truths:
    - "docker compose defines nats:2.12.6 with JetStream enabled on port 4222."
    - "Integration test publishes twice with the same JetStream msgID; second publish ack indicates duplicate per @nats-io/jetstream 3.3.1 API."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Local NATS + Postgres per D-04"
    - path: "packages/protocol/src/jetstream-dedup.integration.test.ts"
      provides: "Transport-level dedup verification"
  key_links:
    - from: "packages/protocol/src/jetstream-dedup.integration.test.ts"
      to: "nats://127.0.0.1:4222"
      via: "NATS_URL"
---

<objective>
Add Docker Compose for NATS 2.12.6 (JetStream) and Postgres 16 (D-04), plus an integration test proving JetStream publish dedup using msgID aligned with idempotency semantics (PROTO-02 transport layer).

Purpose: Validates research assumptions and gives CI/local a reproducible bus.

Output: docker-compose.yml, .env.example, NATS devDependencies, integration test file; optional root script test:jetstream in package.json.
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-protocol-transport-foundation/01-RESEARCH.md
@.planning/phases/01-protocol-transport-foundation/01-CONTEXT.md

## Version pins (concrete)

- NATS image: nats:2.12.6
- Postgres image: postgres:16
- npm devDependencies on @agent-talkie/protocol: @nats-io/transport-node 3.3.1, @nats-io/jetstream 3.3.1

## Stream configuration (concrete)

- Stream name: TALKIE
- Subjects array must include wildcard patterns `talkie.*.control.>` and `talkie.*.conversation.>` (NATS hierarchy wildcard at end).

## Test gating

- Wrap tests in describe.runIf(Boolean(process.env.NATS_URL)) so without NATS_URL the file yields zero failures.
- Subject string for publish: talkie.testspace.conversation.thread_a
- msgID string for both publishes: idem-key-1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker Compose for NATS JetStream and Postgres</name>
  <read_first>
    - .planning/phases/01-protocol-transport-foundation/01-CONTEXT.md
    - .planning/research/STACK.md
  </read_first>
  <files>docker-compose.yml, .env.example</files>
  <action>
    Create docker-compose.yml at repo root:

    - Service nats: image nats:2.12.6, command "-js" and "-m" "8222" as separate argv entries, ports 4222:4222 and 8222:8222.

    - Service postgres: image postgres:16, environment POSTGRES_USER talkie, POSTGRES_PASSWORD talkiedev, POSTGRES_DB talkie, ports 5432:5432, named volume for pgdata.

    Create .env.example with lines NATS_URL=nats://127.0.0.1:4222 and DATABASE_URL=postgresql://talkie:talkiedev@127.0.0.1:5432/talkie (exact URLs).
  </action>
  <acceptance_criteria>
    - Command `grep -q 'nats:2.12.6' docker-compose.yml` exits 0.
    - Command `grep -q '\-js' docker-compose.yml` exits 0.
    - Command `grep -q '4222:4222' docker-compose.yml` exits 0.
    - Command `grep -q 'postgres:16' docker-compose.yml` exits 0.
    - Command `grep -q 'NATS_URL=' .env.example` exits 0.
    - Command `grep -q 'postgresql://talkie:talkiedev@' .env.example` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && (docker compose config -q 2>/dev/null || docker-compose config -q 2>/dev/null || grep -q nats:2.12.6 docker-compose.yml)</automated>
  </verify>
  <done>Compose file present; env example documents connection strings.</done>
</task>

<task type="auto">
  <name>Task 2: JetStream dedup integration test and NATS devDependencies</name>
  <read_first>
    - packages/protocol/package.json
    - packages/protocol/src/subjects.ts
    - docker-compose.yml
    - package.json
  </read_first>
  <files>packages/protocol/package.json, packages/protocol/src/jetstream-dedup.integration.test.ts, package.json</files>
  <action>
    Add to packages/protocol/package.json devDependencies: "@nats-io/transport-node": "3.3.1", "@nats-io/jetstream": "3.3.1". Run npm install at repo root.

    Create packages/protocol/src/jetstream-dedup.integration.test.ts: use describe.runIf(Boolean(process.env.NATS_URL)); connect with servers from process.env.NATS_URL; use jetstreamManager to add stream TALKIE with subjects covering talkie star control and conversation hierarchies per CONTEXT; use jetstream publish twice to subject talkie.testspace.conversation.thread_a with same msgID idem-key-1; assert second ack duplicate property is true per actual PubAck type in 3.3.1; close connection in afterAll.

    Add root package.json script test:jetstream with value: NATS_URL=nats://127.0.0.1:4222 npx vitest run packages/protocol/src/jetstream-dedup.integration.test.ts
  </action>
  <acceptance_criteria>
    - Command `grep -q '@nats-io/transport-node' packages/protocol/package.json` exits 0.
    - Command `test -f packages/protocol/src/jetstream-dedup.integration.test.ts` exits 0.
    - Command `grep -q TALKIE packages/protocol/src/jetstream-dedup.integration.test.ts` exits 0.
    - Command `grep -q msgID packages/protocol/src/jetstream-dedup.integration.test.ts` exits 0.
    - Command `grep -q duplicate packages/protocol/src/jetstream-dedup.integration.test.ts` exits 0.
    - Command `grep -q test:jetstream package.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npx vitest run packages/protocol/src/jetstream-dedup.integration.test.ts</automated>
  </verify>
  <done>Without NATS_URL vitest exits 0 (skipped); with docker compose up nats and NATS_URL set, test:jetstream passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Docker | Dev-only credentials in .env.example |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | I | Postgres talkiedev password | accept | Dev-only; document not for production |
</threat_model>

<verification>
- With NATS running: npm run test:jetstream passes.
- Without NATS_URL: npx vitest run packages/protocol/src/jetstream-dedup.integration.test.ts exits 0.
</verification>

<success_criteria>
PROTO-02 transport dedup path demonstrated; D-04 local infra available for later phases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-transport-foundation/01-03-SUMMARY.md`
</output>
