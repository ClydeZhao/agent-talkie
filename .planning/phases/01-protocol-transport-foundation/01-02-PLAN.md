---
phase: "01-protocol-transport-foundation"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/protocol/src/subjects.ts
  - packages/protocol/src/subjects.test.ts
  - packages/protocol/src/index.ts
  - packages/protocol/README.md
autonomous: true
requirements:
  - PROTO-03
must_haves:
  truths:
    - "controlSubject(spaceId, eventType) returns talkie.{spaceId}.control.{eventType} for valid tokens."
    - "conversationSubject(spaceId, threadId) returns talkie.{spaceId}.conversation.{threadId} for valid tokens."
    - "Invalid space_id, event_type, or thread_id throws; tests assert rejection."
  artifacts:
    - path: "packages/protocol/src/subjects.ts"
      provides: "NATS subject builders for D-07/D-08"
  key_links:
    - from: "packages/protocol/src/subjects.ts"
      to: "talkie.{space_id}.control|conversation.*"
      via: "template string builders"
---

<objective>
Implement centralized NATS subject construction separating control vs conversation hierarchies (PROTO-03, D-07, D-08).

Purpose: Prevents ad-hoc string concatenation and subject-injection issues called out in 01-RESEARCH.

Output: subjects.ts, tests, package README section documenting the split.
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-protocol-transport-foundation/01-CONTEXT.md
@packages/protocol/src/envelope.ts

## Locked decisions

- **D-07:** Separate hierarchies for control vs conversation.
- **D-08:** Patterns talkie.{space_id}.control.{event_type} and talkie.{space_id}.conversation.{thread_id}.

## Token rules (concrete)

- space_id: regex ^[a-zA-Z0-9_-]{1,128}$ (must match envelope).
- event_type: regex ^[a-z][a-z0-9_]{0,63}$ (examples: join, leave, metadata_update).
- thread_id: same regex as event_type.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subject builders and validation</name>
  <read_first>
    - packages/protocol/src/envelope.ts
    - .planning/phases/01-protocol-transport-foundation/01-CONTEXT.md
    - packages/protocol/src/index.ts
  </read_first>
  <files>packages/protocol/src/subjects.ts, packages/protocol/src/index.ts, packages/protocol/README.md</files>
  <action>
    Create packages/protocol/src/subjects.ts: private helper assertToken(name, value, pattern) throws Error with message starting with literal [talkie:subject] when value fails pattern.

    Export controlSubject(spaceId, eventType): validate spaceId with /^[a-zA-Z0-9_-]{1,128}$/, eventType with /^[a-z][a-z0-9_]{0,63}$/, return template literal talkie.${spaceId}.control.${eventType}.

    Export conversationSubject(spaceId, threadId): validate threadId with same pattern as eventType, return talkie.${spaceId}.conversation.${threadId}.

    Update packages/protocol/src/index.ts to export controlSubject and conversationSubject.

    Create packages/protocol/README.md with markdown heading "## NATS subjects" (exact) and body containing literal strings talkie.{space_id}.control.{event_type} and talkie.{space_id}.conversation.{thread_id} and reference D-08.
  </action>
  <acceptance_criteria>
    - Command `grep -q controlSubject packages/protocol/src/subjects.ts` exits 0.
    - Command `grep -q conversationSubject packages/protocol/src/subjects.ts` exits 0.
    - Command `grep -q '\[talkie:subject\]' packages/protocol/src/subjects.ts` exits 0.
    - Command `grep -q "## NATS subjects" packages/protocol/README.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && test -f packages/protocol/src/subjects.ts && grep -q controlSubject packages/protocol/src/subjects.ts</automated>
  </verify>
  <done>Builders implemented; README documents patterns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Subject unit tests</name>
  <read_first>
    - packages/protocol/src/subjects.ts
    - packages/protocol/src/index.ts
  </read_first>
  <files>packages/protocol/src/subjects.test.ts</files>
  <behavior>
    - controlSubject("sp1", "join") equals string talkie.sp1.control.join
    - conversationSubject("sp1", "thread_a") equals talkie.sp1.conversation.thread_a
    - controlSubject("bad.id", "join") throws
    - controlSubject("sp1", "Join") throws
  </behavior>
  <action>
    Implement packages/protocol/src/subjects.test.ts with Vitest describe block named subjects containing tests for the four behavior bullets; use expect with throwing wrapper for error cases per Vitest docs.
  </action>
  <acceptance_criteria>
    - Command `grep -q talkie.sp1.control.join packages/protocol/src/subjects.test.ts` exits 0.
    - Command `grep -q talkie.sp1.conversation.thread_a packages/protocol/src/subjects.test.ts` exits 0.
    - Command `grep -q bad.id packages/protocol/src/subjects.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npx vitest run packages/protocol -t subjects</automated>
  </verify>
  <done>Subject tests pass in isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-supplied IDs to subject string | Unvalidated IDs could widen NATS subscriptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | E | controlSubject, conversationSubject | mitigate | Regex validation before string interpolation |
</threat_model>

<verification>
- npx vitest run packages/protocol -t subjects passes.
</verification>

<success_criteria>
PROTO-03 met: control vs conversation subject families are encoded in one module with automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-transport-foundation/01-02-SUMMARY.md`
</output>
