---
phase: 08-dashboard-distribution-cli-entry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/vite.app.config.ts
  - packages/dashboard/package.json
  - packages/dashboard/src/demo/main.ts
  - package.json
autonomous: true
requirements: [CONN-03]
must_haves:
  truths:
    - "`npm run build -w @agent-talkie/dashboard` 同时产出 `dist/`（lib）与 `dist-app/`（SPA），且 `dist-app/index.html` 中资源引用以 `/dashboard/` 为 base"
    - "生产入口脚本在 `import.meta.env.PROD` 下用 `location.host` 构造 `wsUrl`，开发模式仍连 `ws://127.0.0.1:18765`"
    - "仓库根 `npm run build` 在编译 relay 之前完成 dashboard 构建，保证下游包能解析到 `dist-app`"
  artifacts:
    - path: packages/dashboard/vite.app.config.ts
      provides: "Vite application 构建：base /dashboard/、outDir dist-app"
      contains: "dist-app"
    - path: packages/dashboard/dist-app/index.html
      provides: "可托管的 SPA 入口"
      contains: "/dashboard/"
  key_links:
    - from: packages/dashboard/vite.app.config.ts
      to: packages/dashboard/index.html
      via: "Rollup input index.html"
---

<objective>
为 `@agent-talkie/dashboard` 增加第二条 Vite 构建管线（application 模式），输出到 `dist-app/`，`base: '/dashboard/'`；更新 demo 入口 WebSocket URL 以满足同源生产部署；调整 monorepo 根构建顺序。

Purpose: 为 CONN-03 提供可被 relay 托管的静态产物；避免 `emptyOutDir` 互删（lib 用 `dist/`，app 用 `dist-app/`）。
Output: `vite.app.config.ts`、更新的 npm scripts、`dist-app` 产物约定、根 `package.json` 构建顺序。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dashboard-distribution-cli-entry/08-CONTEXT.md
@.planning/phases/08-dashboard-distribution-cli-entry/08-RESEARCH.md

<interfaces>
自 `packages/dashboard/vite.config.ts`（lib 构建，须保留）：

```typescript
export default defineConfig({
  build: { outDir: "dist", emptyOutDir: true, lib: { entry: resolve(__dirname, "src/index.ts"), formats: ["es"], fileName: "index" } },
  server: { port: 5174, proxy: { "/__agent-talkie": { target: "http://127.0.0.1:18765", changeOrigin: true } } },
});
```

自 `packages/dashboard/src/demo/main.ts`：当前 `const wsUrl = "ws://127.0.0.1:18765";` 须改为 dev/prod 分支（见 action）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vite app 配置与 npm scripts</name>
  <read_first>
    - packages/dashboard/vite.config.ts
    - packages/dashboard/package.json
    - packages/dashboard/index.html
  </read_first>
  <files>packages/dashboard/vite.app.config.ts, packages/dashboard/package.json</files>
  <action>
    1. 新建 `packages/dashboard/vite.app.config.ts`，使用 `import { defineConfig } from "vite"`、`import { resolve, dirname } from "node:path"`、`import { fileURLToPath } from "node:url"`，`const __dirname = dirname(fileURLToPath(import.meta.url))`，`export default defineConfig({ root: ".", base: "/dashboard/", build: { outDir: "dist-app", emptyOutDir: true, rollupOptions: { input: resolve(__dirname, "index.html") } }, server: { port: 5174, proxy: { "/__agent-talkie": { target: "http://127.0.0.1:18765", changeOrigin: true } } } });`（proxy 与现有 `vite.config.ts` 对齐，便于 dev）。
    2. 修改 `packages/dashboard/package.json`：`"scripts"` 中把 `"build": "vite build"` 改为 `"build:lib": "vite build"`、`"build:app": "vite build --config vite.app.config.ts"`、`"build": "npm run build:lib && npm run build:app"`。
    3. 在 `packages/dashboard/package.json` 顶层增加 `"files": ["dist", "dist-app"]`（若已有 `files` 则合并为同时包含二者）。
  </action>
  <verify>
    <automated>npm run build -w @agent-talkie/dashboard</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `packages/dashboard/vite.app.config.ts` 存在且含子串 `base: "/dashboard/"`
    - 文件 `packages/dashboard/vite.app.config.ts` 含子串 `outDir: "dist-app"`
    - `packages/dashboard/package.json` 的 `scripts.build` 含子串 `build:lib` 与 `build:app`
    - `packages/dashboard/package.json` 的 `files` 数组（或等价字段）同时包含 `"dist"` 与 `"dist-app"`
    - 路径 `packages/dashboard/dist-app/index.html` 在构建后存在
    - `packages/dashboard/dist-app/index.html` 内容含子串 `/dashboard/`（在 script 或 link 的 href/src 中）
    - `npm run build -w @agent-talkie/dashboard` 退出码 0
  </acceptance_criteria>
  <done>双产物目录 `dist` + `dist-app` 可通过单条 workspace build 命令生成。</done>
</task>

<task type="auto">
  <name>Task 2: demo WebSocket 同源与根构建顺序</name>
  <read_first>
    - packages/dashboard/src/demo/main.ts
    - package.json
    - packages/dashboard/src/bridge/derive-http-origin.ts
  </read_first>
  <files>packages/dashboard/src/demo/main.ts, package.json</files>
  <action>
    1. 在 `packages/dashboard/src/demo/main.ts` 中，将 `const wsUrl = "ws://127.0.0.1:18765";` 替换为：
       ```typescript
       const wsUrl = import.meta.env.DEV
         ? "ws://127.0.0.1:18765"
         : `${location.protocol === "https:" ? "wss:" : "ws:"}//${location.host}`;
       ```
       保持其余 `deriveHttpOriginFromWsUrl(wsUrl)` 与 bridge 逻辑不变。
    2. 修改仓库根目录 `package.json` 的 `scripts.build`：将片段 `npm run build -w @agent-talkie/relay` 与 `npm run build -w @agent-talkie/dashboard` 交换顺序，使 **`@agent-talkie/dashboard` 在 `@agent-talkie/relay` 之前** 构建（其余 workspace 顺序不变）。
  </action>
  <verify>
    <automated>npm run build -w @agent-talkie/dashboard && npm test -w @agent-talkie/dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/src/demo/main.ts` 含子串 `import.meta.env.DEV`
    - `packages/dashboard/src/demo/main.ts` 含子串 `location.host`
    - 根目录 `package.json` 的 `scripts.build` 中，`@agent-talkie/dashboard` 子串出现在 `@agent-talkie/relay` 子串之前
    - `npm test -w @agent-talkie/dashboard` 退出码 0
  </acceptance_criteria>
  <done>生产构建下 WS 与 HTTP 同 host:port；根构建顺序满足 relay 运行时期望。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | T | `base` 错配导致 asset 404 | mitigate | `base: '/dashboard/'` 与 D-01 一致；以构建产物 HTML 内 `/dashboard/` 前缀验收 |
</threat_model>

<verification>
`npm run build -w @agent-talkie/dashboard` 与 `npm test -w @agent-talkie/dashboard` 通过。
</verification>

<success_criteria>
- `dist-app/index.html` 存在且引用路径带 `/dashboard/`。
- demo 在生产与开发下的 WS 行为符合 RESEARCH 与 D-01。
</success_criteria>

<output>
完成后创建 `.planning/phases/08-dashboard-distribution-cli-entry/08-02-SUMMARY.md`
</output>
