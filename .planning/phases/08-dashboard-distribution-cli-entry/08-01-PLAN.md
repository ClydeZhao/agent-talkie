---
phase: 08-dashboard-distribution-cli-entry
plan: 01
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - packages/relay/package.json
  - packages/relay/src/server.ts
  - packages/relay/src/server.test.ts
autonomous: true
requirements: [CONN-03]
must_haves:
  truths:
    - "对任意非 WebSocket 升级的 HTTP 请求，relay 要么返回 dashboard 静态内容，要么返回明确 HTTP 响应（含 404），连接不会无限挂起"
    - "GET /dashboard 与 GET /dashboard/ 能返回 SPA 的 index.html（含 text/html）"
    - "GET /__agent-talkie/v1/health 在带 relayGenerationToken 时行为与现有一致（200/403/405）"
    - "WebSocket 升级请求仍由 ws 处理，不经静态中间件写 res"
  artifacts:
    - path: packages/relay/package.json
      provides: "依赖 sirv@^3.0.2 与 workspace 依赖 @agent-talkie/dashboard@0.0.0；pretest 含 dashboard 构建"
      contains: '"sirv"'
    - path: packages/relay/src/server.ts
      provides: "/dashboard 静态挂载与 SPA fallback、统一 request 处理链"
      contains: "/dashboard"
  key_links:
    - from: packages/relay/src/server.ts
      to: "@agent-talkie/dashboard 包内 dist-app"
      via: "createRequire(import.meta.url).resolve('@agent-talkie/dashboard/package.json') + path.join(dirname, 'dist-app')"
---

<objective>
在 relay 的裸 `http.Server` 上挂载 `/dashboard` 前缀的静态资源（D-01、D-03），不改动 `/__agent-talkie/v1/health` 与 WebSocket 升级路径（D-02）；静态根目录解析到已安装包内的 `dist-app`（满足 npm/npx 下路径稳定）。

Purpose: 满足 CONN-03（生产同源静态托管）与 CONTEXT 锁定决策 D-01～D-03。
Output: 可测试的 relay HTTP 行为与依赖声明。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-dashboard-distribution-cli-entry/08-CONTEXT.md
@.planning/phases/08-dashboard-distribution-cli-entry/08-RESEARCH.md

<interfaces>
自 `packages/relay/src/server.ts`（当前实现，执行前以 read_first 为准）：

```typescript
export const LISTEN_HOST = "127.0.0.1";
export async function createRelayServer(opts: {
  dbPath: string;
  port?: number;
  pepper?: string;
  relayGenerationToken?: string;
  idleShutdownMs?: number;
  onIdleShutdown?: () => void | Promise<void>;
}): Promise<{ url: string; dbPath: string; close: () => Promise<void> }>;
```

健康检查分支当前仅在 `opts.relayGenerationToken` 为非空 string 时注册；非 health 路径曾 `return` 不写响应（Pitfall 1）。合并后须对「非 upgrade、非 health、非 /dashboard*」返回 `404` 并 `res.end()`。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: relay 依赖与 /dashboard sirv 挂载</name>
  <read_first>
    - packages/relay/src/server.ts
    - packages/relay/package.json
    - packages/dashboard/package.json
    - .planning/phases/08-dashboard-distribution-cli-entry/08-CONTEXT.md
  </read_first>
  <files>packages/relay/package.json, packages/relay/src/server.ts</files>
  <behavior>
    - Vitest：对 `createRelayServer({ dbPath, port: 0, relayGenerationToken: "a".repeat(32) })`，`fetch(httpOrigin + "/__agent-talkie/v1/health?generation=" + token)` 仍为 200 JSON `{ ok: true }` 语义与现有一致。
    - Vitest：`GET /dashboard` 与 `GET /dashboard/deep/route` 返回 `text/html` 且 body 含子串 `Agent Talkie Dashboard`（与 `packages/dashboard/index.html` 标题一致），且状态码 200（由 SPA `single: true` 回落 index.html 实现 D-03）。
    - Vitest：`GET /` 与 `GET /nope` 返回 404，响应结束（非 pending）。
    - Vitest：`req.headers.upgrade === "websocket"` 的请求不进入 sirv（可只断言未 crash；或与现有 WS 测试共存）。
  </behavior>
  <action>
    1. 在 `packages/relay/package.json` 的 `dependencies` 增加 `"sirv": "^3.0.2"` 与 `"@agent-talkie/dashboard": "0.0.0"`（workspace 版本与 persistence 等包一致）。
    2. 将 `packages/relay/package.json` 的 `pretest` 从仅 `protocol`+`persistence` 扩展为末尾追加：`&& npm run build -w @agent-talkie/dashboard`，保证 `dist-app` 在 relay 测试前存在。
    3. 在 `packages/relay/src/server.ts` 文件顶部增加：`import { createRequire } from "node:module"`、`import { dirname, join } from "node:path"`、`import sirv from "sirv"`；在模块级 `const require = createRequire(import.meta.url);`，新增函数体：
       `function resolveDashboardAppDir(): string { const pkg = require.resolve("@agent-talkie/dashboard/package.json"); return join(dirname(pkg), "dist-app"); }`
    4. 在 `createRelayServer` 内于 `http.createServer()` 之后，**统一**注册单个 `server.on("request", (req, res) => { ... })`，顺序严格为：
       - 若 `(req.headers.upgrade ?? "").toLowerCase() === "websocket"`：`return`（不写 `res`）。
       - `const url = new URL(req.url ?? "/", "http://127.0.0.1");`
       - 若 `url.pathname === "/__agent-talkie/v1/health"` 且 `typeof token === "string" && token.length > 0`：保留现有 GET 校验 `generation === token` 返回 200/403、非 GET 返回 405 的逻辑（路径与 JSON 体与当前行 210–226 语义一致）。
       - 若 `url.pathname === "/dashboard" || url.pathname.startsWith("/dashboard/")`：构造 `sirv(resolveDashboardAppDir(), { single: true, dev: false })` 的单例（可模块级缓存）；将 `req.url` 临时改写为 `url.pathname.slice("/dashboard".length) || "/"` 加上 `url.search`（或等价方式），调用 `assets(req, res, () => { res.statusCode = 404; res.end(); })`，在回调前恢复 `req.url`。
       - 否则：`res.writeHead(404, { "Content-Type": "text/plain; charset=utf-8" }); res.end("Not Found");`
    5. **删除**原先仅在 `if (token)` 块内注册的重复 `server.on("request", ...)`，避免双监听器；健康检查逻辑并入上述统一处理器。
  </action>
  <verify>
    <automated>npm run test -w @agent-talkie/relay</automated>
  </verify>
  <acceptance_criteria>
    - `packages/relay/package.json` 含子串 `"sirv": "^3.0.2"`
    - `packages/relay/package.json` 含子串 `"@agent-talkie/dashboard": "0.0.0"`
    - `packages/relay/package.json` 的 `pretest` 含子串 `npm run build -w @agent-talkie/dashboard`
    - `packages/relay/src/server.ts` 含子串 `sirv(` 且含子串 `"/dashboard"`
    - `packages/relay/src/server.ts` 含子串 `resolve("@agent-talkie/dashboard/package.json")` 或含 `join(dirname(pkg), "dist-app")`
    - `npm run test -w @agent-talkie/relay` 退出码 0
  </acceptance_criteria>
  <done>CONN-03 的 relay 侧同源静态路径与 SPA fallback 已实现；HTTP 无挂起分支；Vitest 覆盖 health、/dashboard、404。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP 客户端 → relay | 未信任路径与 Method；仅允许公开静态与 health |

## STRIDE（最小）

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | `/dashboard` 静态 | mitigate | 根目录固定为包内 `dist-app`；使用 sirv，不拼接用户路径到根外 |
| T-08-02 | I | health `generation` | mitigate | 保持与现有实现一致的 token 校验 |
</threat_model>

<verification>
`npm run test -w @agent-talkie/relay` 通过；手动可选：`npm run build -w @agent-talkie/dashboard && npm run build -w @agent-talkie/relay` 无报错。
</verification>

<success_criteria>
- 锁定决策 D-01（`/dashboard` 前缀）、D-02（health/WS 路径不变）、D-03（SPA fallback）在代码中可验证。
- relay 测试证明非匹配 HTTP 路径返回 404 而非挂起。
</success_criteria>

<output>
完成后创建 `.planning/phases/08-dashboard-distribution-cli-entry/08-01-SUMMARY.md`
</output>
