---
phase: 09-core-oversight-ui
plan: 04
type: execute
wave: 4
depends_on:
  - "09-03"
files_modified:
  - packages/dashboard/src/bridge/wire-schemas.ts
  - packages/dashboard/src/bridge/browser-session-bridge.ts
  - packages/dashboard/src/errors/relay-error-copy.ts
  - packages/dashboard/src/errors/talkie-error-bar.ts
  - packages/dashboard/src/store/dashboard-store.ts
  - packages/dashboard/src/demo/main.ts
autonomous: true
requirements:
  - OVER-07
user_setup: []
must_haves:
  truths:
    - "post-handshake `dispatchPostHandshake` 在 `safeParseEnvelope` 之前识别 `type:\"protocol.error\"` 并回调监听器"
    - "静态表 `RELAY_ERROR_COPY` 覆盖下列 error 字符串键（每键有 `title` 与 `hint` 与 `sticky` 布尔）：not_in_space, no_orchestrator, orchestrator_offline, invalid_envelope, invalid_handshake, invalid_json, invalid_session_message, resume_rejected, envelope_version_mismatch, session_mismatch, expected_session_register_or_resume, not_space_owner, orchestrator_designate_forbidden, orchestrator_target_invalid, task_assign_forbidden, metadata_patch_forbidden, idempotency_replay_mismatch, invalid_slug, already_in_space"
    - "瞬态错误 8000ms 后移除；粘性错误保留至用户点击关闭或调用 clear API"
    - "错误条最多展示 3 条，新错误置顶；组件标签名 `talkie-error-bar`"
  artifacts:
    - path: packages/dashboard/src/errors/relay-error-copy.ts
      provides: "RELAY_ERROR_COPY + sticky 集合"
    - path: packages/dashboard/src/errors/talkie-error-bar.ts
      provides: "堆叠 UI + 计时器"
    - path: packages/dashboard/src/bridge/browser-session-bridge.ts
      provides: "onProtocolError"
  key_links:
    - from: packages/dashboard/src/bridge/browser-session-bridge.ts
      to: "talkie-error-bar"
      via: "DashboardStore.pushProtocolError → UI"
---

<objective>
交付 OVER-07：协议错误的人话映射与错误条（D-18–D-21），含 bridge 对 `protocol.error` 帧的分发（修复 09-RESEARCH 所述静默丢弃）。

Purpose: 运维可理解 `no_orchestrator` / `not_in_space` 等失败并获知恢复提示。

Output: `protocolErrorWireSchema`、`onProtocolError`、`RELAY_ERROR_COPY`、`talkie-error-bar`、store 错误队列。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-core-oversight-ui/09-CONTEXT.md
@.planning/phases/09-core-oversight-ui/09-RESEARCH.md

<interfaces>
Wire 形状（relay 已发送）：`{ "type": "protocol.error", "error": "<code>" }`，可选扩展字段忽略即可。

`dispatchPostHandshake` 中须在 **处理 `transcriptCatchupMessageSchema` 之前** 分支：若 `protocolErrorWireSchema.safeParse(parsed).success`，调用 `protocolErrorListeners` 并 `return`，且**不得**落入 `safeParseEnvelope`。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: wire-schema 与 bridge 回调</name>
  <files>packages/dashboard/src/bridge/wire-schemas.ts, packages/dashboard/src/bridge/browser-session-bridge.ts</files>
  <read_first>
    - packages/dashboard/src/bridge/browser-session-bridge.ts
    - packages/dashboard/src/bridge/wire-schemas.ts
  </read_first>
  <action>
    1. 在 `wire-schemas.ts` 增加：
    ```typescript
    export const protocolErrorWireSchema = z.object({
      type: z.literal("protocol.error"),
      error: z.string(),
    });
    export type ProtocolErrorWire = z.infer<typeof protocolErrorWireSchema>;
    ```

    2. 在 `BrowserSessionBridge` 中增加 `private readonly protocolErrorListeners = new Set<(p: ProtocolErrorWire) => void>()` 与公有方法 `onProtocolError(cb: (p: ProtocolErrorWire) => void): () => void`（返回 unsubscribe 函数）。

    3. 在 `dispatchPostHandshake` 内、`pendingJoin` 块结束之后、`transcriptCatchupMessageSchema.safeParse` 之前，插入：若 `protocolErrorWireSchema.safeParse(parsed)` 成功，则对每个 listener 调用 `cb(pe.data)`，然后 **`return`**。

    4. 确保 `envelope_version_mismatch` 的既有 stale 逻辑（设置 `setStaleReason("protocol_version")`）**仍**在该分支前或分支内执行：若当前代码在更早处检测，保持不变；若仅在后段，则在 `protocol.error` 分支内当 `pe.data.error === "envelope_version_mismatch"` 时调用 `this.setStaleReason("protocol_version")` 再 notify。

    5. **不得**在 `pendingRegister` / `pendingResume` / `pendingJoin` 仍为真时改变现有 reject 行为；仅当三者皆空时新分支消费「路由期」`protocol.error`。
  </action>
  <verify>
    <automated>npm test -w @agent-talkie/dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/src/bridge/wire-schemas.ts` 含 `protocolErrorWireSchema`
    - `packages/dashboard/src/bridge/browser-session-bridge.ts` 含 `onProtocolError`
    - `dispatchPostHandshake` 方法体内，`transcriptCatchupMessageSchema` 字符串出现在 `protocolErrorWireSchema` 相关分支之后（即 grep 显示 `protocolErrorWireSchema` 行号小于 `transcriptCatchupMessageSchema` 行号）
    - `npm test -w @agent-talkie/dashboard` 退出码 0
  </acceptance_criteria>
  <done>非握手挂起状态下 `protocol.error` 到达 UI 层监听器。</done>
</task>

<task type="auto">
  <name>Task 2: RELAY_ERROR_COPY 与中文文案</name>
  <files>packages/dashboard/src/errors/relay-error-copy.ts</files>
  <read_first>
    - packages/relay/src/router.ts
    - packages/relay/src/collaboration-handlers.ts
    - packages/relay/src/server.ts
    - packages/relay/src/space-lifecycle.ts
    - .planning/phases/09-core-oversight-ui/09-CONTEXT.md
  </read_first>
  <action>
    新建 `packages/dashboard/src/errors/relay-error-copy.ts`，导出：

    - `export type RelayErrorCopy = { title: string; hint: string; sticky: boolean }`
    - `export const RELAY_ERROR_COPY: Record<string, RelayErrorCopy>` **必须**恰好包含下列键（一字不差，全小写+下划线）：
      `not_in_space`, `no_orchestrator`, `orchestrator_offline`, `invalid_envelope`, `invalid_handshake`, `invalid_json`, `invalid_session_message`, `resume_rejected`, `envelope_version_mismatch`, `session_mismatch`, `expected_session_register_or_resume`, `not_space_owner`, `orchestrator_designate_forbidden`, `orchestrator_target_invalid`, `task_assign_forbidden`, `metadata_patch_forbidden`, `idempotency_replay_mismatch`, `invalid_slug`, `already_in_space`

    - 每个键的 `title` 与 `hint` 为 **简体中文** 句子；`sticky === true` 的键集合必须**恰好**为：`resume_rejected`, `session_mismatch`, `expected_session_register_or_resume`, `envelope_version_mismatch`, `not_in_space`。其余键 `sticky === false`。

    - `no_orchestrator` 的 `title` 须包含子串 **`未指定编排者`**；`not_in_space` 的 `title` 须包含 **`不在该空间`**。

    - 导出函数 `getRelayErrorCopy(code: string): RelayErrorCopy`：若 `RELAY_ERROR_COPY[code]` 存在则返回；否则返回 `{ title: \`未知错误: ${code}\`, hint: "请查看 relay 日志或刷新页面。", sticky: false }`。
  </action>
  <verify>
    <automated>npm test -w @agent-talkie/dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/src/errors/relay-error-copy.ts` 含 `RELAY_ERROR_COPY`
    - 同文件含字面量 `未指定编排者` 与 `不在该空间`
    - 同文件含 `sticky: true` 且同一文件 grep `resume_rejected` 至少一行含 `sticky`
    - `npm test -w @agent-talkie/dashboard` 退出码 0
  </acceptance_criteria>
  <done>错误码枚举可审计且与当前 relay 发送码对齐。</done>
</task>

<task type="auto">
  <name>Task 3: talkie-error-bar、store 与 demo</name>
  <files>packages/dashboard/src/errors/talkie-error-bar.ts, packages/dashboard/src/store/dashboard-store.ts, packages/dashboard/src/demo/main.ts</files>
  <read_first>
    - packages/dashboard/src/store/dashboard-store.ts
    - packages/dashboard/src/demo/main.ts
    - packages/dashboard/index.html
    - packages/dashboard/src/shell/connection-shell.ts
  </read_first>
  <action>
    1. 在 `DashboardStore` 增加结构：`errors: Array<{ id: string; code: string; title: string; hint: string; sticky: boolean; receivedAtMs: number }>`，方法 `pushProtocolError(wire: import("../bridge/wire-schemas.js").ProtocolErrorWire): void`：用 `crypto.randomUUID()` 生成 `id`，`getRelayErrorCopy(wire.error)` 填充字段，**新错误 unshift 到数组头部**，然后 `slice(0, 3)` **截断为最多 3 条**（最旧丢弃）。非 sticky 项在入队后启动 `setTimeout(..., 8000)` 按 `id` 移除；sticky 不自动移除。

    2. 新建 `talkie-error-bar.ts`（`@customElement("talkie-error-bar")`）：接收 `store`；渲染 store.errors 每条显示 `title` 与 `hint`；每条 sticky 显示按钮文案 **`关闭`**，点击调用 `store.dismissError(id)`；非 sticky 显示可选 **`知道了`** 同样 dismiss。样式：全宽条带，位于 header 下方（由 demo 布局保证顺序）。仅用文本节点，禁止 `unsafeHTML`。

    3. 修改 `packages/dashboard/src/demo/main.ts`：`#app` 内顺序为：第一子节点 `talkie-connection-shell`，第二子节点 `talkie-error-bar`，第三子节点为 **flex 行容器**（内含左 roster + 右 main）（若当前结构不同则调整为该顺序以满足 D-03「错误条在顶栏下」）。

    4. `bridge.onProtocolError((w) => store.pushProtocolError(w))`。

    5. `store` 在 dismiss 与超时时触发与 roster 相同的 UI 通知。
  </action>
  <verify>
    <automated>npm run build -w @agent-talkie/dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/src/errors/talkie-error-bar.ts` 含 `talkie-error-bar`
    - 同文件含字面量 `关闭`
    - `packages/dashboard/src/store/dashboard-store.ts` 含 `pushProtocolError`
    - 同文件含 `slice(0, 3)`
    - `packages/dashboard/src/demo/main.ts` 含 `onProtocolError`
    - `npm run build -w @agent-talkie/dashboard` 退出码 0
  </acceptance_criteria>
  <done>错误条可见、可 dismiss、瞬态 8s 消失、最多三条。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protocol.error → UI | 仅展示映射后字符串，不回显原始 WebSocket 二进制 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Information disclosure | error 帧可能含敏感上下文 | accept | localhost；不记录到 localStorage |
| T-09-04-02 | Spoofing | 伪造 error 字符串 | mitigate | 文案来自静态表；未知码显示中性模板 |
</threat_model>

<verification>
`npm test -w @agent-talkie/dashboard` 与 `npm run build -w @agent-talkie/dashboard` 均成功。
</verification>

<success_criteria>
人为触发 `no_orchestrator` 等 relay 错误时，错误条在 8s 内出现映射中文；粘性错误需点击关闭。
</success_criteria>

<output>
完成后创建 `.planning/phases/09-core-oversight-ui/09-04-SUMMARY.md`
</output>
