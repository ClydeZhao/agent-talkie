---
phase: 05-cross-runtime-proof-human-oversight
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - package.json
  - packages/persistence/src/repositories/oversight.ts
  - packages/persistence/src/repositories/oversight.test.ts
  - packages/persistence/src/index.ts
  - packages/adapter-cursor-mcp/package.json
  - packages/adapter-cursor-mcp/tsconfig.json
  - packages/adapter-cursor-mcp/tsup.config.ts
  - packages/adapter-cursor-mcp/src/mcp-server.ts
  - packages/adapter-cursor-mcp/src/index.ts
  - packages/adapter-cursor-mcp/src/mcp-server.test.ts
autonomous: true
requirements:
  - ADAPT-02
  - OVER-02
  - OVER-03
user_setup:
  - service: cursor-mcp-host
    why: "End-to-end MCP tool/resource invocation from Cursor"
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - "Package @agent-talkie/adapter-cursor-mcp exposes an MCP stdio server using @modelcontextprotocol/sdk ^1.29.0"
    - "MCP tools named join_space, send_message, assign_orchestrator, update_metadata exist and map to relay protocol envelopes"
    - "MCP resources expose read-only collaboration snapshots without pushing full transcript into model context by default (pagination + explicit URIs)"
    - "Persistence oversight helpers support SQL-backed reads for MCP resources and later CLI"
  artifacts:
    - path: "packages/adapter-cursor-mcp/src/mcp-server.ts"
      provides: "McpServer wiring, tools, resources"
    - path: "packages/persistence/src/repositories/oversight.ts"
      provides: "Read-only snapshot queries by space slug"
  key_links:
    - from: "packages/adapter-cursor-mcp/src/mcp-server.ts"
      to: "TalkieSessionClient.sendEnvelope"
      via: "tool handlers"
    - from: "packages/adapter-cursor-mcp/src/mcp-server.ts"
      to: "packages/persistence/src/repositories/oversight.ts"
      via: "resource read handlers"
---

<objective>
Ship the **MCP-shaped** proof adapter for ADAPT-02 (CONTEXT D-01/D-03) and give Cursor a **pull-based** read surface for timeline/metadata (OVER-03) while keeping mutations on the normal WebSocket protocol path.

Purpose: Second adapter type proves cross-runtime collaboration; resources replace “ambient full history” with explicit fetches (OVER-03).

Output: `packages/persistence` oversight read helpers, `packages/adapter-cursor-mcp/` MCP server (stdio), tests with mocked MCP host and DB fixtures.
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cross-runtime-proof-human-oversight/05-CONTEXT.md
@.planning/phases/05-cross-runtime-proof-human-oversight/05-RESEARCH.md
@packages/client/src/session-client.ts
@packages/persistence/src/repositories/spaces.ts
@packages/persistence/src/repositories/transcript.ts
@packages/persistence/src/repositories/collaboration-metadata.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read-only oversight repository in persistence</name>
  <files>packages/persistence/src/repositories/oversight.ts,packages/persistence/src/repositories/oversight.test.ts,packages/persistence/src/index.ts</files>
  <read_first>
    - packages/persistence/src/repositories/spaces.ts
    - packages/persistence/src/repositories/transcript.ts
    - packages/persistence/src/repositories/collaboration-metadata.ts
    - packages/persistence/src/repositories/sessions.ts
    - packages/persistence/migrations/003_collaboration_orchestrator_metadata.sql
  </read_first>
  <behavior>
    - Given migrated in-memory DB with one space slug `alpha`, two sessions members, profile+status rows, `getOversightSpaceSummaryBySlug(db,"alpha")` returns object with `slug === "alpha"`, numeric `memberCount >= 2`, `ownerSessionId` key present (may be null before Phase 5-01 backfill)
    - `listOversightTranscriptTailBySlug(db, { slug: "alpha", limit: 3 })` returns at most 3 entries ordered oldest-first within window, each including numeric `createdAtMs` from `transcript_entries.created_at`
    - `listOversightBlockedSessionsBySlug(db, "alpha")` returns only sessions where `collaboration_status.progress = 'blocked'`
  </behavior>
  <action>
    Add `packages/persistence/src/repositories/oversight.ts` exporting:

    - `getOversightSpaceSummaryBySlug(db, slug: string)` → `{ spaceId, slug, ownerSessionId: string | null, orchestratorSessionId: string | null, memberCount: number, members: Array<{ sessionId, displayName, isHuman, role, focus, progress, blockedReason }> }` built from SQL joining `spaces`, `space_memberships` (active), `sessions`, left join `collaboration_profile`, left join `collaboration_status`. Use actual column names from migrations (`display_name` etc. — verify in `sessions` schema).

    - `listOversightTranscriptTailBySlug(db, { slug, limit })` → resolve `spaceId` via `getSpaceBySlug`; SQL `SELECT relay_seq, envelope_json, created_at FROM transcript_entries WHERE space_id = ? ORDER BY relay_seq DESC LIMIT ?` then reverse array to oldest-first; map rows to `{ relaySeq, envelopeJson, createdAtMs: created_at }`.

    - `listOversightBlockedSessionsBySlug(db, slug)` → sessions in space where `collaboration_status.progress = 'blocked'` with `blocked_reason` field.

    Export all three from `packages/persistence/src/index.ts`.

    Add `packages/persistence/src/repositories/oversight.test.ts` proving `behavior` with fixture inserts (reuse patterns from `spaces.test.ts`).
  </action>
  <verify>
    <automated>npm run test -w @agent-talkie/persistence</automated>
  </verify>
  <acceptance_criteria>
    - `packages/persistence/src/repositories/oversight.ts` exists and contains `export function getOversightSpaceSummaryBySlug`
    - Same file contains `export function listOversightTranscriptTailBySlug`
    - Same file contains `export function listOversightBlockedSessionsBySlug`
    - `packages/persistence/src/index.ts` exports all three names from `./repositories/oversight.js`
    - `npm run test -w @agent-talkie/persistence` exits with code 0
  </acceptance_criteria>
  <done>Oversight read helpers implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP server package adapter-cursor-mcp</name>
  <files>package.json,packages/adapter-cursor-mcp/package.json,packages/adapter-cursor-mcp/tsconfig.json,packages/adapter-cursor-mcp/tsup.config.ts,packages/adapter-cursor-mcp/src/index.ts,packages/adapter-cursor-mcp/src/mcp-server.ts,packages/adapter-cursor-mcp/src/mcp-server.test.ts</files>
  <read_first>
    - packages/client/src/session-client.ts
    - packages/persistence/src/repositories/oversight.ts
    - packages/supervisor/src/paths.ts
    - packages/relay/src/daemon.ts
    - docs/adapter-ingress.md
  </read_first>
  <behavior>
    - Register tool **exactly** named `join_space` accepting JSON `{ "slug": string }` (required) — handler resolves relay via `ensureRelayRunning`, uses shared `TalkieSessionClient` instance, ensures `connect`+`registerSession`+`joinSpace` once per server process (store `slug`→`spaceId` map); returns JSON text including `spaceId` substring.
    - Register tools `send_message`, `assign_orchestrator`, `update_metadata` with Zod input schemas (zod 4) validated at handler entry; invalid input returns MCP error payload (non-zero) with substring `validation_error`.
    - Resource URI template `talkie://space/{slug}/timeline` returns JSON with at most **50** transcript lines by default (constant `DEFAULT_TIMELINE_LIMIT = 50` in source).
    - No `console.log` in `packages/adapter-cursor-mcp/src` (grep); diagnostics use `console.error` only.
  </behavior>
  <action>
    **Monorepo**

    - Create `packages/adapter-cursor-mcp/` with `package.json`: name `@agent-talkie/adapter-cursor-mcp`, dependency `"@modelcontextprotocol/sdk": "^1.29.0"`, workspace deps `"@agent-talkie/client": "0.0.0"`, `"@agent-talkie/protocol": "0.0.0"`, `"@agent-talkie/persistence": "0.0.0"`, `"@agent-talkie/supervisor": "0.0.0"`, `"zod": "4.3.6"`, bin `"talkie-cursor-mcp": "./dist/mcp-server.js"`, `tsup` build entry `src/mcp-server.ts` with shebang banner.

    - Root `package.json` workspaces append `"packages/adapter-cursor-mcp"`; extend `scripts.build` with `npm run build -w @agent-talkie/adapter-cursor-mcp` after adapter-codex; extend `scripts.test` similarly.

    **Server (`mcp-server.ts`)**

    - On startup: `resolveAgentTalkieDataDir()` + `join(dataDir, "relay.sqlite")` string constant must match `packages/relay/src/daemon.ts` filename (`relay.sqlite`).

    - Maintain singleton `TalkieSessionClient` configured with relay URL from `ensureRelayRunning({})`; `registerSession` fields: `displayName: process.env.TALKIE_MCP_DISPLAY_NAME ?? "cursor-mcp-adapter"`, `runtime: process.env.TALKIE_MCP_RUNTIME ?? "adapter-cursor-mcp"`, `workspaceLabel: process.env.TALKIE_MCP_WORKSPACE ?? "."`, **`isHuman: process.env.TALKIE_MCP_IS_HUMAN === "0" ? false : true`** (default **human** so `assign_orchestrator` / profile `metadata.patch` match relay rules in `collaboration-handlers.ts`).

    - **Tools** (names exact for D-03):

      1. `join_space` — input `{ slug: z.string().min(1).max(64) }`; call `client.joinSpace({ slug, idempotencyKey: randomUUID() })`; cache `spaceId`.

      2. `send_message` — input `{ spaceId: z.string().uuid(), text: z.string().min(1).max(8000), toSessionId: z.string().uuid().optional() }`; send envelope `{ version:1, id:randomUUID(), sessionId:<registered>, kind:"conversation", type:"chat.message", spaceId, payload:{text}, ...(toSessionId?{to:toSessionId}:{}) }`.

      3. `assign_orchestrator` — input `{ spaceId: z.string().uuid(), orchestratorSessionId: z.string().uuid() }`; send control `{ kind:"control", type:"orchestrator.designate", spaceId, sessionId:<registered>, idempotencyKey:randomUUID(), payload:{ orchestratorSessionId } }`.

      4. `update_metadata` — input discriminated union matching `metadataPatchPayloadSchema` fields minimally: `{ spaceId, namespace: z.enum(["profile","status"]), targetSessionId?: uuid, patch: z.record(z.unknown()) }` then `metadataPatchPayloadSchema.safeParse({namespace,...})` — **must** reuse `metadataPatchPayloadSchema` from `@agent-talkie/protocol` for final validation; send `metadata.patch` envelope with same payload.

    - **Resources** (static template registration using SDK `ResourceTemplate` if available; else document `listResources` + `readResource` URI parsing):

      - `talkie://space/{slug}/participants` → JSON from `getOversightSpaceSummaryBySlug`

      - `talkie://space/{slug}/timeline` → JSON `{ entries: listOversightTranscriptTailBySlug(db,{slug, limit:50}) }`

      - `talkie://space/{slug}/metadata` → JSON snapshot: reuse `getCollaborationMetadataSnapshot` from persistence if exported, else equivalent query

      - `talkie://space/{slug}/blocked` → JSON `{ blocked: listOversightBlockedSessionsBySlug(db, slug) }`

    - Resource handlers open DB via `openDatabase(join(resolveAgentTalkieDataDir(), "relay.sqlite"))` — **no `migrate` call** in MCP process (assume relay created DB). Wrap reads in try/catch; on missing file return MCP resource text explaining path.

    - **OVER-02**: Tool `update_metadata` description string (MCP tool `description` field) must include exact substring `blockedReason` and `native` to document self-report expectation.

    - **Tests** `mcp-server.test.ts`: mock `@modelcontextprotocol/sdk` server registration capture registered tool names; assert array includes exact strings `join_space`, `send_message`, `assign_orchestrator`, `update_metadata`. Mock DB path with `:memory:` injection if refactor exports `createMcpServer(deps)` — **preferred** export `createMcpServer(deps: { dbPath?: string; client?: TalkieSessionClient })` for tests.

    `src/index.ts` re-exports `runMcpServer()` default stdio transport per SDK docs.
  </action>
  <verify>
    <automated>npm run test -w @agent-talkie/adapter-cursor-mcp</automated>
  </verify>
  <acceptance_criteria>
    - `packages/adapter-cursor-mcp/package.json` contains `"@modelcontextprotocol/sdk": "^1.29.0"`
    - `packages/adapter-cursor-mcp/src/mcp-server.ts` contains substring `TALKIE_MCP_IS_HUMAN`
    - `packages/adapter-cursor-mcp/src/mcp-server.ts` contains the exact tool name string `join_space` and `assign_orchestrator`
    - `packages/adapter-cursor-mcp/src/mcp-server.ts` contains URI substring `talkie://space/`
    - `packages/adapter-cursor-mcp/src/mcp-server.ts` contains `DEFAULT_TIMELINE_LIMIT` with value `50`
    - Grep `packages/adapter-cursor-mcp/src` for `console.log` returns no matches (empty rg output)
    - `npm run build -w @agent-talkie/adapter-cursor-mcp` exits 0
    - `npm run test -w @agent-talkie/adapter-cursor-mcp` exits 0
  </acceptance_criteria>
  <done>MCP adapter builds; tools/resources implemented; tests green; no stdout logging in src.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host → MCP server stdin | Tool inputs are untrusted; must validate with Zod before relay envelopes |
| MCP server → SQLite file | Read-only SELECT paths; no migrations from MCP |
| MCP server → relay WS | Same authorization as any client (`not_space_owner` on designate for non-owners) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | MCP tool `assign_orchestrator` | mitigate | Relay enforces `not_space_owner`; Zod validates UUIDs |
| T-05-08 | Information disclosure | timeline resource | mitigate | Cap 50 entries default; explicit URI fetch (OVER-03) |
| T-05-09 | Spoofing | forged MCP payloads | mitigate | `safeParse`/Zod + protocol envelopes only |
| T-05-10 | Denial of service | large `send_message` text | mitigate | `max(8000)` on zod schema |
</threat_model>

<verification>
- `npm run test -w @agent-talkie/persistence` and `npm run test -w @agent-talkie/adapter-cursor-mcp` exit 0
- Manual: Cursor config points stdio to `talkie-cursor-mcp`; list tools shows four exact names; resource read returns JSON for a known slug
</verification>

<success_criteria>
Oversight persistence module exists; MCP package in workspaces; ADAPT-02 MCP adapter + OVER-03 pull model implemented; SDK pinned ^1.29.0.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-runtime-proof-human-oversight/05-03-SUMMARY.md`
</output>
