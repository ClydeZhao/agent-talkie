---
phase: 05-cross-runtime-proof-human-oversight
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - package.json
  - packages/adapter-codex/package.json
  - packages/adapter-codex/tsconfig.json
  - packages/adapter-codex/tsup.config.ts
  - packages/adapter-codex/src/cli.ts
  - packages/adapter-codex/src/codex-bridge.ts
  - packages/adapter-codex/src/index.ts
  - packages/adapter-codex/src/codex-bridge.test.ts
  - packages/client/src/session-client.ts
  - packages/client/src/session-client.join.test.ts
autonomous: true
requirements:
  - ADAPT-02
  - OVER-02
user_setup:
  - service: codex-cli
    why: "Real cross-runtime proof and blocked-event mapping (optional in CI)"
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - "New package @agent-talkie/adapter-codex builds and is listed in root workspaces"
    - "Process bridges Codex child stdin/stdout using Content-Length framing utilities from @agent-talkie/adapter-stdio"
    - "Child stderr lines matching native-interruption heuristics trigger metadata.patch progress blocked with blockedReason (OVER-02 self-report)"
  artifacts:
    - path: "packages/adapter-codex/package.json"
      provides: "workspace package + bin talkie-codex-adapter"
    - path: "packages/adapter-codex/src/codex-bridge.ts"
      provides: "spawn + frame loop + TalkieSessionClient wiring"
  key_links:
    - from: "packages/adapter-codex/src/codex-bridge.ts"
      to: "TalkieSessionClient.sendEnvelope"
      via: "parsed envelopes from ContentLengthFrameReader"
    - from: "packages/adapter-codex/src/codex-bridge.ts"
      to: "metadata.patch"
      via: "namespace status progress blocked"
---

<objective>
Ship the **stdio-side** proof adapter for ADAPT-02: wrap **Codex CLI** as a subprocess, bridge Content-Length framed protocol traffic to the relay via `TalkieSessionClient`, and self-report native interruptions to collaboration metadata (OVER-02 D-07).

Purpose: One of the two structurally distinct adapters locked in CONTEXT D-01/D-02; reuses `@agent-talkie/adapter-stdio` framing (D-02).

Output: `packages/adapter-codex/` with CLI binary, unit tests using a mocked child process (no Codex required in CI).
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cross-runtime-proof-human-oversight/05-CONTEXT.md
@docs/adapter-ingress.md
@packages/adapter-stdio/src/content-length-framing.ts
@packages/adapter-stdio/src/cli.ts
@packages/client/src/session-client.ts
@packages/protocol/src/collaboration-wire.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monorepo package adapter-codex (build + bin)</name>
  <files>package.json,packages/adapter-codex/package.json,packages/adapter-codex/tsconfig.json,packages/adapter-codex/tsup.config.ts,packages/adapter-codex/src/index.ts,packages/adapter-codex/src/cli.ts</files>
  <read_first>
    - packages/adapter-stdio/package.json
    - packages/adapter-stdio/tsup.config.ts
    - packages/adapter-stdio/tsconfig.json
    - package.json
  </read_first>
  <action>
    Add workspace package `packages/adapter-codex/`:

    - `package.json`: `"name": "@agent-talkie/adapter-codex"`, `"version": "0.0.0"`, `"type": "module"`, bin `"talkie-codex-adapter": "./dist/cli.js"`, dependencies: `"@agent-talkie/client": "0.0.0"`, `"@agent-talkie/protocol": "0.0.0"`, `"@agent-talkie/supervisor": "0.0.0"`, `"@agent-talkie/adapter-stdio": "0.0.0"`, `"zod": "4.3.6"` (match other packages’ zod range), devDependencies aligned with `packages/adapter-stdio` (`tsup`, `typescript`, `vitest`, `@types/node`).

    - `tsup.config.ts`: mirror `packages/adapter-stdio/tsup.config.ts` pattern — library entry `src/index.ts` with `dts: true`, CLI entry `src/cli.ts` with shebang banner `#!/usr/bin/env node`.

    - `tsconfig.json`: extend repo pattern from `packages/adapter-stdio/tsconfig.json` if present, or minimal `compilerOptions` matching sibling packages.

    - `src/index.ts`: export `runCodexAdapter(opts?: { spawn?: typeof import('node:child_process').spawn })` from `./codex-bridge.js` (implemented in Task 2) — or re-export bridge after Task 2 creates file.

    - `src/cli.ts`: if main module, call `runCodexAdapter()` and `.catch` set `process.exitCode = 1`.

    **Root `package.json`**: insert `"packages/adapter-codex"` into the `workspaces` array (preserve existing order: append after `packages/adapter-stdio`). Extend root `scripts.build` to include `npm run build -w @agent-talkie/adapter-codex` **after** `@agent-talkie/adapter-stdio`. Extend root `scripts.test` similarly **after** adapter-stdio test.

    **Pretest for adapter-codex** (in its `package.json` scripts): `pretest` must run builds for `@agent-talkie/protocol`, `@agent-talkie/persistence`, `@agent-talkie/relay`, `@agent-talkie/supervisor`, `@agent-talkie/client`, `@agent-talkie/adapter-stdio` in that dependency order (copy dependency list pattern from `packages/adapter-stdio/package.json` pretest).
  </action>
  <verify>
    <automated>npm run build -w @agent-talkie/adapter-codex</automated>
  </verify>
  <acceptance_criteria>
    - Root `package.json` contains the string `"packages/adapter-codex"` inside the workspaces array
    - `packages/adapter-codex/package.json` contains `"name": "@agent-talkie/adapter-codex"`
    - `packages/adapter-codex/package.json` contains `"talkie-codex-adapter": "./dist/cli.js"` under `bin`
    - Root `package.json` `scripts.build` includes `npm run build -w @agent-talkie/adapter-codex`
    - Command `npm run build -w @agent-talkie/adapter-codex` exits with code 0
  </acceptance_criteria>
  <done>adapter-codex package scaffolds, root workspaces/scripts updated, package builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Codex bridge, stderr blocked heuristic, tests</name>
  <files>packages/client/src/session-client.ts,packages/client/src/session-client.join.test.ts,packages/adapter-codex/src/codex-bridge.ts,packages/adapter-codex/src/index.ts,packages/adapter-codex/src/codex-bridge.test.ts</files>
  <read_first>
    - packages/adapter-codex/src/cli.ts
    - packages/adapter-stdio/src/content-length-framing.js
    - packages/adapter-stdio/src/cli.ts
    - packages/client/src/session-client.ts
    - packages/protocol/src/collaboration-wire.ts
    - packages/relay/src/integration.test.ts
  </read_first>
  <behavior>
    - When mock child stderr emits line `needs your approval to run`, bridge calls `sendEnvelope` exactly once with `kind: "control"`, `type: "metadata.patch"`, `payload.namespace` === `"status"`, `payload.patch.progress` === `"blocked"`, non-empty `payload.patch.blockedReason`
    - Content-Length framed JSON object valid as protocol envelope is forwarded via `sendEnvelope` unchanged in shape (same `kind`, `type`, `sessionId` as parsed)
    - After blocked report, same stderr line within 5000ms does not send a second metadata.patch (cooldown)
  </behavior>
  <action>
    **A) `TalkieSessionClient.joinSpace` (client package)**

    In `packages/client/src/session-client.ts` add public method `async joinSpace(args: { slug: string; idempotencyKey: string }): Promise<{ spaceId: string; slug: string }>`:

    - Preconditions: WebSocket open, `registerSession` already completed (throw `Error("joinSpace before registerSession")` if internal state indicates register still pending — detect via absence of stored `sessionId` by adding private field `registeredSessionId: string | null` set in `registerSession` resolve path).

    - Send JSON object exactly matching relay integration shape: `{ version: 1, id: <randomUUID()>, sessionId: this.registeredSessionId, kind: "control", type: "space.join", payload: { slug: args.slug }, idempotencyKey: args.idempotencyKey }` (no `spaceId` property on envelope).

    - Until resolved, on each `message` event: `JSON.parse`; if object has `type === "space.joined"` and string `spaceId`, resolve `{ spaceId, slug: args.slug }`; if `type === "protocol.error"`, reject with `Error(JSON.stringify(obj))`.

    - Add `packages/client/src/session-client.join.test.ts` using **mock WebSocket** or in-memory server **not required** — use fake ws that manually invokes message callbacks (same pattern as existing client tests if any); assert join sends expected JSON substring `"type":"space.join"`.

    **B) `codex-bridge.ts`**

    Implement `packages/adapter-codex/src/codex-bridge.ts` exporting `runCodexAdapter(opts?: { spawn?: typeof spawn; ensureRelay?: typeof ensureRelayRunning })`:

    1. **Relay**: `const relay = await (opts?.ensureRelay ?? ensureRelayRunning)({});` then `new TalkieSessionClient({ url: \`ws://127.0.0.1:${relay.port}\` })`, `connect()`, `registerSession({ displayName: process.env.TALKIE_CODEX_DISPLAY_NAME ?? "codex-adapter", runtime: process.env.TALKIE_CODEX_RUNTIME ?? "adapter-codex", workspaceLabel: process.env.TALKIE_CODEX_WORKSPACE ?? ".", isHuman: false })`.

    1b. **Space**: Let `activeSpaceId: string | null = null`. If `process.env.TALKIE_CODEX_JOIN_SLUG` is non-empty trimmed string, set `activeSpaceId = (await client.joinSpace({ slug: process.env.TALKIE_CODEX_JOIN_SLUG.trim(), idempotencyKey: randomUUID() })).spaceId`. Else if `process.env.TALKIE_CODEX_SPACE_ID` is non-empty UUID-shaped string, set `activeSpaceId` from env. Else leave `null`.

    2. **Spawn** (use injected `spawn` or `child_process.spawn`): executable `process.env.TALKIE_CODEX_COMMAND ?? "codex"`, argv `[]` (empty array) unless `process.env.TALKIE_CODEX_ARGS_JSON` is set — then `JSON.parse(process.env.TALKIE_CODEX_ARGS_JSON)` must yield `string[]` or throw at startup with `process.stderr.write` message prefix `[adapter-codex]`; stdio: `["pipe","pipe","pipe"]`.

    3. **Upstream (child stdout → relay)**: `ContentLengthFrameReader` reading from child stdout async iterator; each parsed object: `safeParseEnvelope` from `@agent-talkie/protocol`; on success `client.sendEnvelope(envelope)`.

    4. **Downstream (stdin ← not in Phase 4 stdio adapter style for codex)** — for Phase 5 minimal bridge, only stderr + stdout as above; **do not** write child stdin unless an envelope arrives on `client.onEnvelope` that is explicitly documented in code comment as future work (v1: one-way agent egress from Codex frames only if product needs; if stdio adapter parity required, forward conversation envelopes to child stdin as JSON lines — **choose minimal**: no stdin write in v1; document in `packages/adapter-codex/README.md` one paragraph).

    5. **OVER-02 stderr heuristic**: buffer `child.stderr` by lines (split on `\n`). For each non-empty line, test regex **exactly** `/\\b(permission|approval|confirm)\\b/i` (JavaScript literal). On match, if `Date.now() - lastBlockedSentMs >= 5000`, send envelope:

       ```ts
       {
         version: 1,
         id: randomUUID(),
         sessionId: registeredSessionId,
         kind: "control",
         type: "metadata.patch",
         spaceId: activeSpaceId, // if null, skip blocked sends and stderr-write JSON `{"level":"warn","event":"adapter_codex_no_space"}` once per process
         idempotencyKey: randomUUID(),
         payload: {
           namespace: "status",
           patch: {
             progress: "blocked",
             blockedReason: line.slice(0, 512),
           },
         },
       }
       ```

       Use `randomUUID` from `node:crypto`. If `activeSpaceId` is `null`, never call `sendEnvelope` for blocked heuristic (only warn once per process using a boolean flag `noSpaceWarned`).

    6. **Tests** `packages/adapter-codex/src/codex-bridge.test.ts`: use **injectable** `spawn` returning mock streams (`PassThrough`); inject mock `ensureRelay`; mock `TalkieSessionClient` class with `connect`, `registerSession`, `joinSpace` resolving `{ spaceId: "00000000-0000-7000-8000-000000000001" }`, and `sendEnvelope` spy. Set `process.env.TALKIE_CODEX_JOIN_SLUG="demo"` in test setup. Minimum: satisfy `behavior` bullets with vitest.

    Update `src/index.ts` to export `runCodexAdapter` signature including optional injection for tests.
  </action>
  <verify>
    <automated>npm run test -w @agent-talkie/client && npm run test -w @agent-talkie/adapter-codex</automated>
  </verify>
  <acceptance_criteria>
    - `packages/client/src/session-client.ts` contains `joinSpace(` as a method name on class `TalkieSessionClient`
    - `packages/adapter-codex/src/codex-bridge.ts` contains the exact regex source `\b(permission|approval|confirm)\b`
    - `packages/adapter-codex/src/codex-bridge.ts` contains substring `TALKIE_CODEX_JOIN_SLUG`
    - `packages/adapter-codex/src/codex-bridge.ts` contains `type: "metadata.patch"` and `progress: "blocked"`
    - `packages/adapter-codex/src/codex-bridge.test.ts` exists
    - `npm run test -w @agent-talkie/client` exits with code 0
    - `npm run test -w @agent-talkie/adapter-codex` exits with code 0
  </acceptance_criteria>
  <done>Codex bridge implemented; stderr → blocked metadata; unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codex child process → adapter | Untrusted stdout/stderr parsed as frames/text |
| Adapter → relay WebSocket | Envelopes must be protocol-valid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | forged Content-Length frames from child | mitigate | `safeParseEnvelope` before `sendEnvelope`; drop invalid |
| T-05-05 | Tampering | malicious stderr triggers false blocked | accept | Heuristic is advisory; human verifies native UI (D-08); cooldown limits noise |
| T-05-06 | Elevation | spawn arbitrary command | mitigate | Executable from `TALKIE_CODEX_COMMAND` only (user env); default `codex`; document trust boundary in README |
</threat_model>

<verification>
- `npm run build -w @agent-talkie/adapter-codex` and `npm run test -w @agent-talkie/adapter-codex` exit 0
- Optional manual: run `talkie-codex-adapter` with `TALKIE_CODEX_SPACE_ID` set and Codex installed — stderr containing “approval” yields blocked metadata in relay/CLI
</verification>

<success_criteria>
adapter-codex in workspaces; framing bridge; OVER-02 self-report path; mocked tests pass without Codex binary.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-runtime-proof-human-oversight/05-02-SUMMARY.md`
</output>
