---
phase: 03-supervisor-daemon-lifecycle
plan: "03"
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - packages/cli/package.json
  - packages/cli/tsconfig.json
  - packages/cli/tsup.config.ts
  - packages/cli/src/cli.ts
  - packages/cli/src/cli.test.ts
  - package.json
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03]
must_haves:
  truths:
    - "Installing the monorepo and running the published-style binary exposes relay management and a ping that auto-starts the relay."
    - "Subcommands exist for relay start/stop/status and a session stub that fails closed with a clear message."
    - "`talkie ping` (or equivalent documented command) calls the same ensure path as automation without requiring a manual long-lived relay process."
  artifacts:
    - path: packages/cli/package.json
      provides: npm `bin` map and commander dependency
    - path: packages/cli/src/cli.ts
      provides: Commander program and subcommand wiring
  key_links:
    - from: packages/cli/src/cli.ts
      to: packages/supervisor/src/ensure-relay.ts
      via: import ensureRelayRunning, stopRelay, getRelayStatus
      pattern: "ensureRelayRunning"
---

<objective>
Add `@agent-talkie/cli`: **`commander@14.0.3`** CLI with **`bin` name `talkie`**, subcommands **`relay start|stop|status`**, **`relay ensure`** (idempotent ensure), **`ping`** (auto-start + health check for CLI-03), and a **session stub** for CLI-02. Wire root **build/test** scripts.

Purpose: Satisfy CLI-01 (npm/npx installable package shape in monorepo), CLI-02 (relay + session entrypoints), CLI-03 (transparent auto-start via `ping` / `relay ensure` using supervisor).
Output: `packages/cli/dist/cli.js` as executable entry.
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-supervisor-daemon-lifecycle/03-CONTEXT.md
@.planning/phases/03-supervisor-daemon-lifecycle/03-RESEARCH.md

**Package:** `"name": "@agent-talkie/cli"`, `"version": "0.0.0"`, `"type": "module"`.

**Binary:** In `packages/cli/package.json` set:
```json
"bin": {
  "talkie": "./dist/cli.js"
}
```
Add `"bin"` field at top level (same file). Prepend to built `cli.js` shebang: first line exactly `#!/usr/bin/env node` — configure tsup `banner: { js: '#!/usr/bin/env node' }` or equivalent so `npm link` / `npx` works.

**Dependencies:** `"@agent-talkie/supervisor": "0.0.0"`, `"commander": "14.0.3"`.

**Command tree (implement exactly):**
- `talkie --help` shows program name `talkie` and top-level subcommands `relay`, `session`, `ping`.
- `talkie relay start` — calls `ensureRelayRunning({})`; prints to stdout exactly one line: `relay port=<port> spawned=<true|false>` using the boolean string `true` or `false`.
- `talkie relay ensure` — same implementation as `relay start` (duplicate handler allowed).
- `talkie relay stop` — calls `stopRelay({})`; prints JSON.stringify result **without** extra whitespace (default `JSON.stringify` one line).
- `talkie relay status` — calls `getRelayStatus({})`; prints JSON.stringify result one line.
- `talkie ping` — calls `ensureRelayRunning({})` then `fetch(\`http://127.0.0.1:${port}/__agent-talkie/v1/health?generation=${encodeURIComponent(generation)}\`)`; if not ok, `process.exitCode = 1`; stdout line exactly `ping ok port=<port>` on success.
- `talkie session list` — prints to stdout exactly `not implemented (Phase 4)` and exits `0`.

**No postinstall scripts** in `packages/cli/package.json` (D-04).

**Root `package.json`:** append `&& npm run build -w @agent-talkie/cli` to `build` after supervisor; append `&& npm run test -w @agent-talkie/cli` to `test` after supervisor tests.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold @agent-talkie/cli + tsup bin</name>
  <files>packages/cli/package.json, packages/cli/tsconfig.json, packages/cli/tsup.config.ts, packages/cli/src/cli.ts, package.json</files>
  <read_first>
    - packages/supervisor/package.json
    - package.json
    - .planning/phases/03-supervisor-daemon-lifecycle/03-RESEARCH.md (commander version)
  </read_first>
  <action>
    1. Create `packages/cli/package.json` with `name`, `version`, `type`, `bin` as in `<context>`, dependencies `@agent-talkie/supervisor` and `commander` exact `14.0.3`, devDeps matching supervisor (`tsup`, `typescript`, `vitest`, `@types/node`).

    2. `tsup.config.ts`: entry `src/cli.ts`, `format: esm`, `dts: false` (CLI only), `clean: true`, set `banner: { js: "#!/usr/bin/env node\n" }` for shebang.

    3. `packages/cli/src/cli.ts`: minimal program so `--help` works before Task 2 — `import { Command } from "commander";` → `const program = new Command();` → `program.name("talkie");` → `program.description("agent-talkie CLI");` → end with `await program.parseAsync(process.argv);` inside async IIFE or top-level await. **No** `relay`/`ping`/`session` subcommands until Task 2.

    4. Add `scripts`: `"build": "tsup"`, `"pretest": "npm run build -w @agent-talkie/protocol && npm run build -w @agent-talkie/persistence && npm run build -w @agent-talkie/relay && npm run build -w @agent-talkie/supervisor && npm run build -w @agent-talkie/cli"`, `"test": "vitest run"`.

    5. Update root `package.json` `scripts.build` and `scripts.test` to include `@agent-talkie/cli` as specified in `<context>`.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run build -w @agent-talkie/cli</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"talkie": "./dist/cli.js"' packages/cli/package.json` exits 0.
    - `grep -q '"commander": "14.0.3"' packages/cli/package.json` exits 0.
    - `head -n 1 packages/cli/dist/cli.js` outputs exactly `#!/usr/bin/env node`
    - `grep -q '@agent-talkie/cli' package.json` exits 0.
    - `npm run build -w @agent-talkie/cli` exits 0.
  </acceptance_criteria>
  <done>CLI package builds a shebang’d `dist/cli.js` and is listed in root scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Commander wiring — relay, ping, session</name>
  <files>packages/cli/src/cli.ts</files>
  <read_first>
    - packages/cli/src/cli.ts
    - packages/supervisor/src/index.ts
    - .planning/phases/03-supervisor-daemon-lifecycle/03-CONTEXT.md (D-01)
  </read_first>
  <action>
    In `packages/cli/src/cli.ts`:

    1. Import `ensureRelayRunning`, `stopRelay`, `getRelayStatus` from `@agent-talkie/supervisor`.

    2. Implement the **Command tree** from `<context>` with Commander subcommands:
       - Use `program.command("relay")` nested commands `start`, `ensure`, `stop`, `status` OR flat `relay-start` avoided — prefer `relay` as command with subcommands via `relayCmd = program.command("relay")` then `relayCmd.command("start")`, etc.

    3. At end of file `await program.parseAsync(process.argv);` (use async main if needed).

    4. For `ping`, use global `fetch` (Node 20+).

    5. On unexpected errors in any command, `console.error(err)` and `process.exitCode = 1`.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run build -w @agent-talkie/cli && node packages/cli/dist/cli.js --help</automated>
  </verify>
  <acceptance_criteria>
    - `node packages/cli/dist/cli.js --help` stdout contains substring `relay`
    - `node packages/cli/dist/cli.js --help` stdout contains substring `ping`
    - `node packages/cli/dist/cli.js --help` stdout contains substring `session`
    - `grep -q 'ensureRelayRunning' packages/cli/src/cli.ts` exits 0.
    - `grep -q 'talkie' packages/cli/src/cli.ts` exits 0.
  </acceptance_criteria>
  <done>All specified subcommands are registered and call supervisor APIs.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest for CLI output contracts</name>
  <files>packages/cli/src/cli.test.ts, packages/cli/package.json</files>
  <read_first>
    - packages/cli/src/cli.ts
    - packages/supervisor/src/ensure-relay.test.ts
  </read_first>
  <action>
    1. Add vitest to `packages/cli` if not already in devDeps (from Task 1).

    2. Create `packages/cli/src/cli.test.ts` that spawns `node` running `dist/cli.js` **after** build path resolved via `fileURLToPath` relative to test file:
       - Test A: `session list` — assert stdout trimmed equals `not implemented (Phase 4)` and exit code `0`.
       - Test B: with `env` `AGENT_TALKIE_DATA_DIR` set to `fs.mkdtempSync` path, run `relay start` — stdout matches regex `^relay port=\\d+ spawned=(true|false)\\s*$` (trimmed); then run `ping` — exit code `0` and stdout matches `^ping ok port=\\d+\\s*$`.

    3. Use `child_process.spawnSync` or `execa`-equivalent from `node:child_process` with `encoding: utf8`.

    4. In `afterEach`, call supervisor `stopRelay({ dataDir })` for the temp dir to avoid orphan processes.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/cli</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/cli/src/cli.test.ts` exits 0.
    - `grep -q 'not implemented (Phase 4)' packages/cli/src/cli.test.ts` exits 0.
    - `npm run test -w @agent-talkie/cli` exits 0.
  </acceptance_criteria>
  <done>CLI output strings are regression-locked by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → commander | Untrusted local invocation; must not shell-out user strings with `shell: true` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | I | CLI flags / env | mitigate | Use commander parsing only; no `eval` of user input |
| T-03-03-02 | E | `relay stop` | accept | Local operator capability by design |
| T-03-03-03 | T | `ping` fetch | mitigate | Fixed path prefix `/__agent-talkie/v1/health`; generation from ensureRelayRunning only |
</threat_model>

<verification>
- `npm run test` at repo root passes (all workspaces).
- Manual: `AGENT_TALKIE_DATA_DIR=$(mktemp -d) node packages/cli/dist/cli.js ping` prints `ping ok port=...` without pre-running relay.
</verification>

<success_criteria>
CLI-01: package has `bin` and builds; CLI-02: relay + session commands exist; CLI-03: `ping`/`relay ensure` auto-starts relay via supervisor.
</success_criteria>

<output>
After completion, create `.planning/phases/03-supervisor-daemon-lifecycle/03-03-SUMMARY.md`
</output>
