---
phase: 04-collaboration-semantics-metadata-adapter-edge
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - packages/relay/src/router.ts
  - packages/relay/src/server.ts
  - packages/relay/src/collaboration-handlers.ts
  - packages/relay/src/__tests__/router-orchestrator.test.ts
autonomous: true
requirements:
  - MSG-04
  - MSG-05
  - MSG-06
  - META-01
  - META-02
  - META-03
  - META-04
must_haves:
  truths:
    - "Human conversation without `to` routes only to spaces.orchestrator_session_id or returns protocol.error no_orchestrator"
    - "Non-human undirected conversation still fan-outs to other members (MSG-02 compatible)"
    - "Explicit envelope.to still delivers without rewrite (MSG-05)"
    - "orchestrator.designate / clear / metadata.patch / metadata.query / task.assign handled with Zod + ACL"
  artifacts:
    - path: packages/relay/src/router.ts
      provides: orchestrator default routing branch
      pattern: no_orchestrator|orchestratorSessionId
    - path: packages/relay/src/collaboration-handlers.ts
      provides: control handlers
      contains: orchestrator.designate
  key_links:
    - from: packages/relay/src/router.ts
      to: packages/persistence/src/repositories/collaboration-metadata.ts
      via: getOrchestratorSessionId
      pattern: getOrchestratorSessionId
---

<objective>
Implement relay-side orchestrator routing (D-02), orchestrator designation (D-01), task assignment control (D-03), collaboration metadata patches with namespace ACL (D-06), incremental fan-out + snapshot query (D-05), and conversation payload hooks for orchestrator follow-up (MSG-06) — all on the existing WebSocket + envelope path (ADAPT-03: no special transport).

Purpose: Deliver MSG-04–MSG-06 and META-01–META-04 behavior authoritatively in the relay.
Output: `collaboration-handlers.ts`, extended `dispatchValidatedEnvelope`, extended `routeEnvelope`, vitest coverage for routing and errors.
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collaboration-semantics-metadata-adapter-edge/04-CONTEXT.md
@.planning/phases/04-collaboration-semantics-metadata-adapter-edge/04-RESEARCH.md
@packages/relay/src/server.ts
@packages/relay/src/router.ts

<interfaces>
From Plan 01 (must exist before this plan): `getSessionById` includes `isHuman`; `getOrchestratorSessionId`, `setOrchestratorSessionId`, `getCollaborationMetadataSnapshot`, `upsertCollaborationProfile`, `upsertCollaborationStatus`; Zod exports from `@agent-talkie/protocol` for collaboration payloads.

Control message `type` string literals to implement exactly:
- `orchestrator.designate`
- `orchestrator.clear`
- `task.assign`
- `metadata.patch`
- `metadata.query`

Protocol errors to emit as `{ type: "protocol.error", error: "<code>" }` with codes exactly:
- `no_orchestrator` — human undirected conversation when `spaces.orchestrator_session_id` IS NULL
- `orchestrator_designate_forbidden` — sender `is_human` is false
- `orchestrator_target_invalid` — designated session not active member of space
- `task_assign_forbidden` — sender is not current orchestrator for space
- `metadata_patch_forbidden` — namespace ACL violation (D-06)
- `orchestrator_offline` — human undirected conversation when orchestrator id set but `getSocketForSession(orchestratorId)` not OPEN
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: routeEnvelope — human default + orchestrator offline</name>
  <files>packages/relay/src/router.ts</files>
  <read_first>
    - packages/relay/src/router.ts
    - packages/persistence/src/repositories/sessions.ts
    - packages/persistence/src/repositories/collaboration-metadata.ts
    - packages/protocol/src/envelope.ts
  </read_first>
  <action>
    Extend `routeEnvelope` before the existing `if (envelope.to)` block (after membership checks and transcript append):

    1. Load sender session with `getSessionById(db, envelope.sessionId)`; if undefined, send `protocol.error` `invalid_envelope` and return (defensive).

    2. For `envelope.kind === "conversation"` AND `envelope.to` is undefined AND `sender.isHuman === true`:
       - Read `orch = getOrchestratorSessionId(db, spaceId)`.
       - If `orch === null`, call existing `sendJson(senderWs, { type: "protocol.error", error: "no_orchestrator" }); return;` — do not fan-out (D-02).
       - If `getSocketForSession(orch)` is not OPEN, `sendJson(senderWs, { type: "protocol.error", error: "orchestrator_offline" }); return;` — do not fan-out.
       - Otherwise, synthesize routing as direct: set local variable `effectiveTo = orch` and execute the same delivery path as explicit `to` (one WebSocket `send` to orchestrator, membership check for `orch` in space via `hasActiveMembership`). Do not mutate the envelope string stored in transcript — transcript must still record original envelope JSON as today; only routing changes.

    3. For all other undirected conversation (non-human senders), keep existing fan-out loop unchanged (broadcast to other members).

    4. When `envelope.to` is present, keep existing behavior unchanged (MSG-05).

    5. Add `metadata.query` to `SKIP_TRANSCRIPT_TYPES` alongside `transcript.query` if metadata query is implemented as envelope routed through router; if handled only in server dispatch, skip router change for metadata.query here.

    Import `getSessionById` from `@agent-talkie/persistence` and `getOrchestratorSessionId` from persistence exports.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/relay</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "no_orchestrator" packages/relay/src/router.ts` returns at least one line.
    - `grep -n "orchestrator_offline" packages/relay/src/router.ts` returns at least one line.
    - `grep -n "getOrchestratorSessionId" packages/relay/src/router.ts` returns at least one line.
    - `grep -n "isHuman" packages/relay/src/router.ts` returns at least one line.
    - `npm run test -w @agent-talkie/relay` exits 0.
  </acceptance_criteria>
  <done>Human undirected conversation targets orchestrator or fails closed per D-02; non-human undirected still broadcasts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest — routing matrix for orchestrator defaults</name>
  <files>packages/relay/src/router.ts,packages/relay/src/__tests__/router-orchestrator.test.ts</files>
  <behavior>
    - Given space with two members (human H, agent A), orchestrator = A, when H sends conversation without `to`, only A's mock socket receives send.
    - Given same but no orchestrator row, sender receives JSON with error exactly `no_orchestrator`.
    - Given orchestrator id set but no open socket for orchestrator, sender receives `orchestrator_offline`.
    - Given A sends undirected conversation, B and H both receive (fan-out), not orchestrator-only logic.
  </behavior>
  <read_first>
    - packages/relay/src/router.ts
    - packages/relay/src/session-registry.ts
    - packages/relay test files glob for patterns
  </read_first>
  <action>
    Add `packages/relay/src/__tests__/router-orchestrator.test.ts` using `vitest`, in-memory SQLite via `openDatabase` + `migrate` from `@agent-talkie/persistence`, stub `WebSocket` objects with `readyState` and captured `send` calls. Seed: two sessions in `sessions` with `is_human` 1 and 0; one space; memberships; set orchestrator via direct SQL or `setOrchestratorSessionId` if available. Drive `routeEnvelope` with envelopes `version: 1` matching negotiated tests elsewhere.

    Document in test file that follow-up/consolidation for MSG-06 uses optional payload keys `threadId` (string) and `forHumanSummary` (string) on conversation messages — no relay rewrite; only documented for future clients.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/relay -- src/__tests__/router-orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/relay/src/__tests__/router-orchestrator.test.ts` exists.
    - `grep -n "no_orchestrator" packages/relay/src/__tests__/router-orchestrator.test.ts` returns at least one line.
    - `grep -n "orchestrator_offline" packages/relay/src/__tests__/router-orchestrator.test.ts` returns at least one line.
    - `npm run test -w @agent-talkie/relay -- src/__tests__/router-orchestrator.test.ts` exits 0.
  </acceptance_criteria>
  <done>Automated proof of MSG-04 default routing and non-human broadcast preserved.</done>
</task>

<task type="auto">
  <name>Task 3: collaboration-handlers + dispatch wiring</name>
  <files>packages/relay/src/collaboration-handlers.ts,packages/relay/src/server.ts</files>
  <read_first>
    - packages/relay/src/server.ts
    - packages/relay/src/space-lifecycle.ts
    - packages/protocol/src/collaboration-wire.ts
    - packages/persistence/src/repositories/collaboration-metadata.ts
    - packages/persistence/src/repositories/idempotency.ts
  </read_first>
  <action>
    Create `packages/relay/src/collaboration-handlers.ts` exporting `handleCollaborationControl(ctx: RelayDispatchContext, envelope: Envelope): boolean` returning `true` if consumed, `false` otherwise.

    Handle only `kind === "control"` and `type` in:
    - `orchestrator.designate`: require `idempotencyKey` UUID on envelope (same pattern as space.join). Parse payload with `orchestratorDesignatePayloadSchema`. Sender must be human: `getSessionById.isHuman === true` else `orchestrator_designate_forbidden`. Target `orchestratorSessionId` must pass `hasActiveMembership(db, spaceId, target)` else `orchestrator_target_invalid`. Call `setOrchestratorSessionId(db, spaceId, target, Date.now())`. Record idempotency via existing `tryRecordIdempotencyKey` pattern from space lifecycle. On success, `sendJson(ws, { type: "orchestrator.designated", spaceId, orchestratorSessionId: target })`. **Mandatory fan-out:** for every other active member session id `sid` in the space (same membership query as router fan-out, excluding sender), if `getSocketForSession(sid)` is OPEN, `sendJson` exactly `{ type: "collaboration.orchestrator", spaceId, orchestratorSessionId: target }` (same `spaceId` UUID string and `orchestratorSessionId` UUID string as in ack).

    - `orchestrator.clear`: require `idempotencyKey`. Human only. `setOrchestratorSessionId(db, spaceId, null, Date.now())`. Ack `sendJson(ws, { type: "orchestrator.cleared", spaceId })`. **Mandatory fan-out:** same membership loop as designate; to each other member with OPEN socket send exactly `{ type: "collaboration.orchestrator", spaceId, orchestratorSessionId: null }`.

    - `task.assign`: parse with `taskAssignPayloadSchema`. Require `envelope.to` UUID. Sender must equal `getOrchestratorSessionId(db, spaceId)` else `task_assign_forbidden`. Validate assignee membership. Then return `false` so `routeEnvelope` delivers payload (transcript + direct send) — OR call `routeEnvelope` recursively; simplest: return `false` and ensure envelope.type is allowed through validation. Do not strip `to`.

    - `metadata.patch`: parse `metadataPatchPayloadSchema`. Resolve `subjectSessionId = namespace === "profile" ? (payload.targetSessionId ?? envelope.sessionId) : envelope.sessionId`. If `namespace === "profile"`: sender must be human (`isHuman === true`) else `metadata_patch_forbidden`. If `payload.targetSessionId` is defined and not equal to `envelope.sessionId`, require sender human (same check) and `hasActiveMembership(db, spaceId, payload.targetSessionId)`. Call `upsertCollaborationProfile(db, { spaceId, sessionId: subjectSessionId, ... })`. If `namespace === "status"`: sender must equal `subjectSessionId` (agents may only patch own status) else `metadata_patch_forbidden`; call `upsertCollaborationStatus`. After DB commit, broadcast to all other active members: `{ type: "collaboration.metadata", spaceId, sessionId: subjectSessionId, namespace, patch: <applied subset>, updatedAt: nowMs }` via their sockets (META-04).

    - `metadata.query`: require active membership; parse payload `{}`; respond `sendJson(senderWs, { type: "metadata.query.result", spaceId, snapshot: getCollaborationMetadataSnapshot(...) })`. Skip transcript append: handle inside handler before routeEnvelope — add to `SKIP_TRANSCRIPT_TYPES` in router for `metadata.query`.

    In `server.ts` `dispatchValidatedEnvelope`, invoke `handleCollaborationControl` immediately after space leave and before `routeEnvelope`; if it returns true, return.

    Use `spaceId` from `envelope.spaceId` for all handlers; require it present else `invalid_envelope`.

    For successful `orchestrator.designate`, `orchestrator.clear`, and `metadata.patch`, call `appendTranscriptEntry` from `@agent-talkie/persistence` with the same arguments shape as `packages/relay/src/router.ts` uses (kind `control`, envelope JSON), so history matches other control traffic. Do not append for `metadata.query`.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/relay</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/relay/src/collaboration-handlers.ts` exists.
    - `grep -n "orchestrator.designate" packages/relay/src/collaboration-handlers.ts` returns at least one line.
    - `grep -n "metadata.query" packages/relay/src/collaboration-handlers.ts` returns at least one line.
    - `grep -n "handleCollaborationControl" packages/relay/src/server.ts` returns at least one line.
    - `grep -n "metadata.query" packages/relay/src/router.ts` matches SKIP_TRANSCRIPT_TYPES or documented skip path.
    - `grep -n "collaboration.orchestrator" packages/relay/src/collaboration-handlers.ts` returns at least one line.
    - `npm run test -w @agent-talkie/relay` exits 0.
  </acceptance_criteria>
  <done>MSG-06 task.assign ACL; META-01–04 persistence + broadcast + snapshot; D-01/D-06 enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → relay | Control payloads and metadata patches are untrusted input until Zod + ACL pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Spoofing | profile metadata | mitigate | Only `isHuman` sessions may apply `metadata.patch` with `namespace=profile`; optional `targetSessionId` only when sender is human and target is a member |
| T-04-02-02 | Elevation | orchestrator designation | mitigate | `orchestrator.designate` / `orchestrator.clear` require `isHuman` + idempotency |
| T-04-02-03 | Tampering | task.assign | mitigate | Only current orchestrator session may send `task.assign` for that space |
| T-04-02-04 | DoS | metadata snapshot | mitigate | Reuse membership checks; snapshot bounded to active members only (no unbounded scan) |
</threat_model>

<verification>
Full relay test suite passes; new router orchestrator tests pass.
</verification>

<success_criteria>
Orchestrator routing, designation, metadata lifecycle, and task.assign ACL are implemented with protocol errors per spec strings above.
</success_criteria>

<output>
After completion, create `.planning/phases/04-collaboration-semantics-metadata-adapter-edge/04-02-SUMMARY.md`
</output>
