---
phase: 12-discovery-topology-attention
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - packages/dashboard/src/shell/talkie-search-panel.ts
  - packages/dashboard/src/transcript/talkie-transcript.ts
  - packages/dashboard/src/roster/talkie-roster.ts
  - packages/dashboard/src/demo/main.ts
  - packages/dashboard/src/theme/theme.css
  - packages/dashboard/src/store/dashboard-store.ts
autonomous: true
requirements:
  - OVER-03
  - OVER-06
user_setup: []
must_haves:
  truths:
    - 主栏 transcript 区存在可切换的右栏搜索面板；开啟时与 transcript 分栏同屏（D-03/D-04）
    - 搜索侧可设 sender/kind/时间、展示 chip 且可移除、输入查询并点结果会调用 `scrollToDedupeKey`（D-05..D-08）
    - 名册顶部在无 blocked 时不渲染「关注」区；有 blocked 时仅顶区显示且主列表无重复 sessionId（D-10..D-13）
  artifacts:
    - path: packages/dashboard/src/shell/talkie-search-panel.ts
      provides: 自定义元素 `talkie-search-panel`
    - path: packages/dashboard/src/roster/talkie-roster.ts
      provides: Needs Attention 区块 + 主列表
  key_links:
    - from: talkie-search-panel 结果行 click
      to: talkie-transcript.scrollToDedupeKey
      via: main.ts 事件或 ref
---

<objective>
实现 12-CONTEXT 的**右栏搜索/筛选 UI**（D-03..D-08、OVER-03）与**名册内关注列**（D-10..D-13、OVER-06）：分栏佈局、可移除 filter chips、结果列表点选跳转至已实现的 `scrollToDedupeKey`；名册将 `progress === "blocked"` 的会话提取到 **Needs Attention** 段并从下方列表排除，无 blocked 时整段隐藏。

Purpose: 在 12-01 数据层之上完成操作者可用的发现与关注意 UI。

Output: `talkie-search-panel` 组件、demo 佈局与 transcript 表头切換、`TalkieRoster` 双段渲染；必要时对 `DashboardStore` 仅补充 **UI 状态**（如 `transcriptSearchPanelOpen`）与 `notify`。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.planning/phases/12-discovery-topology-attention/12-CONTEXT.md
</execution_context>

<context>
@packages/dashboard/src/demo/main.ts
@packages/dashboard/src/transcript/talkie-transcript.ts
@packages/dashboard/src/roster/talkie-roster.ts
@packages/dashboard/src/store/dashboard-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 talkie-search-panel 与 store 的 UI 开闭状态</name>
  <read_first>
    - `packages/dashboard/src/store/dashboard-store.ts`（12-01 后含筛选/search 字段）
    - `packages/dashboard/src/shell/talkie-space-picker.ts`（既有 Lit + 样式参考，仅风格）
    - `packages/dashboard/src/shell/talkie-send-bar.ts`（与 store 绑定的模式）
  </read_first>
  <files>packages/dashboard/src/shell/talkie-search-panel.ts, packages/dashboard/src/store/dashboard-store.ts</files>
  <action>
    1. 在 `DashboardStore` 增加 `transcriptSearchPanelOpen = false` 与 `setTranscriptSearchPanelOpen(open: boolean): void`（`notify()`）。
    2. 新建 `packages/dashboard/src/shell/talkie-search-panel.ts`，`customElement("talkie-search-panel")`：
    - `@property` `store: DashboardStore` 必填
    - UI **至少**包含：文本输入框（绑定 `transcriptSearchQuery`，`@input` 或 debounce 500ms 内更新 store，**必须**最终调用 `setTranscriptSearchQuery`）、sender `<select>`（`option value=""` 表「任何」+ 每个 roster `sessionId` / 显示 `displayName`）、kind `<select>` 或两 radio：`all|control|conversation`、时间：`<select>` 选项 `all|5m|30m`（custom 可 Phase 内用「暂不实现」則不暴露 UI，但若不做 custom UI，**不得**在 store 刪 12-01 的 `transcriptTimeFilter` 联合类型；保持仅测 preset）、**应用**按钮或 `change` 即写 `setTranscriptFilters`）
    - **Filter chips**（D-08）：在面板顶条展示当前 `transcriptFilterSenderSessionId` / `transcriptFilterKind` / `transcriptTimeFilter` 的活跃 chip；每 chip 带 `×` 点击后清回对应维度的“空”值并 `notify`
    - **结果列表**：`store.getVisibleTranscriptLines()` 在 **有非空** `transcriptSearchQuery` 时若与 12-01 重複，则改为：结果应展示 **命中的行** —— 与 12-01 一致地调用同一可见集；**面板内列表**在「有 query」时显示与主栏相同的可见行**或**仅 MiniSearch 命中行（与 12-01 的 `getVisibleTranscriptLines` 一致即可，禁止两套逻辑）；每行显示 `收到时间 H:M:S`、`sender`、kind/type、`payload` 摘要（可重用 `previewPayload` 或简短）
    - 行 **click** 派发 `CustomEvent<"talkie-jump-to-dedupe", { dedupeKey: string }>`（`bubbles: true, composed: true`），`detail: { dedupeKey: line.dedupeKey }`
    3. 样式：用 Lit `css` + CSS 变量 `--talkie-*`；禁止引入 Shoelace。
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm test -w @agent-talkie/dashboard 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `talkie-search-panel.ts` 存在且含 `customElement("talkie-search-panel")`
    - `talkie-search-panel.ts` 含子串 `talkie-jump-to-dedupe` 或 `talkie-jump` 与 `dedupeKey`（`rg 'talkie-jump|dedupeKey' packages/dashboard/src/shell/talkie-search-panel.ts` 有匹配）
    - `dashboard-store.ts` 含 `transcriptSearchPanelOpen` 与 `setTranscriptSearchPanelOpen`
    - `npm test -w @agent-talkie/dashboard` 退出码 0
  </acceptance_criteria>
  <done>可挂载的搜索面板 + 开闭状态；点击结果可发跳转事件。</done>
</task>

<task type="auto">
  <name>Task 2: 分栏佈局、transcript 表头切換、main 接线</name>
  <read_first>
    - `packages/dashboard/src/demo/main.ts`（全文）
    - `packages/dashboard/src/transcript/talkie-transcript.ts`（全文）
    - `packages/dashboard/src/theme/theme.css`（`index.html` 已 link）
    - `packages/dashboard/index.html`（确认样式链）
  </read_first>
  <files>packages/dashboard/src/transcript/talkie-transcript.ts, packages/dashboard/src/demo/main.ts, packages/dashboard/src/theme/theme.css</files>
  <action>
    1. `talkie-transcript.ts`：在 `.head` 行内增加 **按钮** `aria-label="Search transcript"` 或文案 `搜索`，`@click` 调用 `this.store.setTranscriptSearchPanelOpen(!this.store.transcriptSearchPanelOpen)`（或仅派发事件由 main 处理；**必须**最终 toggle `transcriptSearchPanelOpen`）。
    2. `main.ts`：在 `import` 增加 `../shell/talkie-search-panel.js` 副作用注册。
    3. 佈局（D-04）：将原 `mainPanel` 內** transcript + search** 包在一 flex 容器：`<div class="talkie-transcript-workspace" style=…>` 或 CSS class：
    - 默认 `talkie-transcript` `flex:1`
    - 当 `store.transcriptSearchPanelOpen` 为 true 时，右侧渲染 `talkie-search-panel` `width: min(380px, 40vw)` 或**固定 320–400px 之间一值**（在代码中写死数字 + 注释），左 transcript 与右 panel **同排**
    - 监听 `store`：`searchPanel` 的 `store` 复用、并在 `transcriptSearchPanelOpen` 时显示或 `display:none` 二选一则另一路须不占宽（**必须**同屏时两侧皆可见的 flex 行）
    4. 监听 `talkie-jump-to-dedupe`（若事件名不同则与 Task 1 一致）：`const t = document.querySelector("talkie-transcript")` 后 `(t as TalkieTranscript).scrollToDedupeKey(detail.dedupeKey)`；TypeScript 可用类型断言或 `as unknown`。
    5. 在 `theme.css` 增加类 **`.talkie-transcript-workspace`**，**至少**含：`display: flex; flex: 1; min-height: 0;`（可追加 `align-items: stretch`），保证不挤坏 `talkie-send-bar` 与两栏可滚动。
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run build:app -w @agent-talkie/dashboard 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `main.ts` 含 `talkie-search-panel` 字面量或 `createElement("talkie-search-panel")`
    - `main.ts` 含 `scrollToDedupeKey`
    - `talkie-transcript.ts` 的 template 在 `.head` 区域含 `搜索` 或 `Search` 或 `aria-label="Search` 之一（`rg '搜索|Search|aria-label=\"Search' packages/dashboard/src/transcript/talkie-transcript.ts`）
    - `npm run build:app -w @agent-talkie/dashboard` 退出码 0
  </acceptance_criteria>
  <done>分栏与切換、结果跳转接到 `scrollToDedupeKey`；生产构建通过。</done>
</task>

<task type="auto">
  <name>Task 3: 名册 Needs Attention 与去重列表</name>
  <read_first>
    - `packages/dashboard/src/roster/talkie-roster.ts`（全文）
    - `packages/dashboard/src/roster/talkie-roster-entry.ts`（`TalkieRosterEntry` 与 blocked 类名）
  </read_first>
  <files>packages/dashboard/src/roster/talkie-roster.ts</files>
  <action>
    1. 在 `render()` 将 `entries` 拆为：`const blocked = this.entries.filter((r) => r.progress === "blocked")` 与 `const rest = this.entries.filter((r) => r.progress !== "blocked")`。
    2. `rest` 的排序**仅** `sessionId.localeCompare`（升序），**不再**以 blocked 优先排序（因 blocked 已抽出）。
    3. 若 `blocked.length > 0`：在 `<div class="head">Roster</div>` 之后渲染**第一节** `div` class 名 **`talkie-roster-attention`**，内含**可见**子标题**精确字符串** `Needs Attention`（D-10 英文，便于 grep），内为 `blocked.map` → `talkie-roster-entry`（**相同**属性 `.row` 等，复用 D-12）。
    4. 若 `blocked.length === 0`：**不**渲染 `talkie-roster-attention` 节点与 `Needs Attention` 文本（D-13）。
    5. 第二节为其余 `rest` 的列表；若 `rest.length === 0` 且 `blocked.length > 0` 是否显示 “No other members” 由实现选择，**不得**在顶区无 blocked 时显示空 “Needs Attention”。
    6. CSS：为 `.talkie-roster-attention` 加与主列表**可区分**的 `background` 或 `border`（`1px solid var(--talkie-border)` + 深一点的 `background` 即可，具体色值写死在 `talkie-roster.ts` 的 `css` 中）。
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm test -w @agent-talkie/dashboard 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `talkie-roster.ts` 含字符串 `talkie-roster-attention`
    - `talkie-roster.ts` 含 `Needs Attention`
    - `talkie-roster.ts` 含 `r.progress === "blocked"` 与 `!== "blocked"` 两处 filter（`rg 'progress === \"blocked\"' packages/dashboard/src/roster/talkie-roster.ts` 和 `rg '!== \"blocked\"'` 均有结果）
    - `npm test -w @agent-talkie/dashboard` 退出码 0
  </acceptance_criteria>
  <done>关注区 + 主列表不重复；无 blocked 时无关注区块。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 搜索高亮 | 若实现摘要高亮，关键词仅作文本节点分段，禁止 `innerHTML` 拼接未转义查询 |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | I | talkie-search-panel 摘要 | 缓解 | 使用 `html`+字符串片段或 `textContent`；查询来自本机 |
</threat_model>

<verification>`npm test -w @agent-talkie/dashboard` 与 `npm run build:app -w @agent-talkie/dashboard` 均通过；`rg Needs Attention packages/dashboard/src/roster` 命中。</verification>

<success_criteria>OVER-03/06 的 UI 可手动在 demo 验证；与 12-CONTEXT 决策 D-03..D-13 一致。</success_criteria>

<output>完成后创建 `.planning/phases/12-discovery-topology-attention/12-02-SUMMARY.md`</output>
