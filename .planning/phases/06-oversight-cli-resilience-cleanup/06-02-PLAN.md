---
phase: 06-oversight-cli-resilience-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/package.json
  - packages/cli/tsup.config.ts
  - package-lock.json
autonomous: true
gap_closure: true
requirements:
  - CLI-03
user_setup: []
must_haves:
  truths:
    - "`@agent-talkie/cli` 不再声明对 `@agent-talkie/protocol` 的依赖"
    - "CLI 的 `pretest` 不再构建 `@agent-talkie/protocol`"
    - "tsup `external` 列表不含 `@agent-talkie/protocol`"
  artifacts:
    - path: "packages/cli/package.json"
      provides: "dependencies 与 pretest 脚本已去除 protocol"
    - path: "packages/cli/tsup.config.ts"
      provides: "打包 external 与源码一致"
  key_links:
    - from: "packages/cli/package.json"
      to: "npm run test -w @agent-talkie/cli"
      via: "pretest 仍须成功构建 workspace 链路至 cli"
---

<objective>
从 CLI 包中彻底移除未使用的 `@agent-talkie/protocol`（D-04）：依赖声明、tsup externals、以及 `pretest` 中的 protocol 构建步骤，降低安装与测试前置复杂度。

Purpose: 满足阶段成功标准 3，并落实 CONTEXT D-04。

Output: 更新后的 `packages/cli/package.json` 与 `packages/cli/tsup.config.ts`。
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-oversight-cli-resilience-cleanup/06-CONTEXT.md
@packages/cli/package.json
@packages/cli/tsup.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 移除 @agent-talkie/protocol（dependencies、pretest、tsup external）</name>
  <files>packages/cli/package.json,packages/cli/tsup.config.ts</files>
  <read_first>
    - packages/cli/package.json
    - packages/cli/tsup.config.ts
    - packages/cli/src/cli.ts
    - package-lock.json
    - .planning/phases/06-oversight-cli-resilience-cleanup/06-CONTEXT.md
  </read_first>
  <action>
    1. **`packages/cli/package.json` — `dependencies`**：删除键 **`"@agent-talkie/protocol": "0.0.0"`** 整行（含尾部逗号符合 JSON 语法）。
    2. **`packages/cli/package.json` — `scripts.pretest`**：从字符串中删除子串 **`npm run build -w @agent-talkie/protocol && `**（含末尾空格与 `&&`），使 `pretest` 以 **`npm run build -w @agent-talkie/persistence`** 开头，后续仍为 `&& npm run build -w @agent-talkie/relay && ... @agent-talkie/supervisor && ... @agent-talkie/cli`**，顺序不变。
    3. **`packages/cli/tsup.config.ts`**：从 `external` 数组中删除字符串 **`"@agent-talkie/protocol"`**（保留 `better-sqlite3`、`@agent-talkie/persistence`、`@agent-talkie/client`、`@agent-talkie/supervisor`）。
    4. 在仓库根目录执行 **`npm install`**（无额外参数），使根目录 **`package-lock.json`** 与更新后的 `packages/cli/package.json` 一致（npm workspaces 会修剪 CLI 对 `@agent-talkie/protocol` 的链接记录）。
    5. 用 `rg "@agent-talkie/protocol" packages/cli` 确认 `packages/cli` 目录下无残留；并对根目录 `package-lock.json` 中 `@agent-talkie/cli` 依赖段进行目视确认：不应再将 `@agent-talkie/protocol` 列为该包的直接依赖（若 lock 结构为嵌套 `node_modules`，以 npm 实际输出为准）。

    不得新增对 `protocol` 的源码 import；若发现 import，应删除 import 并改用已有依赖（本任务范围内无则跳过）。
  </action>
  <verify>
    <automated>npm run test -w @agent-talkie/cli && npm run build -w @agent-talkie/cli</automated>
  </verify>
  <acceptance_criteria>
    - `packages/cli/package.json` **不包含**子串 `@agent-talkie/protocol`
    - `packages/cli/tsup.config.ts` **不包含**子串 `@agent-talkie/protocol`
    - `rg "@agent-talkie/protocol" packages/cli` 无匹配
    - 根目录 `package-lock.json` 已提交且与当前 `package.json` 一致（`npm install` 后无未提交漂移）
    - `npm run test -w @agent-talkie/cli` 退出码为 0
    - `npm run build -w @agent-talkie/cli` 退出码为 0
  </acceptance_criteria>
  <done>CLI 包与打包配置不再依赖或外部化 `@agent-talkie/protocol`；测试与构建通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm 依赖图 → 运行时模块解析 | 移除未使用依赖，缩小供给面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | 供应链误删必要依赖导致构建失败 | mitigate | `npm run test` / `build` 作为回归闸 |
</threat_model>

<verification>
- `npm run test -w @agent-talkie/cli` 通过
- `npm run build -w @agent-talkie/cli` 通过
</verification>

<success_criteria>
`@agent-talkie/protocol` 从 CLI `package.json`、`pretest`、`tsup.config.ts` 中完全移除；CLI 测试全绿。
</success_criteria>

<output>
After completion, create `.planning/phases/06-oversight-cli-resilience-cleanup/06-02-SUMMARY.md`
</output>
