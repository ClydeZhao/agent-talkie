---
phase: 06-oversight-cli-resilience-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/oversight/db.ts
  - packages/cli/src/cli.test.ts
autonomous: true
gap_closure: true
requirements:
  - OVER-01
  - CLI-03
user_setup: []
must_haves:
  truths:
    - "`talkie who`, `talkie transcript`, `talkie space status` on a fresh `AGENT_TALKIE_DATA_DIR` exit with `space not found` instead of `SqliteError: no such table`"
    - "Data directory is created before opening `relay.sqlite`"
    - "`migrate()` runs after `openDatabase()` so schema exists before oversight queries"
  artifacts:
    - path: "packages/cli/src/oversight/db.ts"
      provides: "mkdir + openDatabase + migrate for relay.sqlite"
    - path: "packages/cli/src/cli.test.ts"
      provides: "regression test for fresh data dir without relay start"
  key_links:
    - from: "packages/cli/src/oversight/static-commands.ts"
      to: "openRelayDatabase"
      via: "every static oversight command opens DB through db.ts"
---

<objective>
在 `openRelayDatabase()` 中于打开 SQLite 之前递归创建数据目录，并在 `openDatabase()` 之后调用 `migrate()`（D-01、D-02），使静态监督命令在从未启动过 relay 的目录上也能安全查询；`talkie watch` 仍仅依赖现有的 `ensureRelayRunning` 路径，不因本变更改为“只 migrate、不连 relay”（D-03）。补充 Vitest 集成用例锁定“无表”崩溃不再出现。

Purpose: 关闭 v1.0 审计中的 oversight CLI 与未初始化 DB 的集成缺口；满足成功标准 1–2 与 4。

Output: 更新后的 `db.ts`、扩展的 `cli.test.ts`。
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-oversight-cli-resilience-cleanup/06-CONTEXT.md
@packages/persistence/src/migrate.ts
@packages/persistence/src/db.ts
@packages/supervisor/src/paths.ts
@packages/cli/src/oversight/static-commands.ts
@packages/cli/src/oversight/watch.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: openRelayDatabase — 创建数据目录并执行 migrate</name>
  <files>packages/cli/src/oversight/db.ts</files>
  <read_first>
    - packages/cli/src/oversight/db.ts
    - packages/persistence/src/index.ts
    - packages/persistence/src/migrate.ts
    - packages/supervisor/src/paths.ts
    - .planning/phases/06-oversight-cli-resilience-cleanup/06-CONTEXT.md
  </read_first>
  <action>
    在 `packages/cli/src/oversight/db.ts` 中实现 D-01、D-02：

    1. 自 `node:fs` 增加 `import { mkdirSync } from "node:fs"`（或等价 `node:fs` 具名导入）。
    2. 将 `@agent-talkie/persistence` 的导入从仅 `openDatabase` 改为同时导入 `migrate`（与 `openDatabase` 同一包、同一 import 语句或相邻语句）。
    3. 在 `openRelayDatabase()` 内：先 `const dataDir = resolveAgentTalkieDataDir()`，再执行 **`mkdirSync(dataDir, { recursive: true })`**（字面选项 `{ recursive: true }`）。
    4. 使用 **`join(dataDir, RELAY_SQLITE_BASENAME)`** 作为路径调用 `openDatabase(...)`，将返回值赋给局部变量 `db`（或等价命名）。
    5. 在返回前对同一 `db` 实例调用 **`migrate(db)`**（`migrate` 来自 `@agent-talkie/persistence`）。
    6. 返回该 `db` 实例。不得在 `openRelayDatabase()` 内调用 `ensureRelayRunning` 或启动 relay 进程。

    不修改 `static-commands.ts` / `watch.ts` 的调用方式；`watch.ts` 中已有的 `ensureRelayRunning` 调用保持不变（D-03）。
  </action>
  <verify>
    <automated>npm run test -w @agent-talkie/cli && npm run build -w @agent-talkie/cli</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `packages/cli/src/oversight/db.ts` 包含子串 `mkdirSync`
    - 同一文件包含子串 `migrate(` 且 `migrate` 从 `@agent-talkie/persistence` 导入
    - 同一文件包含子串 `{ recursive: true }`
    - 同一文件**不包含**子串 `ensureRelayRunning`
    - `npm run build -w @agent-talkie/cli` 退出码为 0
  </acceptance_criteria>
  <done>`openRelayDatabase` 在任意干净数据目录上打开 DB 前创建目录并应用迁移，无 relay 启动副作用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vitest — 未启动 relay 的隔离数据目录上三条静态命令不抛 SQLite 无表错误</name>
  <files>packages/cli/src/cli.test.ts</files>
  <read_first>
    - packages/cli/src/cli.test.ts
    - packages/cli/src/cli.ts
    - packages/cli/src/oversight/db.ts
  </read_first>
  <behavior>
    - 使用仅设置 `AGENT_TALKIE_DATA_DIR` 指向 `mkdtempSync` 目录的 env，**不**调用 `relay start`，对 slug `fresh-oversight-slug` 运行 `who`、`space status`、`transcript` 时，三者 `status` 均为 **1**，且 `stderr` 包含 **`space not found: fresh-oversight-slug`**
    - 上述三次运行的合并 `stderr`（或各次）**不包含**子串 **`SqliteError`** 且**不包含**子串 **`no such table`**
  </behavior>
  <action>
    在 `packages/cli/src/cli.test.ts` 的 `describe("talkie CLI", () => { ... })` 内新增一个 `it(...)`（名称建议包含 `fresh data dir` 或 `oversight`），复用现有的 `runCli`、`mkdtempSync`、`path.join(os.tmpdir(), ...)` 模式：

    1. `dataDir = mkdtempSync(path.join(os.tmpdir(), "agent-talkie-cli-oversight-"))`（前缀可微调但须唯一）。
    2. `const env = { AGENT_TALKIE_DATA_DIR: dataDir }` — **不要**设置 `AGENT_TALKIE_RELAY_PORT`，**不要**执行 `relay start`。
    3. 依次 `runCli(["who", "--slug", "fresh-oversight-slug"], env)`、`runCli(["space", "status", "--slug", "fresh-oversight-slug"], env)`、`runCli(["transcript", "--slug", "fresh-oversight-slug", "--limit", "5"], env)`。
    4. 对每个结果断言 `status === 1`，`stderr` 含 `space not found: fresh-oversight-slug`，且 `stderr` 不含 `SqliteError`、不含 `no such table`。
    5. 在 `afterEach` 中对该测试使用的目录：若与其它测试共享 `dataDir` 变量，确保仅在本测试赋值并在 teardown 中 `rmSync`；若冲突，使用独立局部变量并在 `try/finally` 内清理。

    slug 字符串字面量必须为 **`fresh-oversight-slug`**，与 `acceptance_criteria` 中 grep 一致。
  </action>
  <verify>
    <automated>npm run test -w @agent-talkie/cli</automated>
  </verify>
  <acceptance_criteria>
    - `packages/cli/src/cli.test.ts` 包含子串 `fresh-oversight-slug` 至少 4 次（三处命令 + 一处期望 stderr）
    - 同一文件包含子串 `space not found: fresh-oversight-slug`
    - 同一文件包含对 `SqliteError` 或 `no such table` 的否定断言（如 `not.toContain` / `not.toMatch`）
    - `npm run test -w @agent-talkie/cli` 退出码为 0
  </acceptance_criteria>
  <done>自动化证明静态监督命令在空库路径上走迁移后“空间不存在”分支，而非 SQLite 结构错误。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本机进程 → `AGENT_TALKIE_DATA_DIR` 下 `relay.sqlite` | 本地文件创建与迁移；与既有 CLI 数据目录模型一致 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | 可写数据目录下伪造 `relay.sqlite` | accept | 本地操作者模型；与 Phase 3 监督路径一致 |
| T-06-02 | Denial of service | `mkdir` / 迁移在异常盘上失败 | mitigate | 沿用 `openDatabase` 既有错误语义；不吞异常 |
</threat_model>

<verification>
- `npm run test -w @agent-talkie/cli` 通过
- `npm run build -w @agent-talkie/cli` 通过
</verification>

<success_criteria>
新鲜数据目录上 `who` / `transcript` / `space status` 无 `SqliteError: no such table`；目录自动创建；`migrate` 在查询前执行；CLI 测试全绿。
</success_criteria>

<output>
After completion, create `.planning/phases/06-oversight-cli-resilience-cleanup/06-01-SUMMARY.md`
</output>
