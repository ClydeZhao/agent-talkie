---
phase: 11-space-membership-management
plan: 03
type: execute
wave: 3
depends_on:
  - "11-02"
files_modified:
  - packages/persistence/src/repositories/oversight.ts
  - packages/persistence/src/index.ts
  - packages/persistence/src/repositories/oversight.test.ts
  - packages/relay/src/server.ts
  - packages/dashboard/src/bridge/browser-session-bridge.ts
  - packages/dashboard/src/bridge/wire-schemas.ts
  - packages/dashboard/src/store/dashboard-store.ts
  - packages/dashboard/src/shell/talkie-space-picker.ts
  - packages/dashboard/src/demo/main.ts
autonomous: true
requirements:
  - MGMT-01
  - MGMT-03
user_setup: []
must_haves:
  truths:
    - `GET /__agent-talkie/v1/oversight/spaces` 返回 JSON 数组，元素含 `slug`、`memberCount`、`ownerSessionId`、`orchestratorSessionId`，且仅含 `active` 空间
    - 仪表盘首屏从 `?space=<slug>` 绑定 join slug；无参数时使用 fallback slug `default`（与 `normalizeSpaceSlug` 合法值一致）
    - 头部空间选择器列出上述 API 结果；点击**其他**空间时 `window.open` 打开 `/dashboard?space=<slug>` 新标签（同源）；底部含 **Create new space** 内联 slug 输入与确认，成功则 `joinSpace` 新 slug
    - 所有者可通过选择器内销毁入口触发确认后发送 `space.destroy`（payload.slug 为当前空间规范化 slug）
  artifacts:
    - path: packages/persistence/src/repositories/oversight.ts
      provides: listOversightSpaces
      contains: listOversightSpaces
    - path: packages/relay/src/server.ts
      provides: HTTP GET oversight/spaces
      contains: oversight/spaces
    - path: packages/dashboard/src/shell/talkie-space-picker.ts
      provides: 头部下拉 + 创建 + 销毁 + 切换
      contains: Create new space
    - path: packages/dashboard/src/demo/main.ts
      provides: URL 解析、挂载 picker、拉取列表
      contains: get("space")
  key_links:
    - from: packages/dashboard/src/demo/main.ts
      to: packages/relay/src/server.ts
      via: fetch /__agent-talkie/v1/oversight/spaces
    - from: packages/dashboard/src/shell/talkie-space-picker.ts
      to: packages/dashboard/src/bridge/browser-session-bridge.ts
      via: sendSpaceDestroy / joinSpace
---

<objective>
交付 **空间列表 HTTP API**、**URL 驱动 join**、**头部空间选择器**（D-09–D-12）：列表、新标签切换、内联创建（`space.join`）、所有者销毁确认（`space.destroy`），并完成 `BrowserSessionBridge` 与 store 状态接线。

Purpose: 关闭 MGMT-03 与 MGMT-01 的仪表盘侧能力（创建/销毁/列举/切换）。

Output: persistence 查询、relay 路由、Lit `talkie-space-picker`、`main.ts` 集成。
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@.planning/phases/11-space-membership-management/11-CONTEXT.md
</execution_context>

<context>
@.planning/phases/11-space-membership-management/11-CONTEXT.md
@packages/persistence/src/repositories/oversight.ts
@packages/relay/src/server.ts
@packages/dashboard/src/demo/main.ts
@packages/dashboard/src/bridge/browser-session-bridge.ts
@packages/dashboard/src/store/dashboard-store.ts

## 决策追溯

- **D-01 / D-02**：创建 = 调用已有 `bridge.joinSpace({ slug: <用户输入规范化后>, idempotencyKey: crypto.randomUUID() })`；UI 文案 **Create new space**（英文，便于 grep 验收）；slug 输入须客户端预检 `normalizeSpaceSlug` 同源规则：仅当包可从前端 import 该函数；若 tree-shake 不便，则复制文档化 regex `^[a-z0-9]+(?:-[a-z0-9]+)*$` 最大 64 于 `talkie-space-picker.ts` 顶部常量并与 persistence 注释互指。
- **D-03 / D-05**：销毁前 `window.confirm` 文案须包含子串 `Destroy` 与空间 `slug`（例如 ``confirm(`Destroy space "${slug}"? This cannot be undone.`)``）；仅 `store.selfIsOwner === true` 时显示销毁入口。
- **D-09**：响应体为 **JSON 数组** `[{ slug, memberCount, ownerSessionId, orchestratorSessionId }, ...]`，不包含 invite 字段。
- **D-10 / D-11**：切换空间：`window.open(\`${location.origin}/dashboard?space=${encodeURIComponent(slug)}\`, "_blank", "noopener,noreferrer")\``（生产同源）；开发环境若 `location.origin` 无 `/dashboard` 前缀，使用 ``new URL("/dashboard", location.origin)`` 构造基路径再附加 search。
- **D-12**：`const q = new URLSearchParams(location.search).get("space"); const initialSlug = q && q.length > 0 ? q : "default";` 然后用 `initialSlug` 调用 `joinSpace`（替换现有 `DEMO_SPACE_SLUG` 常量全流程）。

<interfaces>
```typescript
// packages/persistence/src/repositories/oversight.ts — 将新增
export type OversightSpaceListRow = {
  slug: string;
  memberCount: number;
  ownerSessionId: string | null;
  orchestratorSessionId: string | null;
};
export function listOversightSpaces(db: Database.Database): OversightSpaceListRow[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: listOversightSpaces 与 GET /oversight/spaces</name>
  <files>packages/persistence/src/repositories/oversight.ts, packages/persistence/src/index.ts, packages/persistence/src/repositories/oversight.test.ts, packages/relay/src/server.ts</files>
  <read_first>
    - packages/persistence/src/repositories/oversight.ts
    - packages/persistence/src/repositories/oversight.test.ts
    - packages/persistence/src/index.ts
    - packages/relay/src/server.ts
    - packages/persistence/src/repositories/spaces.ts
    - .planning/phases/11-space-membership-management/11-CONTEXT.md
  </read_first>
  <behavior>
    - 两间 `active` 空间 `alpha`、`beta` 时，`listOversightSpaces` 返回长度 2，且按 `slug` **升序** 排列。
    - `archived` 空间不出现在列表中。
    - `memberCount` 等于 `left_at IS NULL` 的成员数。
  </behavior>
  <action>
    - 在 `oversight.ts` 实现 `listOversightSpaces(db)`：`SELECT` `spaces` 表 `status = 'active'`；每行 `slug`、`orchestrator_session_id`；`memberCount` 用子查询或 `LEFT JOIN` 计数 `space_memberships` 且 `left_at IS NULL`；`ownerSessionId` 用 `getSpaceOwnerSessionId(db, space.id)` 或在 SQL 中复用与 `getOversightSpaceSummaryBySlug` 一致的 owner 解析方式；**ORDER BY s.slug ASC**。
    - 在 `packages/persistence/src/index.ts` 导出 `listOversightSpaces` 与 `OversightSpaceListRow`。
    - 在 `oversight.test.ts` 追加至少一个用例：插入两空间、成员，断言顺序与计数。
    - 在 `server.ts`：`import { listOversightSpaces } from "@agent-talkie/persistence"`；在 `space-summary` 路由旁增加 `req.method === "GET" && url.pathname === "/__agent-talkie/v1/oversight/spaces"`：返回 `200`、`Content-Type: application/json`、`JSON.stringify(listOversightSpaces(db))`。
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/persistence -- --run src/repositories/oversight.test.ts && npm run test -w @agent-talkie/relay -- --run src/server.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export function listOversightSpaces" packages/persistence/src/repositories/oversight.ts` 至少一行匹配。
    - `rg "listOversightSpaces" packages/persistence/src/index.ts` 至少一行匹配。
    - `rg "oversight/spaces" packages/relay/src/server.ts` 至少一行匹配。
    - `npm run test -w @agent-talkie/persistence -- --run src/repositories/oversight.test.ts` 退出码为 0。
    - `npm run test -w @agent-talkie/relay -- --run src/server.test.ts` 退出码为 0。
  </acceptance_criteria>
  <done>列表 API 与持久层测试通过。</done>
</task>

<task type="auto">
  <name>Task 2: sendSpaceDestroy、space.destroyed wire、store 列表状态</name>
  <files>packages/dashboard/src/bridge/browser-session-bridge.ts, packages/dashboard/src/bridge/wire-schemas.ts, packages/dashboard/src/store/dashboard-store.ts</files>
  <read_first>
    - packages/dashboard/src/bridge/browser-session-bridge.ts
    - packages/dashboard/src/bridge/wire-schemas.ts
    - packages/dashboard/src/store/dashboard-store.ts
    - packages/relay/src/server.ts
    - .planning/phases/11-space-membership-management/11-01-PLAN.md
  </read_first>
  <action>
    - 在 `browser-session-bridge.ts` 新增 `sendSpaceDestroy(args: { spaceId: string; slug: string; idempotencyKey: string }): void`：`kind: "control"`, `type: "space.destroy"`, `payload: { slug: args.slug }`，`spaceId: args.spaceId`，`idempotencyKey` 取自 args。
    - 在 `wire-schemas.ts` 增加 `spaceDestroyedWireSchema`：`type` 字面量 **`space.destroyed`**，`slug` string；在 bridge 消息分发处（与 `spaceJoinedWireSchema` 并列）解析；收到后对 `ConnectionHealthUiState` 或 store 触发「空间已销毁」状态（**Claude discretion**：可设 `store.spaceDestroyedSlug = slug` 或触发只读 banner），因连接将被服务端关闭。
    - 在 `dashboard-store.ts` 增加可观察状态：`spacesList: OversightSpaceListRow[]`（类型可从 `../bridge/...` 重复定义最小接口或从 persistence 包 re-export 若 dashboard 已依赖 persistence — **优先**在 store 内联 `type SpaceListRow = { slug: string; memberCount: number; ownerSessionId: string | null; orchestratorSessionId: string | null }` 避免新 workspace 依赖）；方法 `setSpacesList(rows: SpaceListRow[])`；可选 `currentSpaceSlug: string` + `setCurrentSpaceSlug` 供 picker 显示。
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `rg "sendSpaceDestroy" packages/dashboard/src/bridge/browser-session-bridge.ts` 至少一行匹配。
    - `rg "space.destroy" packages/dashboard/src/bridge/browser-session-bridge.ts` 至少一行匹配。
    - `rg "spaceDestroyedWireSchema" packages/dashboard/src/bridge/wire-schemas.ts` 至少一行匹配。
    - `rg "space.destroyed" packages/dashboard/src/bridge/wire-schemas.ts` 至少一行匹配。
    - `rg "setSpacesList" packages/dashboard/src/store/dashboard-store.ts` 至少一行匹配。
    - `npm run test -w @agent-talkie/dashboard` 退出码为 0。
  </acceptance_criteria>
  <done>Bridge 与 store 支持销毁与列表状态。</done>
</task>

<task type="auto">
  <name>Task 3: talkie-space-picker、main.ts URL 与集成</name>
  <files>packages/dashboard/src/shell/talkie-space-picker.ts, packages/dashboard/src/demo/main.ts</files>
  <read_first>
    - packages/dashboard/src/demo/main.ts
    - packages/dashboard/src/shell/connection-shell.ts
    - packages/dashboard/src/store/dashboard-store.ts
    - packages/dashboard/src/bridge/browser-session-bridge.ts
    - packages/persistence/src/repositories/spaces.ts
    - .planning/phases/11-space-membership-management/11-CONTEXT.md
  </read_first>
  <action>
    - 新建 `packages/dashboard/src/shell/talkie-space-picker.ts`：Lit 自定义元素 **`talkie-space-picker`**；`@property`：`store: DashboardStore | null`、`bridge: BrowserSessionBridge | null`、`httpOrigin: string`、`currentSlug: string`、`selfIsOwner: boolean`。
    - 打开下拉时 `fetch(\`${httpOrigin}/__agent-talkie/v1/oversight/spaces\`)`，`200` 时 `JSON.parse` 为数组并 `store?.setSpacesList(...)`；渲染每个空间的 `slug` 按钮；点击**非** `currentSlug` 项时执行 D-11 的 `window.open`。
    - 底部渲染字面 **`Create new space`**：展开 `input`（`type="text"`）+ 确认按钮；确认时调用 `bridge.joinSpace({ slug: <trim 后输入>, idempotencyKey: crypto.randomUUID() })`；成功则 `store.setActiveSpaceId`（若 bridge 返回 spaceId）、`history.replaceState` 更新 URL 为 `?space=<slug>`（Claude discretion）、关闭下拉、触发父级已有 `pullSpaceSummary` 回调（通过 `CustomEvent` 或由 main 传入函数 prop）。
    - 所有者可见 **Destroy** 入口（`selfIsOwner`）：点击后执行含 **Destroy** 子串的 `window.confirm`；确认后 `bridge.sendSpaceDestroy({ spaceId, slug: currentSlug, idempotencyKey: crypto.randomUUID() })`。
    - `demo/main.ts`：删除或停用常量 `DEMO_SPACE_SLUG = "dashboard"` 作为唯一 join 源；在 register/resume 后执行：`const initialSlug = (() => { const q = new URLSearchParams(location.search).get("space"); return q && q.length > 0 ? q : "default"; })();`；`await bridge.joinSpace({ slug: initialSlug, idempotencyKey: crypto.randomUUID() });`；将 `space-summary` fetch 中 slug 查询参数改为 `initialSlug` 变量随 `currentSlug` 更新（若创建/切换后 replaceState，须同步后续 snapshot 使用 `store.currentSpaceSlug` 或事件）。
    - 在 header 行挂载：`import "./shell/talkie-space-picker.js"`；创建 `talkie-space-picker`，设置 `httpOrigin = deriveHttpOriginFromWsUrl(wsUrl)`（与现 fetch 一致），与 `talkie-connection-shell` 同排插入（flex 布局：`main.ts` 或 `index.html` 现有 `.talkie-app__header` 内）。
    - `pullSpaceSummary` 中 URL 必须使用当前空间 slug（`encodeURIComponent`），不得硬编码 `dashboard`。
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/dashboard && npm run build -w @agent-talkie/dashboard</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `packages/dashboard/src/shell/talkie-space-picker.ts` 存在。
    - `rg "customElement(\"talkie-space-picker\")" packages/dashboard/src/shell/talkie-space-picker.ts` 至少一行匹配。
    - `rg "Create new space" packages/dashboard/src/shell/talkie-space-picker.ts` 至少一行匹配。
    - `rg "get(\"space\")" packages/dashboard/src/demo/main.ts` 至少一行匹配。
    - `rg '"default"' packages/dashboard/src/demo/main.ts` 至少一行匹配（fallback slug）。
    - `rg "window.open" packages/dashboard/src/shell/talkie-space-picker.ts` 至少一行匹配。
    - `rg "oversight/spaces" packages/dashboard/src/shell/talkie-space-picker.ts` 至少一行匹配。
    - `rg "initialSlug" packages/dashboard/src/demo/main.ts` 至少一行匹配。
    - `rg "joinSpace\\(\\{[^}]*slug:\\s*initialSlug" packages/dashboard/src/demo/main.ts` 至少一行匹配（或等价地 `joinSpace` 的 `slug` 实参为变量 `initialSlug` 而非字面量 `"dashboard"`）。
    - `npm run build -w @agent-talkie/dashboard` 退出码为 0。
  </acceptance_criteria>
  <done>空间选择器与 URL join 集成完成；dashboard 构建通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP GET /oversight/spaces | localhost relay；返回元数据无 transcript 正文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Information disclosure | listOversightSpaces | accept | v2.0 本机调试面；仅 slug 与计数 |
| T-11-07 | Tampering | 客户端 slug 输入 | mitigate | 服务端 `normalizeSpaceSlug`；join/destroy 最终校验在 relay |
</threat_model>

<verification>
- `npm test`（根脚本）或至少 `persistence` + `relay` + `dashboard` workspace 测试与 dashboard build 通过。
</verification>

<success_criteria>
- 运维可列举空间、用新标签切换、创建新空间 join、所有者销毁确认生效；`?space=` 与 `default` fallback 行为可 grep 验证。
</success_criteria>

<output>
After completion, create `.planning/phases/11-space-membership-management/11-03-SUMMARY.md`
</output>
