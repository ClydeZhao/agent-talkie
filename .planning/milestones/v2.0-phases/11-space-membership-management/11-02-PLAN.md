---
phase: 11-space-membership-management
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - packages/relay/src/space-lifecycle.ts
  - packages/relay/src/server.ts
  - packages/relay/src/__tests__/membership-remove.test.ts
  - packages/dashboard/src/bridge/browser-session-bridge.ts
  - packages/dashboard/src/bridge/wire-schemas.ts
  - packages/dashboard/src/errors/relay-error-copy.ts
  - packages/dashboard/src/roster/talkie-roster-entry.ts
  - packages/dashboard/src/roster/talkie-roster.ts
  - packages/dashboard/src/demo/main.ts
autonomous: true
requirements:
  - MGMT-02
user_setup: []
must_haves:
  truths:
    - 空间所有者可从名册对**非自身**、**非 owner** 的会话执行 Remove，中继执行 `membership.remove` 后目标连接被关闭且名册可刷新
    - 非所有者点击 Remove 时不发送 envelope 或中继返回 `not_space_owner`（与 orchestrator 菜单门控一致）
    - REQUIREMENTS 中「invite」在 localhost v2.0 为不适用：仪表盘不提供 invite UI；验收以 remove 与文案/注释可追溯为准
  artifacts:
    - path: packages/relay/src/space-lifecycle.ts
      provides: handleMembershipRemove、isMembershipRemoveEnvelope
      contains: "membership.remove"
    - path: packages/dashboard/src/roster/talkie-roster-entry.ts
      provides: Owner-only「Remove」菜单项
      contains: "Remove"
    - path: packages/dashboard/src/demo/main.ts
      provides: 监听 `talkie-membership-remove` 并调用 bridge
      contains: talkie-membership-remove
  key_links:
    - from: packages/dashboard/src/demo/main.ts
      to: packages/dashboard/src/bridge/browser-session-bridge.ts
      via: sendMembershipRemove 或 sendEnvelope
    - from: packages/relay/src/server.ts
      to: packages/relay/src/space-lifecycle.ts
      pattern: handleMembershipRemove
---

<objective>
实现 **成员剔除**（`membership.remove`）全栈：中继校验所有者、幂等、`markMembershipLeft`、关闭目标 WebSocket；仪表盘 bridge 封装、名册 Owner-only「Remove」、main 事件接线。

Purpose: 满足 MGMT-02 的 remove 语义；invite 按 CONTEXT **D-06** 不在本产品面实现（无 invite UI）。

Output: 新控制类型端到端 + Vitest（relay）+ 仪表盘交互。
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@.planning/phases/11-space-membership-management/11-CONTEXT.md
</execution_context>

<context>
@.planning/phases/11-space-membership-management/11-CONTEXT.md
@packages/relay/src/space-lifecycle.ts
@packages/relay/src/server.ts
@packages/dashboard/src/bridge/browser-session-bridge.ts
@packages/dashboard/src/bridge/wire-schemas.ts
@packages/dashboard/src/roster/talkie-roster-entry.ts
@packages/dashboard/src/demo/main.ts

## 决策追溯

- **D-07** / **D-08**：`type: "membership.remove"`，`payload: { targetSessionId: string }`（JSON 字段名须与此完全一致）；须 `idempotencyKey`；**无**确认对话框；Owner-only 可见（与 Phase 10 orchestrator 菜单同一 `selfIsOwner` 门控）。
- 禁止移除 `getSpaceOwnerSessionId` 等于的 `targetSessionId`：返回错误码 **`cannot_remove_space_owner`**（新码，须在 `relay-error-copy.ts` 增加人类可读文案）。
- 禁止移除发送者自身：返回 **`membership_remove_self`**（RESEARCH 建议；同样加入 `relay-error-copy.ts`）。
- 目标须在同一空间有 `left_at IS NULL` 的成员关系，否则 `not_in_space` 或专用错误 **`target_not_in_space`**（择一写死并在测试中断言）。
- 成功后：`markMembershipLeft`；`ctx.registry.get(targetSessionId)?.close()`；可选向空间内其他成员广播名册刷新（若无现成 fan-out，可省略，仅靠 HTTP snapshot 刷新）。

<interfaces>
```typescript
// browser-session-bridge — 参考现有 sendEnvelope / joinSpace
async joinSpace(args: { slug: string; idempotencyKey: string }): Promise<{ spaceId: string; slug: string }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: membership.remove 中继逻辑与测试</name>
  <files>packages/relay/src/space-lifecycle.ts, packages/relay/src/server.ts, packages/relay/src/__tests__/membership-remove.test.ts</files>
  <read_first>
    - packages/relay/src/space-lifecycle.ts
    - packages/relay/src/server.ts
    - packages/relay/src/__tests__/space-destroy.test.ts
    - packages/persistence/src/repositories/space-owner.ts
    - .planning/phases/11-space-membership-management/11-CONTEXT.md
  </read_first>
  <behavior>
    - 所有者对人类会话发送 `membership.remove` 且 `targetSessionId` 为另一活跃成员时，`markMembershipLeft` 被调用且返回成功 outcome。
    - `targetSessionId === ownerSessionId` 时返回 error 且 error 字段为 `cannot_remove_space_owner`。
    - `targetSessionId === senderSessionId` 时返回 `membership_remove_self`。
  </behavior>
  <action>
    在 `space-lifecycle.ts` 新增 `isMembershipRemoveEnvelope`、`handleMembershipRemove`，模式对齐 `handleSpaceLeave` 的 idempotency 事务；校验 `findActiveMembershipForSession` 发送者在当前空间；`getSpaceOwnerSessionId === senderSessionId`；`getSessionById(sender).isHuman`；`payload.targetSessionId` 为 string 且目标在该空间活跃；执行 `markMembershipLeft(spaceId, targetSessionId, nowMs)`；若移除后 `countActiveMembers === 0` 则与 leave 一致 `setSpaceArchived`（保持与 `handleSpaceLeave` 行为一致）。
    在 `server.ts` 的 `dispatchValidatedEnvelope` 中于 `isSpaceDestroyEnvelope` 分支之后（或按你插入的 destroy 分支紧邻位置）、`handleCollaborationControl` 之前，增加 `isMembershipRemoveEnvelope` 分支：解析 `idempotencyKey`、`payload.targetSessionId`；调用 handler；错误则 `protocol.error`；成功则 `sendJson` 类型 **`membership.removed`**，body 含 `spaceId`、`targetSessionId`（与现有 `space.left` 风格一致）；然后 `ctx.registry.get(targetSessionId)?.close()`。
    新建 `packages/relay/src/__tests__/membership-remove.test.ts` 覆盖 owner 成功、移除 owner 目标失败、移除 self 失败三类断言。
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/relay -- --run src/__tests__/membership-remove.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "membership.remove" packages/relay/src/space-lifecycle.ts` 至少一行匹配。
    - `rg "isMembershipRemoveEnvelope" packages/relay/src/server.ts` 至少一行匹配。
    - `rg "membership.removed" packages/relay/src/server.ts` 至少一行匹配。
    - `rg "cannot_remove_space_owner" packages/relay/src/space-lifecycle.ts` 至少一行匹配。
    - `rg "membership_remove_self" packages/relay/src/space-lifecycle.ts` 至少一行匹配。
    - `npm run test -w @agent-talkie/relay -- --run src/__tests__/membership-remove.test.ts` 退出码为 0。
  </acceptance_criteria>
  <done>Relay 侧 remove 完成且 Vitest 通过。</done>
</task>

<task type="auto">
  <name>Task 2: Bridge、wire 解析、错误文案与名册 Remove</name>
  <files>packages/dashboard/src/bridge/browser-session-bridge.ts, packages/dashboard/src/bridge/wire-schemas.ts, packages/dashboard/src/errors/relay-error-copy.ts, packages/dashboard/src/roster/talkie-roster-entry.ts, packages/dashboard/src/roster/talkie-roster.ts, packages/dashboard/src/demo/main.ts</files>
  <read_first>
    - packages/dashboard/src/bridge/browser-session-bridge.ts
    - packages/dashboard/src/bridge/wire-schemas.ts
    - packages/dashboard/src/errors/relay-error-copy.ts
    - packages/dashboard/src/roster/talkie-roster-entry.ts
    - packages/dashboard/src/roster/talkie-roster.ts
    - packages/dashboard/src/demo/main.ts
    - packages/dashboard/src/store/dashboard-store.ts
  </read_first>
  <action>
    - 在 `browser-session-bridge.ts` 新增 `sendMembershipRemove(args: { spaceId: string; targetSessionId: string; idempotencyKey: string }): void`：内部使用 `getNegotiatedEnvelopeVersion()`、`getRegisteredSessionId()`；`kind: "control"`, `type: "membership.remove"`, `payload: { targetSessionId: args.targetSessionId }`，`spaceId: args.spaceId`，`idempotencyKey: args.idempotencyKey`；**不得**包含 invite 字段。
    - 在 `wire-schemas.ts` 增加入站解析（若 bridge 监听 `membership.removed`）：使用 zod 定义 `membershipRemovedWireSchema`（字段 `type` 字面量 `membership.removed`，`spaceId`、`targetSessionId` 字符串）。
    - 在 `relay-error-copy.ts` 的 `RELAY_ERROR_COPY` 增加键 **`cannot_remove_space_owner`**、**`membership_remove_self`**（及 Task 1 选用的 `target_not_in_space` 若存在），英文或中英文短句均可，须非空字符串。
    - 在 `talkie-roster.ts` 增加 `@property({ type: String }) selfSessionId = ""`；`render` 里传给 entry：`.selfSessionId=${this.selfSessionId}`。
    - 在 `talkie-roster-entry.ts` 增加 `@property({ type: String }) selfSessionId = ""`；在 `ownerMenu` 面板中 orchestrator 按钮**下方**新增 Remove 按钮；仅当 `this.selfSessionId.length > 0 && r.sessionId !== this.selfSessionId` 时渲染 Remove；`_onRemove` 派发 `CustomEvent("talkie-membership-remove", { detail: { sessionId: r.sessionId }, bubbles: true, composed: true })`。
    - 在 `demo/main.ts` 于 `roster` 创建后赋值 `roster.selfSessionId = selfSessionId`（在 `selfSessionId` 变量可用之后，与 join/register 流程同一作用域）。
    - 在 `demo/main.ts` 中 `addEventListener("talkie-membership-remove", ...)`：读取 `detail.sessionId` 作为 `targetSessionId`，调用 `bridge.sendMembershipRemove({ spaceId: store.activeSpaceId!, targetSessionId, idempotencyKey: crypto.randomUUID() })`（在 `version`、`sessionId`、`spaceId` 非 null 时）。
    - 在 `main.ts` 文件**顶部注释**单行：`// MGMT-02 invite: N/A for localhost v2.0 per phase 11 CONTEXT D-06; sessions join via adapter/CLI.`
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `rg "sendMembershipRemove" packages/dashboard/src/bridge/browser-session-bridge.ts` 至少一行匹配。
    - `rg "talkie-membership-remove" packages/dashboard/src/demo/main.ts` 至少一行匹配。
    - `rg "MGMT-02 invite: N/A" packages/dashboard/src/demo/main.ts` 恰好一行匹配。
    - `packages/dashboard/src/roster/talkie-roster-entry.ts` 含子串 `>Remove</button>`（或 `Remove</button>` 无 `>` 前缀亦可：`rg "Remove</button>" packages/dashboard/src/roster/talkie-roster-entry.ts` 至少一行）。
    - `rg "selfSessionId" packages/dashboard/src/roster/talkie-roster.ts` 至少 2 行匹配。
    - `rg "cannot_remove_space_owner" packages/dashboard/src/errors/relay-error-copy.ts` 至少一行匹配。
    - `npm run test -w @agent-talkie/dashboard` 退出码为 0。
  </acceptance_criteria>
  <done>仪表盘可发 `membership.remove`；错误码有文案；Vitest 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 仪表盘 → relay | Remove 须服务端 owner 校验，客户端门控仅 UX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Elevation of privilege | handleMembershipRemove | mitigate | `getSpaceOwnerSessionId` + human sender + 禁止移除 owner |
| T-11-05 | Spoofing | targetSessionId | mitigate | 仅关闭 DB 中该空间活跃成员对应的 registry socket |
</threat_model>

<verification>
- `npm run test -w @agent-talkie/relay && npm run test -w @agent-talkie/dashboard`
</verification>

<success_criteria>
- 所有者可在名册移除其他成员；relay 测试覆盖 owner/self/owner-target 失败路径；代码中可 grep 到 MGMT-02 invite N/A 注释。
</success_criteria>

<output>
After completion, create `.planning/phases/11-space-membership-management/11-02-SUMMARY.md`
</output>
