---
phase: 07-browser-connection-session-bridge
plan: 3
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - packages/dashboard/src/bridge/browser-session-bridge.ts
  - packages/dashboard/src/bridge/reconnect-schedule.ts
  - packages/dashboard/src/bridge/reconnect-schedule.test.ts
  - packages/dashboard/src/bridge/transcript-catchup-dedupe.test.ts
  - packages/dashboard/src/demo/main.ts
autonomous: true
requirements:
  - CONN-02
user_setup: []
must_haves:
  truths:
    - WebSocket 异常断开后自动重连：指数退避起始 1000 ms、倍增、上限 30000 ms，并带 0–300 ms 随机抖动（实现见 `reconnect-schedule.ts`）
    - 重连尝试无上限，直至用户调用 `close()` 或 generation 探测判定 stale（停止重连并触发既有 stale UI）
    - `transcript.catchup` 的 `relaySeq` 小于等于已跟踪最大值时不提升 `maxRelaySeq`、不重复投递给 `onTranscriptCatchup` 监听者（若 Plan 1 未加监听者则在本 plan 增加 `onTranscriptCatchup(cb)` 仅在新 seq 时调用）
    - `session.resume` 收到 `protocol.error` 后清除 `SESSION_ID_KEY` 与 `RECONNECT_SECRET_KEY` 并走 `registerNewSession` 回退（与 D-14 一致）
  artifacts:
    - path: packages/dashboard/src/bridge/reconnect-schedule.ts
      provides: 退避毫秒计算
      contains: nextReconnectDelayMs
    - path: packages/dashboard/src/bridge/browser-session-bridge.ts
      provides: 重连循环 + catch-up 去重 + resume 失败回退
      contains: reconnecting
  key_links:
    - from: packages/dashboard/src/bridge/browser-session-bridge.ts
      to: packages/dashboard/src/bridge/relay-generation.ts
      via: 重连前 `probeRelayGenerationHealth`
      pattern: probeRelayGenerationHealth
---

<objective>
在 `BrowserSessionBridge` 中实现：断线后无限重试、指数退避（1 s 起、×2、顶 30 s）与抖动；每次重连打开 socket 前若 `sessionStorage` 中 `RELAY_GENERATION_KEY` 非空则调用 `probeRelayGenerationHealth`，失败则 `notifyRelayGenerationStale()` 并**停止**自动重连；`transcript.catchup` 按 `relaySeq` 去重；`session.resume` 失败时清凭证并注册新会话。

Purpose: 满足 CONN-02 与 CONTEXT D-09–D-12、D-14。

Output: 可测试的退避模块与桥接重连行为。
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-browser-connection-session-bridge/07-CONTEXT.md
@.planning/phases/07-browser-connection-session-bridge/07-RESEARCH.md

**Catch-up 语义：** relay `sendTranscriptCatchUp` 发送固定尾窗口（最多 100 条），非 `afterSeq` 增量；客户端必须用 `relaySeq` 去重（RESEARCH 已验证）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconnect-schedule 与单元测试</name>
  <read_first>
    - /Users/ruihao/Documents/github/agent-talkie/.planning/phases/07-browser-connection-session-bridge/07-CONTEXT.md
    - /Users/ruihao/Documents/github/agent-talkie/packages/dashboard/src/bridge/browser-session-bridge.ts
  </read_first>
  <files>packages/dashboard/src/bridge/reconnect-schedule.ts,packages/dashboard/src/bridge/reconnect-schedule.test.ts</files>
  <action>
    1. 新建 `reconnect-schedule.ts`，导出纯函数 `nextReconnectDelayMs(attemptIndex: number): number`，语义：`base = min(30000, 1000 * 2 ** attemptIndex)`（`attemptIndex` 从 0 开始），`return base + floor(random * 301)` 即抖动区间为 **0–300 ms**（含 300）；使用 `Math` 标准函数。
    2. `reconnect-schedule.test.ts`：`vi.spyOn(Math, "random").mockReturnValue(0)` 时 `nextReconnectDelayMs(0) === 1000`、`nextReconnectDelayMs(1) === 2000`、`nextReconnectDelayMs(5) === 30000`（因 32000 被 cap）；恢复 `random` mock；另测 `attemptIndex === 0` 且 `random` 返回 0.999 时结果 `=== 1300`。
  </action>
  <acceptance_criteria>
    - `packages/dashboard/src/bridge/reconnect-schedule.ts` 含子串 `30000` 与 `1000` 与 `301`
    - `packages/dashboard/src/bridge/reconnect-schedule.test.ts` 含子串 `nextReconnectDelayMs` 与 `1300`
    - 命令 `cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/dashboard -- reconnect-schedule` 退出码为 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/dashboard -- reconnect-schedule</automated>
  </verify>
  <done>退避函数与测试稳定通过</done>
</task>

<task type="auto">
  <name>Task 2: 桥接重连、credential 回退与 catch-up 去重</name>
  <read_first>
    - /Users/ruihao/Documents/github/agent-talkie/packages/dashboard/src/bridge/browser-session-bridge.ts
    - /Users/ruihao/Documents/github/agent-talkie/packages/dashboard/src/bridge/session-storage-keys.ts
    - /Users/ruihao/Documents/github/agent-talkie/packages/dashboard/src/bridge/relay-generation.ts
    - /Users/ruihao/Documents/github/agent-talkie/packages/dashboard/src/bridge/reconnect-schedule.ts
    - /Users/ruihao/Documents/github/agent-talkie/packages/client/src/session-client.ts
  </read_first>
  <files>packages/dashboard/src/bridge/browser-session-bridge.ts,packages/dashboard/src/bridge/transcript-catchup-dedupe.test.ts,packages/dashboard/src/demo/main.ts</files>
  <action>
    1. `browser-session-bridge.ts`：增加 `onTranscriptCatchup(cb: (row: { spaceId: string; relaySeq: number; envelope: unknown }) => void): () => void`；在处理通过 `transcriptCatchupMessageSchema` 校验的帧时：设 `prev = maxRelaySeq`，仅当 `relaySeq > prev` 时更新内部 `maxRelaySeq` 并调用 `cb`。
    2. 重连：`connectWithAutoReconnect()` 作为对外的推荐入口（或给 `connect()` 增加选项 `{ autoReconnect?: boolean }` 默认 `false` 以保持测试兼容 — 选择一种并在 `demo/main.ts` 使用 `true`）；当 `autoReconnect` 生效时：WebSocket `close` / `error` 在已完成至少一次 `joinSpace` 成功之后且非用户显式 `close()` 时，将健康设为 `reconnecting`，若 `sessionStorage.getItem(RELAY_GENERATION_KEY)` 非 null 且非空字符串，则 `await probeRelayGenerationHealth(deriveHttpOriginFromWsUrl(this.url), stored)` 为 false 时调用 `notifyRelayGenerationStale()`、将健康设为 `disconnected` 并**不**调度重连；否则用 `setTimeout` 按 `nextReconnectDelayMs(attemptIndex++)` 调度下一次 `internalReconnect()`（指数 attempt 每次失败或关闭后递增，成功 `joinSpace` 后置 0）。
    3. `internalReconnect()`：`connect()` 握手后：若 `sessionStorage` 中存在 `SESSION_ID_KEY` 与 `RECONNECT_SECRET_KEY` 则发送 `session.resume`（复用与 `resumeFromStorage()` 相同的发送/解析路径）；若 resume Promise reject 或收到针对 resume 的 `protocol.error`（与现有 pending 逻辑一致），则 `sessionStorage.removeItem(SESSION_ID_KEY)`、`sessionStorage.removeItem(RECONNECT_SECRET_KEY)` 并调用 `registerNewSession({ displayName: "Human", runtime: "browser", workspaceLabel: "dashboard" })`（字面量与 `demo/main.ts` 一致）；随后 `joinSpace({ slug: "dashboard", idempotencyKey: crypto.randomUUID() })`（slug 必须与 `demo/main.ts` 中 `DEMO_SPACE_SLUG` 的值 `"dashboard"` 保持一字不差一致）。
    4. `close()`：设内部 `_userRequestedClose = true`，清除重连 timer，关闭 socket，健康 `disconnected`，不触发重连。
    5. `transcript-catchup-dedupe.test.ts`：Mock WebSocket 驱动两条 `transcript.catchup` JSON 字符串，第二条与第一条 **相同** `relaySeq`；注册 `onTranscriptCatchup` 计数器；断言最终计数为 `1` 且 `getMaxRelaySeq()` 等于该 `relaySeq`。
    6. 更新 `demo/main.ts`：使用 `autoReconnect`/新 API；在 `window.addEventListener("beforeunload", () => bridge.close())` 可选（非必须）；保持 `joinSpace` slug 与 Task 2 常量一致。
    7. 检查并更新 Plan 01 中 `browser-session-bridge.test.ts` 的测试用例：如果 Plan 01 的测试假设"每条 `transcript.catchup` 都提升 `maxRelaySeq`"，则修改为与去重语义一致——仅 `relaySeq > maxRelaySeq` 时才提升。确保修改后 `npm run test -w @agent-talkie/dashboard` 仍通过。
  </action>
  <acceptance_criteria>
    - `packages/dashboard/src/bridge/browser-session-bridge.ts` 含子串 `reconnecting` 与 `onTranscriptCatchup` 与 `nextReconnectDelayMs`
    - `packages/dashboard/src/bridge/browser-session-bridge.ts` 含子串 `removeItem(SESSION_ID_KEY)` 或 `sessionStorage.removeItem` 与 `SESSION_ID_KEY` 在同一逻辑块邻近 5 行内出现 `RECONNECT_SECRET_KEY`
    - `packages/dashboard/src/bridge/transcript-catchup-dedupe.test.ts` 含子串 `transcript.catchup` 与 `toBe(1)`
    - 命令 `cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/dashboard` 退出码为 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/dashboard</automated>
  </verify>
  <done>全包测试通过；demo 启用自动重连</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 重连循环 | 仅重连到用户配置的 `ws://127.0.0.1:18765`；不跟随不可信重定向 URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | D | 无限重连 | mitigate | generation 探测失败时停止重连并提示刷新（D-12）；用户 `close()` 停止 |
| T-07-03-02 | E | catch-up envelope | mitigate | 继续仅作数据解析；`onTranscriptCatchup` 消费者不得 `eval`；Phase 9 渲染再硬化 |
| T-07-03-03 | T | `sessionStorage` 清除 | accept | 同源脚本模型；resume 失败清凭证避免僵尸会话 |
</threat_model>

<verification>
- `npm run test -w @agent-talkie/dashboard` 通过
- `npm run build -w @agent-talkie/dashboard` 通过
</verification>

<success_criteria>
- 断线重连、退避与 generation 门禁行为可测
- catch-up 重叠尾窗下不重复计数/不抬高错误 seq
</success_criteria>

<output>
完成后创建 `.planning/phases/07-browser-connection-session-bridge/07-03-SUMMARY.md`
</output>
