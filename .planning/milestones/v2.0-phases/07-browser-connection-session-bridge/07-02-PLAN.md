---
phase: 07-browser-connection-session-bridge
plan: 2
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - packages/dashboard/package.json
  - packages/dashboard/src/bridge/browser-session-bridge.ts
  - packages/dashboard/src/bridge/relay-generation.ts
  - packages/dashboard/src/bridge/relay-generation.test.ts
  - packages/dashboard/src/shell/connection-shell.ts
  - packages/dashboard/src/shell/connection-shell.test.ts
  - packages/dashboard/src/demo/main.ts
  - packages/dashboard/index.html
  - packages/dashboard/src/index.ts
autonomous: true
requirements:
  - CONN-01
user_setup: []
must_haves:
  truths:
    - 连接壳层展示四态文案 Connected / Connecting / Reconnecting / Disconnected 与颜色点（绿/琥珀/红映射见 action）
    - 协议版本握手失败（`handshake.nack`）或 envelope 版本错误导致不可恢复时，显示不可关闭的刷新提示条，文案包含 `Please refresh the page`
    - 在 Vite 开发服务器上可通过 `GET /__agent-talkie/v1/health?generation=`（经 proxy）校验 relay generation；403 时显示同上的刷新提示条
  artifacts:
    - path: packages/dashboard/src/shell/connection-shell.ts
      provides: Lit 连接壳 + 状态点 + 横幅
      contains: talkie-connection-shell
    - path: packages/dashboard/src/bridge/relay-generation.ts
      provides: generation 读取与 HTTP 探测
      contains: probeRelayGenerationHealth
  key_links:
    - from: packages/dashboard/src/demo/main.ts
      to: packages/dashboard/src/bridge/browser-session-bridge.ts
      via: 实例化桥并订阅 onConnectionHealthChange
      pattern: onConnectionHealthChange
---

<objective>
在 `BrowserSessionBridge` 上增加可订阅的连接健康状态与协议/代际失败信号；实现 Lit `talkie-connection-shell`（色点 + 标签 + 非可关闭刷新横幅）；提供 Vite `index.html` + `demo/main.ts` 将桥与壳层联调；`relay-generation` 模块从 URL `?generation=` 或 `import.meta.env.VITE_RELAY_GENERATION` 读取 token 并调用 `GET /__agent-talkie/v1/health?generation=…`（依赖 Plan 1 的 Vite proxy）。

Purpose: 完成 CONN-01 的「实时健康指示」与 D-05–D-08 的可视化。

Output: 可本地 `npm run dev -w @agent-talkie/dashboard` 预览的连接壳与 generation 探测逻辑。
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-browser-connection-session-bridge/07-CONTEXT.md
@.planning/phases/07-browser-connection-session-bridge/07-RESEARCH.md

Relay health 行为：`packages/relay/src/server.ts` 中 `GET /__agent-talkie/v1/health`，查询参数 `generation` 等于 `relayGenerationToken` 时返回 HTTP 200 与 body `{"ok":true,"generation":"<token>"}`，否则 403 与 `{"ok":false}`。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 桥接健康状态、generation 探测与 Lit connection-shell</name>
  <read_first>
    - /Users/ruihao/Documents/github/agent-talkie/packages/dashboard/src/bridge/browser-session-bridge.ts
    - /Users/ruihao/Documents/github/agent-talkie/packages/dashboard/src/bridge/session-storage-keys.ts
    - /Users/ruihao/Documents/github/agent-talkie/packages/dashboard/src/bridge/derive-http-origin.ts
    - /Users/ruihao/Documents/github/agent-talkie/packages/dashboard/vite.config.ts
    - /Users/ruihao/Documents/github/agent-talkie/.planning/phases/07-browser-connection-session-bridge/07-CONTEXT.md
  </read_first>
  <files>packages/dashboard/package.json,packages/dashboard/src/bridge/browser-session-bridge.ts,packages/dashboard/src/bridge/relay-generation.ts,packages/dashboard/src/shell/connection-shell.ts,packages/dashboard/index.html,packages/dashboard/src/demo/main.ts,packages/dashboard/src/index.ts</files>
  <action>
    1. `packages/dashboard/package.json` 的 `devDependencies` 增加 `"happy-dom": "^20.0.0"`（或执行时 `npm view happy-dom version` 取当前主版本写入精确号）。
    2. `relay-generation.ts`：导出 `readBootstrapRelayGeneration(): string | null` — 顺序为：若 `typeof window !== "undefined"` 则取 `new URLSearchParams(window.location.search).get("generation")`，否则 null；再否则取 `import.meta.env.VITE_RELAY_GENERATION`（Vite 注入为 string | undefined）；空串视为 null；导出 `async function probeRelayGenerationHealth(httpOrigin: string, generation: string): Promise&lt;boolean&gt;` — `fetch(\`${httpOrigin}/__agent-talkie/v1/health?generation=${encodeURIComponent(generation)}\`)` 返回 `response.ok === true` 时为 true；导出 `persistRelayGenerationIfMissing(gen: string): void`，当 `sessionStorage.getItem(RELAY_GENERATION_KEY)` 为空且 `gen.length &gt; 0` 时 `setItem(RELAY_GENERATION_KEY, gen)`（`RELAY_GENERATION_KEY` 从 `session-storage-keys.ts` 导入）。
    3. `browser-session-bridge.ts`：定义并导出字面量联合类型 `ConnectionHealthUiState = "connected" | "connecting" | "reconnecting" | "disconnected"`；增加私有字段 `_health: ConnectionHealthUiState` 初始 `"disconnected"`；在 `connect()` 入口设 `"connecting"` 并通知监听者；WebSocket `open` 后仍为 `connecting` 直至 `joinSpace` Promise resolve 后设 `"connected"`；`close()` 或 socket `close` 事件将状态设为 `"disconnected"` 并通知（Plan 3 将把关闭重连路径改为 `reconnecting`）；增加 `getConnectionHealth(): ConnectionHealthUiState` 与 `onConnectionHealthChange(cb: (s: ConnectionHealthUiState) =&gt; void): () =&gt; void`；握手收到 `relayHandshakeNackSchema` 成功解析时设置内部 `_staleReason: "protocol_version"` 并调用 `onStaleUiChange`；若解析为 `protocol.error` 且 `error` 字段精确等于字符串 `envelope_version_mismatch`（与 `packages/relay/src/server.ts` 一致）亦设 `_staleReason: "protocol_version"`；导出 `getStaleUiReason(): null | "protocol_version" | "relay_generation"` 与 `onStaleUiChange(cb: () =&gt; void): () =&gt; void`；`relay_generation` 由 demo 在探测失败时调用桥的 `notifyRelayGenerationStale(): void` 设置（公开方法，清理由 `clearStaleUi(): void`）。
    4. `connection-shell.ts`：`@customElement("talkie-connection-shell")`，`@property({ type: String }) healthState: ConnectionHealthUiState = "disconnected"`；`@property({ type: Boolean, reflect: true }) showRefreshBanner = false`；`@property({ type: String }) refreshBannerText = "Please refresh the page to reconnect to the relay."`；渲染：左侧圆点 `span`，`background-color` 映射 — `connected`=`#16a34a`，`connecting`=`#ca8a04`，`reconnecting`=`#ca8a04`，`disconnected`=`#dc2626`；右侧标签英文 — `connected`→`Connected`，`connecting`→`Connecting`，`reconnecting`→`Reconnecting`，`disconnected`→`Disconnected`；`showRefreshBanner` 为 true 时在下方渲染 `div`（非 `dialog`），含 `refreshBannerText`，CSS `position: fixed; top: 0; left: 0; right: 0; padding: 12px; background: #fef3c7; color: #78350f; font-weight: 600; z-index: 9999;`（值须原样出现在源码中以便 grep）。
    5. `index.html`：根挂载 `&lt;div id="app"&gt;&lt;/div&gt;`；`&lt;script type="module" src="/src/demo/main.ts"&gt;&lt;/script&gt;`。
    6. `demo/main.ts`：文件顶部定义 `const DEMO_SPACE_SLUG = "dashboard"`；`import "@agent-talkie/dashboard/shell/connection-shell"` 或相对路径 `../shell/connection-shell.js"`；`wsUrl` 默认 `ws://127.0.0.1:18765`；`const bridge = new BrowserSessionBridge({ url: wsUrl })`（若构造函数尚无 options，则增加可选参数 `{ url?: string }` 默认 `ws://127.0.0.1:18765`）；`const shell = document.createElement("talkie-connection-shell"); document.getElementById("app")!.append(shell)`；`bridge.onConnectionHealthChange((s) => { shell.healthState = s; })`；在 `connect()` 前：`const gen = readBootstrapRelayGeneration()`；若 `gen` 非 null：`sessionStorage.setItem(RELAY_GENERATION_KEY, gen)`（覆写策略：bootstrap 非空总是 `setItem`）；`const httpOrigin = deriveHttpOriginFromWsUrl(wsUrl)`；若 `gen` 非 null 且 `await probeRelayGenerationHealth(httpOrigin, gen) === false` 则 `shell.showRefreshBanner = true` 并 `bridge.notifyRelayGenerationStale()`，不调用 `connect()`；否则 `await bridge.connect()`，再 `resume` 或 `register`（demo 可固定 `registerNewSession({ displayName: "Human", runtime: "browser", workspaceLabel: "dashboard" })`），再 `joinSpace({ slug: DEMO_SPACE_SLUG, idempotencyKey: crypto.randomUUID() })` — 若 join 失败捕获并 `shell.healthState = "disconnected"`；成功则 `persistRelayGenerationIfMissing(gen ?? sessionStorage.getItem(RELAY_GENERATION_KEY) ?? "")` 跳过空串。
    7. `src/index.ts`：导出 `ConnectionHealthUiState`、`talkie-connection-shell` 侧效应注册（import 再 export 模块路径 `./shell/connection-shell.js`）、`readBootstrapRelayGeneration`、`probeRelayGenerationHealth`、`deriveHttpOriginFromWsUrl`、`RELAY_GENERATION_KEY`。
  </action>
  <acceptance_criteria>
    - `packages/dashboard/src/shell/connection-shell.ts` 含自定义元素名 `talkie-connection-shell` 与颜色十六进制 `#16a34a` 与 `#dc2626`
    - `packages/dashboard/src/shell/connection-shell.ts` 含精确子串 `Please refresh the page to reconnect to the relay.`
    - `packages/dashboard/src/bridge/relay-generation.ts` 含子串 `/__agent-talkie/v1/health` 与 `encodeURIComponent`
    - `packages/dashboard/src/bridge/browser-session-bridge.ts` 含子串 `onConnectionHealthChange` 与 `notifyRelayGenerationStale`
    - `packages/dashboard/index.html` 含 `src/demo/main.ts`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/dashboard && npm run build -w @agent-talkie/dashboard</automated>
  </verify>
  <done>壳层与 demo 可编译；测试通过（Task 2 补齐 Vitest 后一并绿）</done>
</task>

<task type="auto">
  <name>Task 2: Vitest — generation 探测与 connection-shell 渲染</name>
  <read_first>
    - /Users/ruihao/Documents/github/agent-talkie/packages/dashboard/src/bridge/relay-generation.ts
    - /Users/ruihao/Documents/github/agent-talkie/packages/dashboard/src/shell/connection-shell.ts
    - /Users/ruihao/Documents/github/agent-talkie/packages/dashboard/vitest.config.ts
  </read_first>
  <files>packages/dashboard/src/bridge/relay-generation.test.ts,packages/dashboard/src/shell/connection-shell.test.ts,packages/dashboard/vitest.config.ts</files>
  <action>
    1. 在 `relay-generation.test.ts` 中 mock `globalThis.fetch`：`probeRelayGenerationHealth("http://127.0.0.1:18765","abc")` 在 mock 返回 `Response` status 200 时结果为 true，status 403 时为 false；`readBootstrapRelayGeneration` 在 `vi.stubGlobal("window", { location: { search: "?generation=xyz" } })` 时返回 `"xyz"`（测试后恢复）。
    2. 在 `connection-shell.test.ts` 文件首行加 `// @vitest-environment happy-dom`；`import "./connection-shell.js"`；`const el = document.createElement("talkie-connection-shell")`；`(el as HTMLElement & { healthState: string }).healthState = "connected"`；`document.body.appendChild(el)`；`await Promise.resolve()`；断言 `el.shadowRoot?.textContent` 包含子串 `Connected`。
    3. 若 `vitest.config.ts` 需为 happy-dom 测试设置 `poolOptions` 等，仅做最小改动使上述测试在 `npm run test -w @agent-talkie/dashboard` 下通过。
  </action>
  <acceptance_criteria>
    - `packages/dashboard/src/bridge/relay-generation.test.ts` 含子串 `probeRelayGenerationHealth` 与 `403`
    - `packages/dashboard/src/shell/connection-shell.test.ts` 含 `@vitest-environment happy-dom` 与子串 `Connected`
    - 命令 `cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/dashboard` 退出码为 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/dashboard</automated>
  </verify>
  <done>新增测试文件存在且 CI 本地命令通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetch → relay HTTP | 仅传递 generation token；响应体按 JSON 读取，不执行 |
| 浏览器 UI | 横幅为静态文案，不信任 relay 返回 HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | S | `probeRelayGenerationHealth` | mitigate | 仅 GET health；不把 token 写入 URL 查询以外的日志；generation 存 sessionStorage（同源策略） |
| T-07-02-02 | T | `fetch` 响应处理 | mitigate | 不把 `response.text()` 注入 `innerHTML`；仅 boolean / 固定 UI |
| T-07-02-03 | I | generation 查询参数 | accept | localhost 信任模型；未来 RSEC 覆盖远程场景 |
</threat_model>

<verification>
- `npm run test -w @agent-talkie/dashboard` 通过
- `npm run build -w @agent-talkie/dashboard` 通过
</verification>

<success_criteria>
- 用户可从壳层读取四态与刷新横幅（D-05–D-08）
- health HTTP 探测与 Vite proxy 路径一致
</success_criteria>

<output>
完成后创建 `.planning/phases/07-browser-connection-session-bridge/07-02-SUMMARY.md`
</output>
