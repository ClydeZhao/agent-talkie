---
phase: 09-core-oversight-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/persistence/src/repositories/oversight.ts
  - packages/persistence/src/repositories/oversight.test.ts
  - packages/relay/src/server.ts
  - packages/dashboard/src/store/dashboard-store.ts
  - packages/dashboard/src/roster/talkie-roster.ts
  - packages/dashboard/src/roster/talkie-roster-entry.ts
  - packages/dashboard/src/demo/main.ts
  - packages/dashboard/index.html
  - packages/dashboard/src/theme/theme.css
autonomous: true
requirements:
  - OVER-01
user_setup: []
must_haves:
  truths:
    - "GET /__agent-talkie/v1/oversight/space-summary?slug= 对当前 space 返回 200 JSON 或未知 slug 时 404"
    - "OversightMember JSON 含 sessionId、displayName、isHuman、role、focus、progress、blockedReason、runtime、workspaceLabel"
    - "仪表盘 join 成功后拉取快照并每 10s 刷新名册（弥补 relay 不向其他连接广播 space.joined）"
    - "左栏宽约 280px + talkie-roster / talkie-roster-entry 展示 displayName、runtime 徽章、workspace、人类/代理图标与 orchestrator 标记"
  artifacts:
    - path: packages/relay/src/server.ts
      provides: "只读 oversight HTTP 与 persistence 汇总"
    - path: packages/dashboard/src/store/dashboard-store.ts
      provides: "名册 Map + HTTP 填充 + 10s 定时器"
    - path: packages/dashboard/src/roster/talkie-roster.ts
      provides: "名册根组件"
  key_links:
    - from: packages/dashboard/src/demo/main.ts
      to: "/__agent-talkie/v1/oversight/space-summary"
      via: "fetch after joinSpace + setInterval 10000"
---

<objective>
交付 OVER-01：与 CLI oversight 同源的只读 HTTP 名册快照（含 runtime/workspace），以及符合 D-01/D-02/D-04–D-07 的左栏名册与集中 store 雏形（transcript/errors 占位数组供后续 plan 扩展）。

Purpose: 落实 **D-06** / **D-23**：名册在 `joinSpace` 成功后用 `GET /__agent-talkie/v1/oversight/space-summary?slug=` 初始化，并以约 **10s** 轮询刷新；行间元数据由后续 plan 的 `metadata.patch`（`onEnvelope`）在轮询间隔内增量更新。不采用「仅靠 WS catch-up 重建名册」——与 09-RESEARCH 结论及 D-06 说明一致（`space.joined`/`space.left` 不向其他连接广播、亦不写入 transcript）。

Output: relay HTTP 路由、扩展后的 OversightSpaceSummary、dashboard-store、talkie-roster*、全局 theme.css 与两栏布局骨架。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-core-oversight-ui/09-CONTEXT.md
@.planning/phases/09-core-oversight-ui/09-RESEARCH.md

**名册数据流（对齐 D-06、D-23）：** HTTP `space-summary` 快照填充 `DashboardStore.roster`，`setInterval(10000)` 周期性重新拉取同一端点；**不**依赖 `onEnvelope` 的 `space.joined` / `space.left` 来维护名册完整列表。

<interfaces>
自 `packages/persistence/src/repositories/oversight.ts`（扩展后）对外 JSON 字段名须与类型一致：`OversightSpaceSummary` 含 `spaceId`, `slug`, `ownerSessionId`, `orchestratorSessionId`, `memberCount`, `members`；每个 `OversightMember` 须含 `sessionId`, `displayName`, `isHuman`, `role`, `focus`, `progress`, `blockedReason`, **`runtime`**, **`workspaceLabel`**（camelCase，来自 `sessions.runtime` / `sessions.workspace_label`）。

HTTP：`GET /__agent-talkie/v1/oversight/space-summary?slug=<slug>`，成功 `200` + `Content-Type: application/json`；未知 slug `404` + `application/json` 体 `{"ok":false,"error":"space_not_found"}`（该 JSON 字符串须出现在实现中供 grep）。

`BrowserSessionBridge.joinSpace` 成功后 demo 已知 `slug`（当前 `DEMO_SPACE_SLUG = "dashboard"`），`httpOrigin` 使用已有 `deriveHttpOriginFromWsUrl(wsUrl)`。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 OversightMember 与 SQL</name>
  <files>packages/persistence/src/repositories/oversight.ts, packages/persistence/src/repositories/oversight.test.ts</files>
  <read_first>
    - packages/persistence/src/repositories/oversight.ts
    - packages/persistence/src/repositories/oversight.test.ts
    - packages/persistence/src/repositories/sessions.ts
  </read_first>
  <action>
    在 `OversightMember` 类型中新增两个必填字段：`runtime: string`、`workspaceLabel: string`。

    在 `getOversightSpaceSummaryBySlug` 的 `memberRows` 查询中，从 `sessions`（别名 `sess`）选出 `sess.runtime AS runtime`、`sess.workspace_label AS workspace_label`，并在 `members` 映射中为每项设置 `runtime: r.runtime`、`workspaceLabel: r.workspace_label`（与 `getSessionById` 风格一致，camelCase）。

    更新 `oversight.test.ts` 中断言：每个 member 对象包含 `runtime` 与 `workspaceLabel` 键，且与插入 session 时使用的值一致。
  </action>
  <verify>
    <automated>npm test -w @agent-talkie/persistence -- --run packages/persistence/src/repositories/oversight.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/persistence/src/repositories/oversight.ts` 含字面量 `sess.runtime AS runtime`
    - 同文件含字面量 `workspace_label AS workspace_label`
    - `export type OversightMember` 块内同一文件出现 `runtime:` 与 `workspaceLabel:`
    - `npm test -w @agent-talkie/persistence -- --run packages/persistence/src/repositories/oversight.test.ts` 退出码 0
  </acceptance_criteria>
  <done>扩展后的汇总查询在无迁移变更下通过 persistence 单测。</done>
</task>

<task type="auto">
  <name>Task 2: Relay 只读 GET space-summary</name>
  <files>packages/relay/src/server.ts</files>
  <read_first>
    - packages/relay/src/server.ts
    - packages/persistence/src/repositories/oversight.ts
  </read_first>
  <action>
    在 `createRelayServer` 的 `server.on("request", …)` 中、dashboard `sirv` 分支之前，增加：

    - 条件：`req.method === "GET"` 且 `url.pathname === "/__agent-talkie/v1/oversight/space-summary"`
    - 读取 query：`const slug = url.searchParams.get("slug")`；若 `!slug` 则 `400` + JSON `{"ok":false,"error":"missing_slug"}`
    - 调用 `getOversightSpaceSummaryBySlug(db, slug)`；若 `undefined`，`404` + JSON `{"ok":false,"error":"space_not_found"}`
    - 否则 `200`，`Content-Type: application/json`，`res.end(JSON.stringify(summary))`，其中 `summary` 为函数返回值（字段名保持 camelCase，与类型一致）

    不新增鉴权头（v2.0 localhost；监听仍为 `LISTEN_HOST`）。
  </action>
  <verify>
    <automated>npm test -w @agent-talkie/relay -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `packages/relay/src/server.ts` 含字面量路径 `"/__agent-talkie/v1/oversight/space-summary"`
    - 同文件含 `getOversightSpaceSummaryBySlug`
    - 同文件含字面量 `"space_not_found"`
    - `npm test -w @agent-talkie/relay` 退出码 0
  </acceptance_criteria>
  <done>Relay 对有效 slug 返回 200 JSON 汇总；未知 slug 返回 404 与约定 error 字段。</done>
</task>

<task type="auto">
  <name>Task 3: Store、名册组件、布局与主题</name>
  <files>packages/dashboard/src/store/dashboard-store.ts, packages/dashboard/src/roster/talkie-roster.ts, packages/dashboard/src/roster/talkie-roster-entry.ts, packages/dashboard/src/demo/main.ts, packages/dashboard/index.html, packages/dashboard/src/theme/theme.css</files>
  <read_first>
    - packages/dashboard/src/demo/main.ts
    - packages/dashboard/src/shell/connection-shell.ts
    - packages/dashboard/src/bridge/browser-session-bridge.ts
    - packages/dashboard/src/bridge/derive-http-origin.ts
    - packages/dashboard/index.html
    - .planning/phases/09-core-oversight-ui/09-CONTEXT.md
  </read_first>
  <action>
    1. 新建 `packages/dashboard/src/theme/theme.css`：在 `:root` 上定义暗色 CSS 变量，至少包含 `--talkie-bg`, `--talkie-fg`, `--talkie-muted`, `--talkie-border`, `--talkie-accent-danger`（具体色值自选，暗底浅字）。在 `packages/dashboard/index.html` 的 `&lt;head&gt;` 内增加 `&lt;link rel="stylesheet" href="/src/theme/theme.css" /&gt;`。

    2. 新建 `packages/dashboard/src/store/dashboard-store.ts`：导出类 `DashboardStore`，构造时无参；内部 `roster: Map&lt;string, RosterRow&gt;`（`RosterRow` 含 `sessionId`, `displayName`, `isHuman`, `runtime`, `workspaceLabel`, `orchestrator`, `owner` 布尔、`role`, `focus`, `progress`, `blockedReason` 字符串字段，与快照及后续 metadata 对齐）；`transcriptRows: unknown[]` 与 `errors: unknown[]` 初始化为空数组占位；方法 `hydrateFromSpaceSummary(summary: OversightSpaceSummary, selfSessionId: string)`：清空 roster 后按 `summary.members` 写入，`orchestrator = sessionId === summary.orchestratorSessionId`，`owner = sessionId === summary.ownerSessionId`；方法 `scheduleSnapshotRefresh(fetchSummary: () => Promise&lt;void&gt;, intervalMs: number)` 使用 `window.setInterval`，**intervalMs 固定传入 `10000`**；`requestUpdateCallback` 或 `addListener(cb: () => void)` 供 Lit 订阅刷新。

    3. 新建 `talkie-roster-entry` / `talkie-roster`（`packages/dashboard/src/roster/`，无 Shoelace）：左栏目标宽度 **280px**（`width: 280px; flex-shrink: 0` 或等价）；每行展示 `displayName`、runtime 文本徽章、`workspaceLabel`；`isHuman === true` 用「人形」含义的 inline SVG，`false` 用「机器人」含义的 inline SVG；若 `orchestrator === true` 在图标角上叠小星形/皇冠 SVG；**不在本 task 实现 D-14–D-17 的 chips 与 blocked 边框**（留给 09-03）。

    4. 修改 `packages/dashboard/src/demo/main.ts`：在 `#app` 内构建布局——顶行：`talkie-connection-shell` 全宽；其下 **flex 行**：左 `talkie-roster`，右主区占位 `&lt;div id="talkie-main-panel"&gt;&lt;/div&gt;`（min-height 便于后续 transcript）；`background`/`color` 使用 `var(--talkie-*)`。

    5. 在 `main.ts` 中在 `registerNewSession` 或 `resumeFromStorage` 成功后把返回的 `sessionId` 保存到局部变量 `selfSessionId`（类型 `string`），在 `joinSpace` resolve 后执行 `fetch(\`${httpOrigin}/__agent-talkie/v1/oversight/space-summary?slug=${encodeURIComponent(DEMO_SPACE_SLUG)}\`)`，`200` 时 `response.json()` 解析为 `OversightSpaceSummary` 后调用 `store.hydrateFromSpaceSummary(summary, selfSessionId)`；并 `store.scheduleSnapshotRefresh(() => 同上 fetch+hydrate, 10000)`。

    6. 将 `talkie-roster` 的 `.entries = Array.from(store.roster.values())` 通过属性或回调绑定；store 更新后 `roster.requestUpdate()`。
  </action>
  <verify>
    <automated>npm run build -w @agent-talkie/dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/index.html` 含 `href="/src/theme/theme.css"`
    - `packages/dashboard/src/theme/theme.css` 含 `--talkie-bg:` 与 `--talkie-fg:`
    - `packages/dashboard/src/store/dashboard-store.ts` 含字面量 `10000`（作为 `setInterval` 间隔参数传递）
    - `packages/dashboard/src/roster/talkie-roster.ts` 含 `280px`
    - `packages/dashboard/src/demo/main.ts` 含 `talkie-main-panel` 与 `/__agent-talkie/v1/oversight/space-summary`
    - `npm run build -w @agent-talkie/dashboard` 退出码 0
  </acceptance_criteria>
  <done>构建通过；名册从 HTTP 初始化并每 10 秒刷新；布局符合 D-01/D-02 骨架。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP GET → relay | slug 查询参数；仅 localhost 监听 |
| JSON → DOM | 名册字段经 Lit 文本插值渲染，禁止 `unsafeHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | `space-summary` query | accept | localhost-only；无跨机暴露（v2.0 范围） |
| T-09-01-02 | Spoofing | 恶意 `displayName` / workspace | mitigate | Lit `html\`\`` 文本绑定，不注入 HTML |
| T-09-01-03 | Information disclosure | 汇总 JSON | accept | 与已有 relay+DB 一致，无新增密钥字段 |
</threat_model>

<verification>
`npm test -w @agent-talkie/persistence -- --run packages/persistence/src/repositories/oversight.test.ts` 与 `npm test -w @agent-talkie/relay` 与 `npm run build -w @agent-talkie/dashboard` 均成功。
</verification>

<success_criteria>
名册 HTTP 与 UI 骨架可演示；OVER-01 的 runtime/workspace/role 字段中除 role 外均在快照 JSON 与 UI 行中可见（role 空串可在 09-03 以 chip 强调）。
</success_criteria>

<output>
完成后创建 `.planning/phases/09-core-oversight-ui/09-01-SUMMARY.md`
</output>
