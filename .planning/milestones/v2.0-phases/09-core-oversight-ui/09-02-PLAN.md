---
phase: 09-core-oversight-ui
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - packages/dashboard/package.json
  - package-lock.json
  - packages/dashboard/src/store/dashboard-store.ts
  - packages/dashboard/src/transcript/talkie-transcript.ts
  - packages/dashboard/src/transcript/talkie-transcript-entry.ts
  - packages/dashboard/src/demo/main.ts
autonomous: true
requirements:
  - OVER-02
user_setup: []
must_haves:
  truths:
    - "依赖 @lit-labs/virtualizer 的虚拟化 transcript 列表"
    - "onTranscriptCatchup 与 onEnvelope 共用同一追加与渲染路径"
    - "仅在用户已滚到底部时新消息自动贴底；否则显示可点击的「↓ N 新消息」提示"
    - "按 kind/type 规则上色：control 暗淡、conversation 前景、type 含子串 error 时危险色"
  artifacts:
    - path: packages/dashboard/src/transcript/talkie-transcript.ts
      provides: "lit-virtualizer + 滚底逻辑"
    - path: packages/dashboard/src/store/dashboard-store.ts
      provides: "transcript 行数组、去重、activeSpaceId"
  key_links:
    - from: packages/dashboard/src/demo/main.ts
      to: "BrowserSessionBridge.onTranscriptCatchup / onEnvelope"
      via: "store.appendTranscriptCatchup / appendTranscriptEnvelope"
---

<objective>
交付 OVER-02：终端日志式 transcript 时间线（D-08–D-13），含 `@lit-labs/virtualizer`、catch-up 与 live 统一管道、贴底与「新消息」提示。

Purpose: 在 Phase 7 序号语义之上提供可扩展的大量历史展示。

Output: virtualizer 依赖、store 的 transcript 归约、`talkie-transcript` / `talkie-transcript-entry`、demo 主区挂载。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-core-oversight-ui/09-CONTEXT.md
@.planning/phases/09-core-oversight-ui/09-RESEARCH.md
@packages/dashboard/src/bridge/browser-session-bridge.ts

<interfaces>
`TranscriptCatchupRow`（dashboard bridge 导出）：`{ spaceId: string; relaySeq: number; envelope: unknown }`。

`Envelope`（`@agent-talkie/protocol`）：含 `id`, `sessionId`, `kind` (`"control"` | `"conversation"`), `type`, `payload`, 可选 `spaceId`。

去重键：catch-up 行使用字符串 `` `${row.spaceId}:${row.relaySeq}` ``；live envelope 使用 `` `${env.spaceId ?? "none"}:${env.id}` ``（同一 `id` 不得重复追加）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 @lit-labs/virtualizer</name>
  <files>packages/dashboard/package.json, package-lock.json</files>
  <read_first>
    - packages/dashboard/package.json
    - package-lock.json
  </read_first>
  <action>
    在工作区根目录执行：`npm install @lit-labs/virtualizer@^2.1.1 -w @agent-talkie/dashboard`

    确保 `packages/dashboard/package.json` 的 `dependencies` 中出现 `"@lit-labs/virtualizer": "^2.1.1"`（或 npm 解析后的兼容范围）。
  </action>
  <verify>
    <automated>npm ls @lit-labs/virtualizer -w @agent-talkie/dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/package.json` 含字符串 `"@lit-labs/virtualizer"`
    - `npm ls @lit-labs/virtualizer -w @agent-talkie/dashboard` 退出码 0 且输出行包含 `@lit-labs/virtualizer@`
  </acceptance_criteria>
  <done>依赖可解析且 lockfile 更新。</done>
</task>

<task type="auto">
  <name>Task 2: Store 扩展 transcript 与去重</name>
  <files>packages/dashboard/src/store/dashboard-store.ts</files>
  <read_first>
    - packages/dashboard/src/store/dashboard-store.ts
    - packages/dashboard/src/bridge/browser-session-bridge.ts
    - packages/protocol/src/envelope.ts
  </read_first>
  <action>
    在 `DashboardStore` 中：

    1. 新增字段 `activeSpaceId: string | null`（初始 `null`）与方法 `setActiveSpaceId(id: string): void`。

    2. 定义 `TranscriptLine` 类型：`{ dedupeKey: string; receivedAtMs: number; envelope: import("@agent-talkie/protocol").Envelope }`（catch-up 路径在追加前对 `row.envelope` 使用 `safeParseEnvelope`，失败则丢弃该行）。

    3. 将占位 `transcriptRows` 改为 `TranscriptLine[]`（或重命名为 `transcriptLines` 并在全文件一致使用）。

    4. 实现 `appendTranscriptCatchup(row: import("../bridge/browser-session-bridge.js").TranscriptCatchupRow): void`：若 `row.spaceId !== this.activeSpaceId` 则 return；`dedupeKey = \`${row.spaceId}:${row.relaySeq}\``；若 Map/Set 已存在该 key 则 return；否则 push 并通知监听。

    5. 实现 `appendTranscriptEnvelope(env: import("@agent-talkie/protocol").Envelope): void`：若 `env.spaceId !== undefined` 且 `env.spaceId !== this.activeSpaceId` 则 return；`dedupeKey = \`${env.spaceId ?? "none"}:${env.id}\``；重复则 return；否则 push。

    6. `receivedAtMs` 一律使用 `Date.now()` 在追加瞬间写入。

    暴露只读 getter 或字段供组件读取排序后的数组（按追加顺序即可）。
  </action>
  <verify>
    <automated>npm run build -w @agent-talkie/dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/src/store/dashboard-store.ts` 含 `appendTranscriptCatchup`
    - 同文件含 `appendTranscriptEnvelope`
    - 同文件含 `setActiveSpaceId`
    - 同文件含字面量 `` `${row.spaceId}:${row.relaySeq}` `` 或等价模板（grep `relaySeq}`）
    - `npm run build -w @agent-talkie/dashboard` 退出码 0
  </acceptance_criteria>
  <done>catch-up 与 live 共用 store 队列且按 space 过滤、按 key 去重。</done>
</task>

<task type="auto">
  <name>Task 3: talkie-transcript 与 demo 接线</name>
  <files>packages/dashboard/src/transcript/talkie-transcript.ts, packages/dashboard/src/transcript/talkie-transcript-entry.ts, packages/dashboard/src/demo/main.ts</files>
  <read_first>
    - packages/dashboard/src/store/dashboard-store.ts
    - packages/dashboard/src/demo/main.ts
    - packages/dashboard/src/shell/connection-shell.ts
    - .planning/phases/09-core-oversight-ui/09-CONTEXT.md
  </read_first>
  <action>
    1. 新建 `packages/dashboard/src/transcript/talkie-transcript-entry.ts`：`TalkieTranscriptEntry extends LitElement`，`@property({ type: Object }) line!: TranscriptLine`；渲染单行文本：`` [${HH:MM:SS}] ${senderLabel} (${env.kind} / ${env.type}): ${preview} ``，其中 `HH:MM:SS` 来自 `new Date(line.receivedAtMs).toLocaleTimeString(undefined, { hour12: false })` 取 8 字符时间片段或等价；`senderLabel` 为 `store` 中 roster 的 `displayName`，找不到则用 `env.sessionId` 前 8 字符 + `…`；`preview` 为 `JSON.stringify(env.payload)` 截断至 **240** 字符（超出追加 `…`）。**禁止** `unsafeHTML`。

    2. 样式：`env.kind === "control"` 使用 `color: var(--talkie-muted)`；`env.kind === "conversation"` 使用 `color: var(--talkie-fg)`；若 `env.type.includes("error")` 为真则整行 `color: var(--talkie-accent-danger)`（覆盖前两规则）。

    3. 新建 `talkie-transcript.ts`：内部使用 `@lit-labs/virtualizer` 的 `LitVirtualizer`（从包文档的 Lit 3 用法：`import { LitVirtualizer } from "@lit-labs/virtualizer/lit-virtualizer.js"` 或当前版本推荐入口，以能编译为准）。`items` 绑定 `store.transcriptLines`；`renderItem` 返回 `html\`<talkie-transcript-entry .line=${item}></talkie-transcript-entry>\``（需 `import "./talkie-transcript-entry.js"`）。

    4. **贴底逻辑（D-10）**：在 transcript 容器上监听 scroll；定义阈值 `PIN_THRESHOLD_PX = 48`；当 `scrollHeight - scrollTop - clientHeight <= PIN_THRESHOLD_PX` 视为在底部，`isPinnedToBottom = true`。每当追加行：若 `isPinnedToBottom`，在 `requestAnimationFrame` 或 `updated` 回调中将滚动条置底；若否，递增内部计数 `pendingNew` 并在 UI 显示字面按钮文案 **`↓ `** 拼接 `pendingNew` 再拼接 **` 新消息`**（中文固定）。点击按钮后滚底并清零 `pendingNew`。

    5. 修改 `packages/dashboard/src/demo/main.ts`：在 `joinSpace` 成功后调用 `store.setActiveSpaceId(返回的 spaceId)`；注册 `bridge.onTranscriptCatchup((row) => store.appendTranscriptCatchup(row))`；注册 `bridge.onEnvelope((env) => store.appendTranscriptEnvelope(env))`；将 `talkie-transcript` 挂到 `#talkie-main-panel` 内并传入 store（property `store` 或回调）。

    6. 在 `main.ts` 或 transcript 组件文件顶部 `import "@lit-labs/virtualizer"` 若包要求注册（以构建通过为准）。
  </action>
  <verify>
    <automated>npm run build -w @agent-talkie/dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dashboard/src/transcript/talkie-transcript.ts` 含 `@lit-labs/virtualizer` 或 `LitVirtualizer`
    - 同目录或 entry 文件含 `talkie-transcript-entry`
    - `talkie-transcript` 相关源码含 `PIN_THRESHOLD_PX` 且值为 `48`
    - `talkie-transcript` 或 demo 含字面量 `新消息`（与 `↓` 同按钮语义）
    - `packages/dashboard/src/demo/main.ts` 含 `setActiveSpaceId` 与 `onTranscriptCatchup`
    - `npm run build -w @agent-talkie/dashboard` 退出码 0
  </acceptance_criteria>
  <done>主区展示可滚动虚拟列表，行为符合 D-08–D-11、D-12 规则与 D-13 时间格式意图。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| envelope.payload → DOM | 视为不可信，仅文本/截断 JSON 字符串 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Spoofing | 恶意 payload 内嵌 HTML | mitigate | Lit 文本插值 + `JSON.stringify` 预览，禁止 `unsafeHTML` |
| T-09-02-02 | DoS | 超长 payload | mitigate | 预览硬截断 240 字符 |
</threat_model>

<verification>
`npm run build -w @agent-talkie/dashboard` 成功。
</verification>

<success_criteria>
连接 relay 后 catch-up 行与后续 live envelope 出现在同一列表中，滚底与「新消息」行为可手动验证。
</success_criteria>

<output>
完成后创建 `.planning/phases/09-core-oversight-ui/09-02-SUMMARY.md`
</output>
