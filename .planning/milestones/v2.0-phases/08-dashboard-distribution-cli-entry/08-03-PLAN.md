---
phase: 08-dashboard-distribution-cli-entry
plan: 03
type: execute
wave: 3
depends_on: ["08-01"]
files_modified:
  - packages/cli/package.json
  - packages/cli/src/cli.ts
  - packages/cli/src/cli.test.ts
autonomous: true
requirements: [CONN-04]
must_haves:
  truths:
    - "`talkie dashboard` 调用 `ensureRelayRunning({})` 后向 stdout 打印形如 `http://127.0.0.1:<port>/dashboard` 的完整 URL（port 为十进制数字）"
    - "默认用 `open` 包打开该 URL；传入 `--no-open` 时不调用打开浏览器逻辑"
    - "命令非阻塞退出（不等待用户关闭浏览器），relay 仍以 daemon 方式存活（与现有 `relay start` 一致）"
  artifacts:
    - path: packages/cli/src/cli.ts
      provides: "`dashboard` 子命令"
      contains: ".command(\"dashboard\")"
    - path: packages/cli/package.json
      provides: "open@^11.0.0 依赖"
      contains: '"open"'
  key_links:
    - from: packages/cli/src/cli.ts
      to: packages/supervisor/src/ensure-relay.ts
      via: "ensureRelayRunning({}) 返回 port"
---

<objective>
新增 `talkie dashboard`：确保 relay 运行（D-05）、打印 dashboard URL（D-04）、默认打开系统浏览器（D-04）、立即退出（D-06）；可选 `--no-open`（RESEARCH 建议，执行体裁量）。

Purpose: 满足 CONN-04 与 CONTEXT D-04～D-06。
Output: CLI 命令、依赖、`cli.test.ts` 回归。
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dashboard-distribution-cli-entry/08-CONTEXT.md

<interfaces>
自 `packages/cli/src/cli.ts` 中 `ping` 范式：

```typescript
const { port, generation } = await ensureRelayRunning({});
const url = `http://127.0.0.1:${port}/__agent-talkie/v1/health?generation=${encodeURIComponent(generation)}`;
```

自 `packages/relay/src/server.ts`：`export const LISTEN_HOST = "127.0.0.1";` — dashboard URL 主机必须使用该字面量（与 `ping` 一致），路径为 `/dashboard`（无尾部斜杠亦可，打印统一为 `http://127.0.0.1:${port}/dashboard`）。

自 `packages/supervisor/src/ensure-relay.ts`：

```typescript
export async function ensureRelayRunning(opts?: EnsureRelayOptions): Promise<{
  port: number;
  generation: string;
  pid: number;
  spawned: boolean;
}>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: open 依赖与 dashboard 子命令</name>
  <read_first>
    - packages/cli/src/cli.ts
    - packages/cli/package.json
    - packages/supervisor/src/ensure-relay.ts
  </read_first>
  <files>packages/cli/package.json, packages/cli/src/cli.ts</files>
  <action>
    1. 在 `packages/cli/package.json` 的 `dependencies` 增加 `"open": "^11.0.0"`。
    2. 在 `packages/cli/src/cli.ts` 顶部增加 `import open from "open";`（或该包 ESM 默认导出等价写法，以类型检查为准）。
    3. 在 `program` 链上、`ping` 命令附近增加：
       ```typescript
       program
         .command("dashboard")
         .description("Ensure relay is running and open the web dashboard")
         .option("--no-open", "Print URL only; do not open a browser")
         .action(async (opts: { noOpen?: boolean }) => {
           try {
             const { port } = await ensureRelayRunning({});
             const url = `http://127.0.0.1:${port}/dashboard`;
             console.log(url);
             if (!opts.noOpen) {
               await open(url);
             }
           } catch (e) {
             handleError(e);
           }
         });
       ```
       主机字面量必须为 `127.0.0.1`（禁止 `localhost`），路径为 `/dashboard`（满足 D-01）。
  </action>
  <verify>
    <automated>npm run build -w @agent-talkie/cli</automated>
  </verify>
  <acceptance_criteria>
    - `packages/cli/package.json` 含子串 `"open": "^11.0.0"`
    - `packages/cli/src/cli.ts` 含子串 `.command("dashboard")`
    - `packages/cli/src/cli.ts` 含子串 `` `http://127.0.0.1:${port}/dashboard` ``
    - `packages/cli/src/cli.ts` 含子串 `--no-open`
    - `npm run build -w @agent-talkie/cli` 退出码 0
  </acceptance_criteria>
  <done>D-04（打印+打开）、D-05（ensureRelayRunning）、D-06（无长驻）在代码层落实。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI 集成测试 dashboard --no-open</name>
  <read_first>
    - packages/cli/src/cli.test.ts
    - packages/cli/src/cli.ts
  </read_first>
  <files>packages/cli/src/cli.test.ts</files>
  <behavior>
    - 在隔离 `AGENT_TALKIE_DATA_DIR` 与 `AGENT_TALKIE_RELAY_PORT=0` 下，`runCli(["dashboard", "--no-open"], env)` 的 `status === 0`。
    - `stdout.trim()` 匹配正则 `/^http:\/\/127\.0\.0\.1:\d+\/dashboard$/`。
  </behavior>
  <action>
    在 `packages/cli/src/cli.test.ts` 的 `describe("talkie CLI")` 内新增 `it(...)`：复制 `relay start then ping` 测试的环境构造（`mkdtempSync`、`AGENT_TALKIE_RELAY_PORT: "0"`），将 `dataDir` 赋给 `afterEach` 清理路径；调用 `runCli(["dashboard", "--no-open"], env)`；断言 `status` 与 `stdout` 如上正则。
  </action>
  <verify>
    <automated>npm run test -w @agent-talkie/cli</automated>
  </verify>
  <acceptance_criteria>
    - `packages/cli/src/cli.test.ts` 含子串 `dashboard` 且含子串 `--no-open`
    - `packages/cli/src/cli.test.ts` 含子串 `127.0.0.1` 且含子串 `/dashboard`
    - `npm run test -w @agent-talkie/cli` 退出码 0
  </acceptance_criteria>
  <done>CONN-04 具备自动化回归：CLI 在 ephemeral port 下输出正确 URL。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | S | 打开任意 URL | accept | URL 由本机 relay port 与固定 path 拼接，无用户可控 host |
</threat_model>

<verification>
`npm run test -w @agent-talkie/cli` 通过。
</verification>

<success_criteria>
- `talkie dashboard` 与 `talkie dashboard --no-open` 行为可测。
- stdout URL 与 RESEARCH 给出的形式一致。
</success_criteria>

<output>
完成后创建 `.planning/phases/08-dashboard-distribution-cli-entry/08-03-SUMMARY.md`
</output>
