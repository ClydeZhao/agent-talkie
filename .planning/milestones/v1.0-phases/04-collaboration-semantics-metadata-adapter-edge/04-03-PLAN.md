---
phase: 04-collaboration-semantics-metadata-adapter-edge
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - package.json
  - package-lock.json
  - packages/client/package.json
  - packages/client/tsup.config.ts
  - packages/client/src/index.ts
  - packages/client/src/session-client.ts
  - packages/adapter-stdio/package.json
  - packages/adapter-stdio/tsup.config.ts
  - packages/adapter-stdio/src/index.ts
  - packages/adapter-stdio/src/cli.ts
  - packages/adapter-stdio/src/content-length-framing.ts
  - packages/adapter-stdio/src/bounded-queue.ts
  - docs/adapter-ingress.md
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-03
  - ADAPT-04
must_haves:
  truths:
    - "`@agent-talkie/client` connects with the same handshake + session.register + envelope path as the relay test harness"
    - "`@agent-talkie/adapter-stdio` reads/writes Content-Length framed JSON and forwards through the client"
    - "Queue overflow drops oldest, writes structured JSON line to stderr, increments counter — no relay protocol error"
  artifacts:
    - path: packages/client/src/session-client.ts
      provides: TalkieSessionClient class
      contains: registerSession
    - path: docs/adapter-ingress.md
      provides: ADAPT-01 pattern documentation
      contains: ADAPT-03
  key_links:
    - from: packages/adapter-stdio/src/cli.ts
      to: packages/client/src/session-client.ts
      via: import @agent-talkie/client
      pattern: @agent-talkie/client
---

<objective>
Ship the shared WebSocket session client (`@agent-talkie/client`), the stdio adapter (`@agent-talkie/adapter-stdio`) with Content-Length framing and bounded outbound queue, and `docs/adapter-ingress.md` describing the adapter ingress pattern (ADAPT-01) and same-transport rule (ADAPT-03).

Purpose: Give Phase 5 runtimes a canonical library and a reference edge adapter without forking relay transport.
Output: Two new workspace packages, root workspace + test script wiring, documentation file.
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collaboration-semantics-metadata-adapter-edge/04-CONTEXT.md
@packages/supervisor/src/ensure-relay.ts
@packages/protocol/src/relay-wire.ts
@packages/protocol/src/handshake.ts

<interfaces>
Relay URL default: `ws://127.0.0.1:18765` (from `DEFAULT_RELAY_PORT` + `LISTEN_HOST` in `packages/relay/src/server.ts`).

Handshake message shape: `relayClientHandshakeSchema` — `{ type: "handshake", supportedVersions: { minVersion, maxVersion } }` using same numeric bounds as relay (`minVersion: 1`, `maxVersion: 1` until PROTO evolves).

Envelope: `Envelope` from `@agent-talkie/protocol` — use `safeParseEnvelope` before send when building from external JSON.

Stdio framing (D-10): headers end with `\r\n\r\n`; required header line `Content-Length: <n>` where n is decimal bytes of UTF-8 JSON body; no other headers required for v1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package @agent-talkie/client</name>
  <files>packages/client/package.json,packages/client/tsup.config.ts,packages/client/src/index.ts,packages/client/src/session-client.ts,package.json,package-lock.json</files>
  <read_first>
    - packages/relay/src/server.ts
    - packages/protocol/src/relay-wire.ts
    - packages/protocol/src/handshake.ts
    - packages/supervisor/package.json
    - packages/relay/package.json
  </read_first>
  <action>
    1. Create workspace package `packages/client` named `@agent-talkie/client` with `type: module`, `tsup` build matching sibling packages, dependencies: `@agent-talkie/protocol`, `ws`, `zod` (peer or direct per relay), `devDependencies`: `typescript`, `tsup`, `@types/node`, `@types/ws`.

    2. Implement `packages/client/src/session-client.ts` exporting class `TalkieSessionClient` with constructor options `{ url?: string; supportedVersions?: { minVersion: number; maxVersion: number } }` defaulting `url` to `ws://127.0.0.1:18765` and supportedVersions to `{ minVersion: 1, maxVersion: 1 }`.

    3. Methods (exact names): `connect(): Promise<void>` — open WebSocket, send handshake, await `handshake.ack`, throw on nack/close; `registerSession(input: SessionRegisterMessage['newSession'] & { isHuman?: boolean }): Promise<{ sessionId: string; reconnectSecret: string; displayName: string }>` — send `{ type: "session.register", newSession: ... }`, await `session.registered`; `sendEnvelope(envelope: Envelope): void` — `ws.send(JSON.stringify(envelope))` when OPEN; `onEnvelope(handler: (env: Envelope) => void): void` — parse incoming text with `safeParseEnvelope`, ignore invalid; `close(): void`.

    4. Export `TalkieSessionClient` from `packages/client/src/index.ts`.

    5. Add `"packages/client"` to root `package.json` workspaces array (keep alphabetical or existing order).

    6. Extend root `scripts.test` to append `&& npm run test -w @agent-talkie/client` after existing packages.

    7. Extend root `scripts.build` similarly with `npm run build -w @agent-talkie/client`.

    8. Add `packages/client` vitest minimal test: mock `ws` or use `vitest` with manual mock verifying handshake JSON shape contains `"type":"handshake"` and `supportedVersions`.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm install && npm run build -w @agent-talkie/client && npm run test -w @agent-talkie/client</automated>
  </verify>
  <acceptance_criteria>
    - `packages/client/package.json` contains `"name": "@agent-talkie/client"`.
    - `grep -n "class TalkieSessionClient" packages/client/src/session-client.ts` returns at least one line.
    - `grep -n "ws://127.0.0.1:18765" packages/client/src/session-client.ts` returns at least one line.
    - Root `package.json` workspaces array includes the string `@agent-talkie/client` or path `packages/client`.
    - `npm run build -w @agent-talkie/client` exits 0.
    - `npm run test -w @agent-talkie/client` exits 0.
  </acceptance_criteria>
  <done>Publishable internal client package builds and tests.</done>
</task>

<task type="auto">
  <name>Task 2: Package @agent-talkie/adapter-stdio — framing + queue + CLI</name>
  <files>packages/adapter-stdio/package.json,packages/adapter-stdio/tsup.config.ts,packages/adapter-stdio/src/content-length-framing.ts,packages/adapter-stdio/src/bounded-queue.ts,packages/adapter-stdio/src/cli.ts,packages/adapter-stdio/src/index.ts,package.json,package-lock.json</files>
  <read_first>
    - packages/relay/src/server.ts
    - packages/client/src/session-client.ts
    - packages/supervisor/src/ensure-relay.ts
  </read_first>
  <action>
    1. Create `packages/adapter-stdio` named `@agent-talkie/adapter-stdio`, dependencies: `@agent-talkie/client`, `@agent-talkie/protocol`, `@agent-talkie/supervisor`, `ws`, `zod`; devDependencies like other packages; `bin` entry mapping `talkie-stdio-adapter` → `./dist/cli.js` (or `node dist/cli.js` pattern used in repo).

    2. `content-length-framing.ts`: export async generator or class `ContentLengthFrameReader` reading from `process.stdin` with **max body bytes = 262144** (same integer as `MAX_INBOUND_WS_BYTES` in relay). On oversize, emit error to stderr as one JSON line `{"level":"error","event":"stdio_adapter_frame_too_large","maxBytes":262144}` and `process.exit(1)`.

    3. `bounded-queue.ts`: export `createBoundedQueue<T>(maxLen: number)` with `enqueue(item: T, onDrop: (dropped: T) => void): void` implementing drop-oldest when length exceeds `maxLen` before push (D-11).

    4. `cli.ts`: parse env `TALKIE_STDIO_MAX_QUEUE` as positive integer default **100** if unset or invalid. Call `ensureRelayRunning()` from `@agent-talkie/supervisor` before `TalkieSessionClient.connect()` (import path must resolve in workspace). Read framed JSON from stdin; each parsed object must pass `safeParseEnvelope`; on failure stderr JSON `{"level":"warn","event":"stdio_adapter_invalid_envelope"}` and skip. On success, push the `Envelope` into the bounded queue; a drain loop (async) calls `client.sendEnvelope(env)` for each item in FIFO order. On drop-oldest, write stderr JSON exactly `{"level":"warn","event":"stdio_adapter_queue_overflow","dropped":1}` and increment in-memory `droppedCount` (number). Export getter for tests.

    5. `index.ts` re-export framing + queue utilities.

    6. Add workspace + root scripts for build/test same as client.

    7. Vitest: unit test `ContentLengthFrameReader` with buffer `Content-Length: 11\r\n\r\n{"a":1}` yields one object; test bounded queue drops oldest and invokes onDrop once.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm install && npm run build -w @agent-talkie/adapter-stdio && npm run test -w @agent-talkie/adapter-stdio</automated>
  </verify>
  <acceptance_criteria>
    - `packages/adapter-stdio/package.json` contains `"name": "@agent-talkie/adapter-stdio"`.
    - `grep -n "262144" packages/adapter-stdio/src/content-length-framing.ts` returns at least one line.
    - `grep -n "stdio_adapter_queue_overflow" packages/adapter-stdio/src/cli.ts` returns at least one line.
    - `grep -n "ensureRelayRunning" packages/adapter-stdio/src/cli.ts` returns at least one line.
    - `grep -n "TALKIE_STDIO_MAX_QUEUE" packages/adapter-stdio/src/cli.ts` returns at least one line.
    - `npm run test -w @agent-talkie/adapter-stdio` exits 0.
  </acceptance_criteria>
  <done>Stdio adapter meets D-10–D-12 and ADAPT-04.</done>
</task>

<task type="auto">
  <name>Task 3: docs/adapter-ingress.md (ADAPT-01 + ADAPT-03)</name>
  <files>docs/adapter-ingress.md</files>
  <read_first>
    - ARCHITECTURE-CONSTRAINTS.md
    - .planning/phases/04-collaboration-semantics-metadata-adapter-edge/04-CONTEXT.md
    - packages/client/src/session-client.ts
  </read_first>
  <action>
    Create `docs/adapter-ingress.md` with markdown sections exactly titled: `## Adapter ingress pattern`, `## Same client and transport as consumers`, `## Stdio reference adapter`, `## Security notes`. Include verbatim bullet: `Adapters must NOT become a second transport architecture.` Include code fence showing import path `@agent-talkie/client` and `TalkieSessionClient`. Document Content-Length header line format `Content-Length: N` followed by `\r\n\r\n`. Mention env `TALKIE_STDIO_MAX_QUEUE` default 100. Reference `ensureRelayRunning()` from `@agent-talkie/supervisor` for local daemon. State that overload is stderr-only per D-12 (no relay error).
  </action>
  <verify>
    <automated>test -f /Users/ruihao/Documents/github/agent-talkie/docs/adapter-ingress.md</automated>
  </verify>
  <acceptance_criteria>
    - Path `docs/adapter-ingress.md` exists.
    - `grep -n "## Adapter ingress pattern" docs/adapter-ingress.md` returns at least one line.
    - `grep -n "second transport" docs/adapter-ingress.md` returns at least one line.
    - `grep -n "@agent-talkie/client" docs/adapter-ingress.md` returns at least one line.
    - `grep -n "TALKIE_STDIO_MAX_QUEUE" docs/adapter-ingress.md` returns at least one line.
    - `grep -n "ensureRelayRunning" docs/adapter-ingress.md` returns at least one line.
  </acceptance_criteria>
  <done>ADAPT-01 documented; ADAPT-03 explicit in doc body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stdio → adapter | Untrusted framed JSON; must be size-bounded and Zod-validated before relay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | stdin JSON | mitigate | `safeParseEnvelope` + max frame 262144 bytes |
| T-04-03-02 | DoS | memory | mitigate | Bounded queue drop-oldest; max frame size |
| T-04-03-03 | Spoofing | session identity | accept | Localhost relay; same trust as any client registering a session |
</threat_model>

<verification>
`npm run build -w @agent-talkie/client && npm run build -w @agent-talkie/adapter-stdio && npm run test -w @agent-talkie/client && npm run test -w @agent-talkie/adapter-stdio` passes; root `npm run build` includes new packages.
</verification>

<success_criteria>
Client library and stdio adapter build, test, and are documented without relay-core special casing.
</success_criteria>

<output>
After completion, create `.planning/phases/04-collaboration-semantics-metadata-adapter-edge/04-03-SUMMARY.md`
</output>
