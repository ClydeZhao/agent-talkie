---
phase: 04-collaboration-semantics-metadata-adapter-edge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/protocol/src/relay-wire.ts
  - packages/protocol/src/collaboration-wire.ts
  - packages/protocol/src/index.ts
  - packages/persistence/migrations/003_collaboration_orchestrator_metadata.sql
  - packages/persistence/src/repositories/sessions.ts
  - packages/persistence/src/repositories/collaboration-metadata.ts
  - packages/persistence/src/index.ts
  - packages/relay/src/server.ts
autonomous: true
requirements:
  - MSG-04
  - MSG-05
  - MSG-06
  - META-01
  - META-02
  - META-03
  - META-04
must_haves:
  truths:
    - "SQLite has sessions.is_human, spaces.orchestrator_session_id, collaboration_profile, collaboration_status"
    - "session.register accepts optional boolean isHuman (default false) and persists it"
    - "Zod exports exist for orchestrator.designate, orchestrator.clear, task.assign, metadata.patch, metadata.query payloads"
  artifacts:
    - path: packages/persistence/migrations/003_collaboration_orchestrator_metadata.sql
      provides: DDL for orchestrator + collaboration metadata tables
      contains: is_human
    - path: packages/protocol/src/collaboration-wire.ts
      provides: collaboration control payload schemas
      contains: orchestratorDesignatePayloadSchema
  key_links:
    - from: packages/protocol/src/relay-wire.ts
      to: packages/persistence/src/repositories/sessions.ts
      via: newSession.isHuman → INSERT is_human
      pattern: is_human|isHuman
---

<objective>
Add durable columns and tables plus Zod contracts for human session classification, per-space orchestrator, collaboration metadata (profile vs status namespaces), and control payloads (`orchestrator.designate`, `orchestrator.clear`, `task.assign`, `metadata.patch`, `metadata.query`) so the relay can enforce D-01–D-06 in Plan 02.

Purpose: Persist authoritative state and typed wire shapes before routing and handlers ship.
Output: Migration 003 applied by migrate(), extended session registration, new `collaboration-wire.ts`, persistence helpers for reading/writing orchestrator and metadata rows.
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-collaboration-semantics-metadata-adapter-edge/04-CONTEXT.md
@.planning/phases/04-collaboration-semantics-metadata-adapter-edge/04-RESEARCH.md
@PRD.md
@ARCHITECTURE-CONSTRAINTS.md

<interfaces>
From `packages/persistence/migrations/002_relay_spaces_transcripts.sql`: spaces, space_memberships, sessions, transcript_entries patterns.

From `packages/protocol/src/relay-wire.ts` (current `sessionRegisterMessageSchema.newSession` shape): displayName, runtime, workspaceLabel, branch?, focus?.

From `packages/persistence/src/repositories/sessions.ts`: `createSession(db, input)` inserts into `sessions` with fixed column list — must gain `is_human` column binding.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 003 — orchestrator + metadata DDL</name>
  <files>packages/persistence/migrations/003_collaboration_orchestrator_metadata.sql</files>
  <read_first>
    - packages/persistence/migrations/001_initial.sql
    - packages/persistence/migrations/002_relay_spaces_transcripts.sql
    - packages/persistence/src/migrate.ts
  </read_first>
  <action>
    Create `packages/persistence/migrations/003_collaboration_orchestrator_metadata.sql` as version `003` (numeric prefix only). Include exactly these DDL changes:

    1. `ALTER TABLE sessions ADD COLUMN is_human INTEGER NOT NULL DEFAULT 0;` (SQLite: store 0/1 boolean).

    2. `ALTER TABLE spaces ADD COLUMN orchestrator_session_id TEXT;` — nullable; application validates membership; do not add a second transport.

    3. `CREATE TABLE IF NOT EXISTS collaboration_profile (`  
       `space_id TEXT NOT NULL REFERENCES spaces(id) ON DELETE CASCADE,`  
       `session_id TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,`  
       `role TEXT NOT NULL DEFAULT '',`  
       `focus TEXT NOT NULL DEFAULT '',`  
       `updated_at INTEGER NOT NULL,`  
       `PRIMARY KEY (space_id, session_id)`  
       `);`

    4. `CREATE TABLE IF NOT EXISTS collaboration_status (`  
       `space_id TEXT NOT NULL REFERENCES spaces(id) ON DELETE CASCADE,`  
       `session_id TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,`  
       `progress TEXT NOT NULL DEFAULT 'idle' CHECK(progress IN ('idle','working','blocked','done')),`  
       `blocked_reason TEXT,`  
       `last_activity_ms INTEGER,`  
       `updated_at INTEGER NOT NULL,`  
       `PRIMARY KEY (space_id, session_id)`  
       `);`

    5. `CREATE INDEX IF NOT EXISTS idx_collab_profile_space ON collaboration_profile(space_id);`  
    6. `CREATE INDEX IF NOT EXISTS idx_collab_status_space ON collaboration_status(space_id);`

    No other tables in this migration file.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/persistence</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/persistence/migrations/003_collaboration_orchestrator_metadata.sql` exists.
    - `grep -n "is_human" packages/persistence/migrations/003_collaboration_orchestrator_metadata.sql` returns at least one line.
    - `grep -n "orchestrator_session_id" packages/persistence/migrations/003_collaboration_orchestrator_metadata.sql` returns at least one line.
    - `grep -n "collaboration_profile" packages/persistence/migrations/003_collaboration_orchestrator_metadata.sql` returns at least one line.
    - `grep -n "CHECK(progress IN ('idle','working','blocked','done'))" packages/persistence/migrations/003_collaboration_orchestrator_metadata.sql` returns at least one line.
    - `npm run test -w @agent-talkie/persistence` exits with code 0.
  </acceptance_criteria>
  <done>Migration file present; persistence test suite passes with migrate() applying 003 on fresh DB.</done>
</task>

<task type="auto">
  <name>Task 2: Session register + persistence — isHuman</name>
  <files>packages/protocol/src/relay-wire.ts,packages/persistence/src/repositories/sessions.ts,packages/persistence/src/index.ts</files>
  <read_first>
    - packages/protocol/src/relay-wire.ts
    - packages/persistence/src/repositories/sessions.ts
    - packages/relay/src/server.ts
  </read_first>
  <action>
    1. In `sessionRegisterMessageSchema`, extend `newSession` with `isHuman: z.boolean().optional().default(false)` (Zod 4 compatible).

    2. Extend `NewSessionInput` and `validateSessionFields` in `sessions.ts` to accept optional `isHuman?: boolean`; treat undefined as false; no new max-length fields.

    3. Change `createSession` INSERT to include column `is_human` with value `input.isHuman === true ? 1 : 0`.

    4. Extend `getSessionById` return type and SELECT to include `isHuman: boolean` mapped from `is_human === 1`.

    5. Re-export updated types from `packages/persistence/src/index.ts` if `NewSessionInput` is part of public API.

    6. In `packages/relay/src/server.ts`, pass `reg.data.newSession` (which now includes `isHuman`) into `createSession(db, reg.data.newSession)` — only the call site wiring; full relay logic stays in Plan 02 except this compile fix.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run build -w @agent-talkie/protocol && npm run build -w @agent-talkie/persistence && npm run build -w @agent-talkie/relay && npm run test -w @agent-talkie/persistence && npm run test -w @agent-talkie/relay</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isHuman" packages/protocol/src/relay-wire.ts` returns at least one line.
    - `grep -n "is_human" packages/persistence/src/repositories/sessions.ts` returns at least one line.
    - `createSession` INSERT SQL in `sessions.ts` contains the substring `is_human`.
    - `npm run build -w @agent-talkie/relay` exits 0.
    - `npm run test -w @agent-talkie/relay` exits 0.
  </acceptance_criteria>
  <done>Human flag is server-persisted at registration time per 04-RESEARCH open question; relay compiles.</done>
</task>

<task type="auto">
  <name>Task 3: collaboration-wire Zod + persistence repository</name>
  <files>packages/protocol/src/collaboration-wire.ts,packages/protocol/src/index.ts,packages/persistence/src/repositories/collaboration-metadata.ts,packages/persistence/src/index.ts</files>
  <read_first>
    - packages/protocol/src/envelope.ts
    - packages/protocol/src/index.ts
    - packages/persistence/src/repositories/sessions.ts
  </read_first>
  <action>
    1. Add `packages/protocol/src/collaboration-wire.ts` exporting Zod schemas and inferred types:

       - `progressSchema = z.enum(["idle","working","blocked","done"])`

       - `orchestratorDesignatePayloadSchema = z.object({ orchestratorSessionId: z.string().uuid() })` — control `type` literal will be `orchestrator.designate` (document in file comment).

       - `orchestratorClearPayloadSchema = z.object({})` — for `orchestrator.clear`.

       - `taskAssignPayloadSchema = z.object({ threadId: z.string().min(1).max(256).optional(), summary: z.string().min(1).max(4000) })` — for `task.assign`; routing uses envelope `to` as assignee (MSG-06, D-03).

       - `metadataPatchPayloadSchema = z.discriminatedUnion("namespace", [`  
         `z.object({ namespace: z.literal("profile"), targetSessionId: z.string().uuid().optional(), patch: z.object({ role: z.string().max(256).optional(), focus: z.string().max(512).optional() }).strict(), }),`  
         `z.object({ namespace: z.literal("status"), patch: z.object({ progress: progressSchema.optional(), blockedReason: z.string().max(512).optional(), lastActivityMs: z.number().int().nonnegative().optional() }).strict(), }),`  
         `])`  
         (Relay resolves target as `payload.targetSessionId ?? envelope.sessionId` for profile; only `isHuman` senders may use `targetSessionId` different from `envelope.sessionId`, and target must be an active member.)

       - `metadataQueryPayloadSchema = z.object({})` — for `metadata.query`; space from envelope.spaceId.

    2. Export all of the above from `packages/protocol/src/index.ts`.

    3. Add `packages/persistence/src/repositories/collaboration-metadata.ts` with functions (names exact):

       - `getOrchestratorSessionId(db, spaceId: string): string | null` — reads `spaces.orchestrator_session_id`.

       - `setOrchestratorSessionId(db, spaceId: string, orchestratorSessionId: string | null, nowMs: number): void` — UPDATE spaces SET orchestrator_session_id=?, updated_at if column exists OR omit updated_at on spaces if no such column (spaces table has no updated_at today — only set orchestrator_session_id).

       - `getCollaborationMetadataSnapshot(db, spaceId: string): { sessions: Array<{ sessionId: string; profile: { role: string; focus: string; updatedAt: number }; status: { progress: string; blockedReason: string | null; lastActivityMs: number | null; updatedAt: number } }> }` — for each active member session in the space (JOIN space_memberships WHERE left_at IS NULL), LEFT JOIN collaboration_profile and collaboration_status; if missing rows, default profile `{role:'',focus:'',updatedAt:0}` and status `{progress:'idle',blockedReason:null,lastActivityMs:null,updatedAt:0}`.

       - `upsertCollaborationProfile(db, { spaceId, sessionId, patch, nowMs })` and `upsertCollaborationStatus(db, { spaceId, sessionId, patch, nowMs })` applying non-destructive merges (only defined patch keys updated).

    4. Export the new repository functions from `packages/persistence/src/index.ts`.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run build -w @agent-talkie/protocol && npm run build -w @agent-talkie/persistence && npm run test -w @agent-talkie/protocol && npm run test -w @agent-talkie/persistence</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/protocol/src/collaboration-wire.ts` exists.
    - `grep -n "orchestratorDesignatePayloadSchema" packages/protocol/src/collaboration-wire.ts` returns at least one line.
    - `grep -n "metadataPatchPayloadSchema" packages/protocol/src/collaboration-wire.ts` returns at least one line.
    - File `packages/persistence/src/repositories/collaboration-metadata.ts` exists.
    - `grep -n "getCollaborationMetadataSnapshot" packages/persistence/src/repositories/collaboration-metadata.ts` returns at least one line.
    - `grep -n "getOrchestratorSessionId" packages/persistence/src/index.ts` returns at least one line.
    - `npm run test -w @agent-talkie/persistence` exits 0.
  </acceptance_criteria>
  <done>Typed payloads and DB accessors ready for relay handlers and router (Plan 02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → relay | Untrusted `session.register` must not flip `isHuman` without product policy; v1 trusts registrant for localhost relay (documented risk). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Spoofing | `is_human` column | mitigate | Persist only from validated `session.register`; optional future auth; document localhost trust in code comment near schema |
| T-04-01-02 | Tampering | metadata DDL | mitigate | FK + CHECK constraints; relay will authorize writes in Plan 02 |
| T-04-01-03 | Elevation | profile fields in DB | mitigate | Relay enforces namespace ACL in Plan 02 (D-06); schema stays minimal per D-04 |
</threat_model>

<verification>
`npm run build -w @agent-talkie/protocol && npm run build -w @agent-talkie/persistence && npm run test -w @agent-talkie/persistence && npm run test -w @agent-talkie/relay` passes after all tasks.
</verification>

<success_criteria>
Migration 003 applies; `isHuman` round-trips on register; collaboration Zod + snapshot repository compile and tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-collaboration-semantics-metadata-adapter-edge/04-01-SUMMARY.md`
</output>
