---
phase: 05-cross-runtime-proof-human-oversight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/persistence/migrations/004_space_owner.sql
  - packages/persistence/src/repositories/space-owner.ts
  - packages/persistence/src/index.ts
  - packages/persistence/src/repositories/space-owner.test.ts
  - packages/relay/src/space-lifecycle.ts
  - packages/relay/src/collaboration-handlers.ts
  - packages/relay/src/__tests__/router-orchestrator.test.ts
  - packages/relay/src/collaboration-handlers.owner.test.ts
autonomous: true
requirements:
  - MHUM-01
user_setup: []
must_haves:
  truths:
    - "spaces table persists optional owner_session_id for management authorization"
    - "First human to join a space with no owner becomes owner_session_id"
    - "orchestrator.designate and orchestrator.clear are rejected with error not_space_owner when a different session is already owner"
    - "Relay tests pass with updated fixtures for owner semantics"
  artifacts:
    - path: "packages/persistence/migrations/004_space_owner.sql"
      provides: "DDL for owner_session_id"
      contains: "owner_session_id"
    - path: "packages/persistence/src/repositories/space-owner.ts"
      provides: "getSpaceOwnerSessionId, tryAssignSpaceOwnerIfUnsetForHuman"
    - path: "packages/relay/src/collaboration-handlers.ts"
      provides: "owner checks on orchestrator control types"
  key_links:
    - from: "packages/relay/src/space-lifecycle.ts"
      to: "tryAssignSpaceOwnerIfUnsetForHuman"
      via: "after successful membership activation for joining session"
    - from: "packages/relay/src/collaboration-handlers.ts"
      to: "getSpaceOwnerSessionId"
      via: "before orchestrator.designate / orchestrator.clear mutations"
---

<objective>
Persist per-space human owner identity (D-11, D-12) and enforce it server-side on orchestrator management controls so multiple humans can share a space without non-owners hijacking orchestration.

Purpose: Closes the RESEARCH.md gap vs current `collaboration-handlers.ts` (any `isHuman` could designate). Required before MCP `assign_orchestrator` and for MHUM-01 trust model.

Output: Migration 004, persistence helpers, `handleSpaceJoin` owner assignment, stricter `orchestrator.designate` / `orchestrator.clear`, updated vitest fixtures.
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cross-runtime-proof-human-oversight/05-CONTEXT.md
@.planning/phases/05-cross-runtime-proof-human-oversight/05-RESEARCH.md
@packages/persistence/migrations/003_collaboration_orchestrator_metadata.sql
@packages/persistence/src/repositories/spaces.ts
@packages/relay/src/collaboration-handlers.ts
@packages/relay/src/space-lifecycle.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 004 and space-owner persistence API</name>
  <files>packages/persistence/migrations/004_space_owner.sql,packages/persistence/src/repositories/space-owner.ts,packages/persistence/src/index.ts,packages/persistence/src/repositories/space-owner.test.ts</files>
  <read_first>
    - packages/persistence/migrations/003_collaboration_orchestrator_metadata.sql
    - packages/persistence/src/repositories/spaces.ts
    - packages/persistence/src/repositories/sessions.ts
    - packages/persistence/src/migrate.ts
    - packages/persistence/src/index.ts
  </read_first>
  <behavior>
    - Opening DB, running migrate(), then inspecting pragma table_info(spaces) includes column owner_session_id
    - tryAssignSpaceOwnerIfUnsetForHuman returns false and leaves DB unchanged when session is not is_human
    - tryAssignSpaceOwnerIfUnsetForHuman sets owner_session_id to sessionId when session is human and owner_session_id IS NULL
    - Second human calling tryAssignSpaceOwnerIfUnsetForHuman does not change existing owner
  </behavior>
  <action>
    Create `packages/persistence/migrations/004_space_owner.sql` with exactly:

    ```sql
    -- Migration 004: per-space human owner for management actions (Phase 5 MHUM-01).
    ALTER TABLE spaces ADD COLUMN owner_session_id TEXT REFERENCES sessions(id);
    ```

    Create `packages/persistence/src/repositories/space-owner.ts` exporting:

    - `getSpaceOwnerSessionId(db, spaceId: string): string | null` — `SELECT owner_session_id FROM spaces WHERE id = ?`, return `null` when column missing or value NULL (after migration always column exists).

    - `tryAssignSpaceOwnerIfUnsetForHuman(db, args: { spaceId: string; sessionId: string }): boolean` — load session via existing `getSessionById`; if `!row?.isHuman` return `false`; run `UPDATE spaces SET owner_session_id = ? WHERE id = ? AND owner_session_id IS NULL` with `[args.sessionId, args.spaceId]`; return `true` if `changes > 0`, else `false`.

    Re-export `getSpaceOwnerSessionId` and `tryAssignSpaceOwnerIfUnsetForHuman` from `packages/persistence/src/index.ts`.

    Add `packages/persistence/src/repositories/space-owner.test.ts` using `openDatabase(":memory:")`, `migrate(db)`, `createSession`, `insertSpaceWithSlug`, `insertMembership` as needed to prove the four behaviors in `behavior`.
  </action>
  <verify>
    <automated>npm run test -w @agent-talkie/persistence</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/persistence/migrations/004_space_owner.sql` exists and contains the exact line `ALTER TABLE spaces ADD COLUMN owner_session_id TEXT REFERENCES sessions(id);`
    - `packages/persistence/src/repositories/space-owner.ts` contains `export function getSpaceOwnerSessionId`
    - `packages/persistence/src/repositories/space-owner.ts` contains `export function tryAssignSpaceOwnerIfUnsetForHuman`
    - `packages/persistence/src/index.ts` contains `getSpaceOwnerSessionId` and `tryAssignSpaceOwnerIfUnsetForHuman` in export list from `./repositories/space-owner.js`
    - Command `npm run test -w @agent-talkie/persistence` exits with code 0
  </acceptance_criteria>
  <done>Migration 004 applies cleanly; space-owner helpers tested; persistence package exports updated.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce owner on join and orchestrator controls</name>
  <files>packages/relay/src/space-lifecycle.ts,packages/relay/src/collaboration-handlers.ts,packages/relay/src/__tests__/router-orchestrator.test.ts,packages/relay/src/collaboration-handlers.owner.test.ts</files>
  <read_first>
    - packages/relay/src/space-lifecycle.ts
    - packages/relay/src/collaboration-handlers.ts
    - packages/persistence/src/repositories/space-owner.ts
    - packages/relay/src/__tests__/router-orchestrator.test.ts
    - packages/relay/src/server.ts
  </read_first>
  <action>
    **space-lifecycle.ts**

    - Import `tryAssignSpaceOwnerIfUnsetForHuman` from `@agent-talkie/persistence`.

    - In `handleSpaceJoin`, inside transaction `run`, after a successful path that results in the joining session having an active membership in `targetSpaceId` (after `insertMembership` or `clearMembershipLeftAt` that leaves `left_at IS NULL`), call:

      `tryAssignSpaceOwnerIfUnsetForHuman(db, { spaceId: targetSpaceId, sessionId: args.sessionId });`

    - Also call the same after the idempotent replay branch that returns `{ kind: "joined", ... }` when `inserted` is false **if** that path represents an active membership (so reconnecting human can claim owner if still unset — only succeeds when NULL).

    **collaboration-handlers.ts**

    - Import `getSpaceOwnerSessionId` from `@agent-talkie/persistence`.

    - In `orchestrator.designate` after validating sender is human and target membership, **before** `tryRecordIdempotencyKey`:

      1. Let `owner = getSpaceOwnerSessionId(db, spaceId)`.

      2. If `owner !== null && owner !== envelope.sessionId`, send `{ type: "protocol.error", error: "not_space_owner" }` and return `true`.

      3. If `owner === null`, call `tryAssignSpaceOwnerIfUnsetForHuman(db, { spaceId, sessionId: envelope.sessionId })` once (claim-on-first-management for legacy rows/tests); if it returns `false` (should not happen for human sender), send `not_space_owner` and return `true`.

    - In `orchestrator.clear`, after human check, apply the same `owner` / `null` / `not_space_owner` gate before the idempotency transaction (same steps 1–3).

    **Tests**

    - Update every relay test file under `packages/relay/src/__tests__/` that issues `orchestrator.designate` or `orchestrator.clear` so the acting human session is the space owner: either rely on join order (first human in `handleSpaceJoin` tests) or explicitly call `tryAssignSpaceOwnerIfUnsetForHuman` after fixture membership inserts where tests bypass `handleSpaceJoin`.

    - Add `packages/relay/src/collaboration-handlers.owner.test.ts` importing `handleCollaborationControl` from `./collaboration-handlers.js` with a minimal `RelayDispatchContext` mock (in-memory DB via `openDatabase(":memory:")`, `migrate`, fake `WebSocket` like `router-orchestrator.test.ts`). Test: two humans `idH1` and `idH2` in same space, `UPDATE spaces SET owner_session_id = ?` to `idH1`, `idH2` sends `orchestrator.designate` with valid payload and idempotency key → first `send` payload JSON string contains `"error":"not_space_owner"` (exact substring).
  </action>
  <verify>
    <automated>npm run test -w @agent-talkie/relay</automated>
  </verify>
  <acceptance_criteria>
    - `packages/relay/src/collaboration-handlers.ts` contains the substring `"not_space_owner"`
    - `packages/relay/src/collaboration-handlers.ts` imports `getSpaceOwnerSessionId` from `@agent-talkie/persistence`
    - `packages/relay/src/space-lifecycle.ts` contains `tryAssignSpaceOwnerIfUnsetForHuman`
    - `npm run test -w @agent-talkie/relay` exits with code 0
    - Grep `packages/relay/src/__tests__` finds at least one expectation or assertion involving `not_space_owner`
  </acceptance_criteria>
  <done>Owner assigned on human join; non-owner humans cannot designate/clear; relay test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human session → relay control | Any `isHuman` session could previously mutate orchestrator; now bounded by persistent owner identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of privilege | `orchestrator.designate` / `orchestrator.clear` in `collaboration-handlers.ts` | mitigate | Require `envelope.sessionId === getSpaceOwnerSessionId` when owner set; claim owner on first human join or first management op when NULL; error `not_space_owner` otherwise |
| T-05-02 | Tampering | SQLite `spaces.owner_session_id` | mitigate | Column written only via relay transactions; FK to `sessions(id)` |
| T-05-03 | Repudiation | Owner assignment | accept | Local-first; no external audit requirement at ASVS L1 |
</threat_model>

<verification>
- `npm run test -w @agent-talkie/persistence` and `npm run test -w @agent-talkie/relay` both exit 0
- Manual: two human sessions in one space, non-owner designate → `not_space_owner` in WebSocket JSON
</verification>

<success_criteria>
Migration 004 present; owner helpers exported; join path assigns first human owner; orchestrator controls owner-gated; new negative test for non-owner designate.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-runtime-proof-human-oversight/05-01-SUMMARY.md`
</output>
