---
phase: 01-protocol-persistence-foundation
plan: "04"
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - packages/persistence/package.json
  - packages/persistence/src/repositories/sessions.ts
  - packages/persistence/src/repositories/idempotency.ts
  - packages/persistence/src/repositories/sessions.test.ts
  - packages/persistence/src/repositories/idempotency.test.ts
  - packages/persistence/src/index.ts
autonomous: true
requirements: [PROTO-03, SESS-01, SESS-02, SESS-03, SESS-04]
must_haves:
  truths:
    - "Session rows persist UUID v7 ids and D-06 workspace fields with length limits; no filesystem path columns."
    - "display name disambiguation appends numeric suffix `-1`, `-2`, ... for collisions per D-05."
    - "Idempotency record dedupes duplicate keys within TTL window; prune removes expired rows (default window 300000 ms)."
    - "Integration test proves session row survives new DB connection to same file path after process-level reopen."
  artifacts:
    - path: packages/persistence/src/repositories/sessions.ts
      provides: Session CRUD + disambiguation helper
    - path: packages/persistence/src/repositories/idempotency.ts
      provides: Try-insert + prune for PROTO-03
  key_links:
    - from: packages/persistence/src/repositories/sessions.ts
      to: packages/persistence/migrations/001_initial.sql
      via: INSERT/UPDATE sessions
---

<objective>
Implement session repository (stable UUID v7 ids, display name disambiguation, minimal workspace context validation), idempotency repository (5-minute default window per D-12), and a file-based integration test proving SESS-04 recovery after reopen.

Purpose: Session identity is durable and consumable by Phase 2 relay.

Output: Exported repository functions and passing tests.
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-protocol-persistence-foundation/01-CONTEXT.md

<interfaces>
Import `v7 as uuidv7` from `uuid` in persistence package — add `"uuid": "^13.0.0"` to `packages/persistence/package.json` dependencies.

String max lengths for validation before INSERT: `display_name` 128, `runtime` 64, `workspace_label` 256, `branch` 128, `focus` 512 (concrete caps for SESS-03).

`disambiguateDisplayName(preferred: string, existingDisplayNames: string[]): string` — if `preferred` not in `existingDisplayNames`, return `preferred`; else return `${preferred}-${n}` for smallest positive integer `n` such that result not in set (D-05 style).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session repository and disambiguation</name>
  <files>packages/persistence/src/repositories/sessions.ts, packages/persistence/package.json, packages/persistence/src/index.ts</files>
  <read_first>
    - packages/persistence/migrations/001_initial.sql
    - packages/persistence/src/db.ts
    - packages/persistence/src/migrate.ts
    - .planning/phases/01-protocol-persistence-foundation/01-CONTEXT.md (D-04, D-05, D-06)
  </read_first>
  <action>
    Add dependency `"uuid": "^13.0.0"` to `packages/persistence/package.json`.

    Create `packages/persistence/src/repositories/sessions.ts` exporting:
    - `export function disambiguateDisplayName(preferred: string, existingDisplayNames: readonly string[]): string` per INTERFACE spec.
    - `export type NewSessionInput = { displayName: string; runtime: string; workspaceLabel: string; branch?: string; focus?: string }`
    - `export function validateSessionFields(input: NewSessionInput): void` — throw `Error` with message prefix `Invalid session field:` if lengths exceed maxima (displayName 128, runtime 64, workspaceLabel 256, branch 128, focus 512) or any required string is empty after trim.
    - `export function createSession(db: import('better-sqlite3').Database, input: NewSessionInput, opts?: { id?: string; displayNameResolver?: (base: string) => string }): { id: string; displayName: string }` — `validateSessionFields`, generate `id` with `opts?.id ?? uuidv7()`, list existing `SELECT display_name FROM sessions`, compute final display name using `opts?.displayNameResolver ?? (base => disambiguateDisplayName(base, existing))`, `INSERT INTO sessions (...) VALUES (...)` with `created_at`/`updated_at` as `Date.now()`, return `{ id, displayName }`.
    - `export function getSessionById(db: import('better-sqlite3').Database, id: string): { id: string; displayName: string; runtime: string; workspaceLabel: string; branch: string | null; focus: string | null; createdAt: number; updatedAt: number } | undefined` mapping columns.

    Re-export from `packages/persistence/src/index.ts`.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm install && npm run build -w @agent-talkie/persistence</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'disambiguateDisplayName' packages/persistence/src/repositories/sessions.ts` exits 0.
    - `grep -q 'uuidv7' packages/persistence/src/repositories/sessions.ts` exits 0.
    - `grep -q 'INSERT INTO sessions' packages/persistence/src/repositories/sessions.ts` exits 0.
    - `grep -q 'validateSessionFields' packages/persistence/src/repositories/sessions.ts` exits 0.
  </acceptance_criteria>
  <done>SESS-01, SESS-02, SESS-03 persistence paths implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Idempotency repository</name>
  <files>packages/persistence/src/repositories/idempotency.ts, packages/persistence/src/index.ts</files>
  <read_first>
    - packages/persistence/migrations/001_initial.sql
    - .planning/phases/01-protocol-persistence-foundation/01-CONTEXT.md (D-11, D-12)
  </read_first>
  <action>
    Create `packages/persistence/src/repositories/idempotency.ts` exporting:
    - `export function tryRecordIdempotencyKey(db: import('better-sqlite3').Database, key: string, sessionId: string, nowMs: number = Date.now()): { inserted: boolean }` — `INSERT OR IGNORE` or try/catch on unique constraint: prefer single `INSERT INTO idempotency_keys (idempotency_key, session_id, first_seen_at) VALUES (?, ?, ?)`; if success return `{inserted:true}`; on unique violation return `{inserted:false}`.
    - `export function pruneExpiredIdempotencyKeys(db: import('better-sqlite3').Database, nowMs: number, windowMs: number = 300_000): number` — `DELETE FROM idempotency_keys WHERE first_seen_at < ?` with threshold `nowMs - windowMs`, return `changes`.

    Re-export from index.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run build -w @agent-talkie/persistence</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '300_000' packages/persistence/src/repositories/idempotency.ts` exits 0.
    - `grep -q 'idempotency_keys' packages/persistence/src/repositories/idempotency.ts` exits 0.
    - `grep -q 'pruneExpiredIdempotencyKeys' packages/persistence/src/repositories/idempotency.ts` exits 0.
  </acceptance_criteria>
  <done>PROTO-03 durable dedup helpers with D-12 default window constant.</done>
</task>

<task type="auto">
  <name>Task 3: Repository and restart persistence tests</name>
  <files>packages/persistence/src/repositories/sessions.test.ts, packages/persistence/src/repositories/idempotency.test.ts</files>
  <read_first>
    - packages/persistence/src/repositories/sessions.ts
    - packages/persistence/src/repositories/idempotency.ts
    - packages/persistence/src/db.ts
    - packages/persistence/src/migrate.ts
  </read_first>
  <action>
    **`sessions.test.ts`**: In-memory DB, `migrate`, create two sessions with same preferred display name `impl` — assert second stored display name is `impl-1`. `validateSessionFields` throws for `workspaceLabel` length > 256.

    **`idempotency.test.ts`**: In-memory DB, `migrate`, same key twice — first `inserted: true`, second `inserted: false`. `pruneExpiredIdempotencyKeys` with `nowMs` large and `windowMs` 300000 removes old row when `first_seen_at` set in the past (insert with manual SQL or helper test hook).

    **`SESS-04` file restart test** (same file as `sessions.test.ts` or dedicated `sessions.restart.test.ts`): use `os.tmpdir()` + random file name `talkie-test-${randomUUID}.db`, `openDatabase(path)`, `migrate`, `createSession`, `db.close()`, new `openDatabase(samePath)`, `migrate`, `getSessionById` must return the session. Delete temp file in `afterAll`.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/persistence</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -w @agent-talkie/persistence` exits 0.
    - `grep -q 'impl-1' packages/persistence/src/repositories/sessions.test.ts` exits 0.
    - `grep -q 'inserted: false' packages/persistence/src/repositories/idempotency.test.ts || grep -q "inserted: false" packages/persistence/src/repositories/idempotency.test.ts` exits 0.
    - `grep -qE 'tmpdir|mkdtemp|talkie-test' packages/persistence/src/repositories/sessions.test.ts` exits 0, or the same `grep` against `packages/persistence/src/repositories/sessions.restart.test.ts` exits 0 if the reopen test lives in that file.
  </acceptance_criteria>
  <done>SESS-04 and idempotency behavior verified by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository inputs | Validated lengths; relay must still authorize session_id ownership in Phase 2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I | workspace_label | mitigate | No path columns; max length enforced in validateSessionFields. |
| T-04-02 | T | idempotency tryRecord | mitigate | Keys UUID-shaped at wire; PK prevents cross-session overwrite of same key. |
</threat_model>

<verification>
`npm run test -w @agent-talkie/persistence` passes including temp-file reopen test.
</verification>

<success_criteria>
Sessions persist with disambiguation; idempotency insert/prune works; file DB survives close/reopen.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-persistence-foundation/01-04-SUMMARY.md`
</output>
