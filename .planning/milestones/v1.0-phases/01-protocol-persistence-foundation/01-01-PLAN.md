---
phase: 01-protocol-persistence-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.base.json
  - packages/protocol/package.json
  - packages/protocol/tsconfig.json
  - packages/protocol/tsup.config.ts
  - packages/protocol/vitest.config.ts
  - packages/protocol/src/envelope.ts
  - packages/protocol/src/index.ts
  - packages/protocol/src/envelope.test.ts
autonomous: true
requirements: [PROTO-01, PROTO-03, PROTO-04, PROTO-05]
must_haves:
  truths:
    - "Envelope JSON with version 1 and valid UUID fields parses; invalid payloads return structured Zod errors (not thrown exceptions from public parse API)."
    - "Wire shape matches D-01/D-03: flat object with version, id, sessionId, kind, type, payload, optional idempotencyKey, seq, to, spaceId."
    - "kind is only control or conversation; optional idempotencyKey and seq validate as UUID and non-negative integer when present."
  artifacts:
    - path: packages/protocol/src/envelope.ts
      provides: Zod envelope schema + parse helpers
    - path: packages/protocol/package.json
      provides: protocol package dependencies and scripts
  key_links:
    - from: packages/protocol/src/index.ts
      to: packages/protocol/src/envelope.ts
      via: export
---

<objective>
Bootstrap an npm workspaces monorepo and implement the versioned flat message envelope (Zod 4) with explicit `version`, `kind` (control | conversation), optional idempotency key and sequence, and addressing fields `to` / `spaceId` per D-01–D-03 and D-09.

Purpose: Establishes the wire contract Phase 2 relay will validate identically.

Output: Runnable `packages/protocol` with `envelopeSchema`, `safeParseEnvelope`, structured error type, and Vitest coverage.
</objective>

<execution_context>
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/ruihao/Documents/github/agent-talkie/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-protocol-persistence-foundation/01-CONTEXT.md
@.planning/phases/01-protocol-persistence-foundation/01-RESEARCH.md
@.planning/research/STACK.md
@PRD.md
@ARCHITECTURE-CONSTRAINTS.md

<interfaces>
Greenfield: no pre-existing types. Normative field names (camelCase): `version` (integer ≥ 1), `id` (UUID), `sessionId` (UUID, v7 at generation time per D-04), `kind` `'control' | 'conversation'`, `type` (non-empty string, max 256), `payload` (`Record<string, unknown>`), optional `idempotencyKey` (UUID), optional `seq` (integer ≥ 0), optional `to` (UUID), optional `spaceId` (UUID).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root workspace and packages/protocol package skeleton</name>
  <files>package.json, tsconfig.base.json, packages/protocol/package.json, packages/protocol/tsconfig.json, packages/protocol/tsup.config.ts, packages/protocol/src/index.ts</files>
  <read_first>
    - .planning/phases/01-protocol-persistence-foundation/01-CONTEXT.md (decisions D-01, D-09, camelCase)
    - .planning/research/STACK.md (versions)
  </read_first>
  <action>
    Create repository root `package.json` with `"private": true`, `"type": "module"`, `"workspaces": ["packages/*"]`, `"engines": { "node": ">=20" }`, and scripts: `"test": "npm run test -w @agent-talkie/protocol"`, `"build": "npm run build -w @agent-talkie/protocol"`.

    Add root `tsconfig.base.json` with `"compilerOptions": { "target": "ES2022", "module": "NodeNext", "moduleResolution": "NodeNext", "strict": true, "declaration": true, "declarationMap": true, "skipLibCheck": true }`.

    Create `packages/protocol/package.json` with `"name": "@agent-talkie/protocol"`, `"version": "0.0.0"`, `"type": "module"`, `"main": "./dist/index.cjs"`, `"module": "./dist/index.js"`, `"types": "./dist/index.d.ts"`, `"exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js", "require": "./dist/index.cjs" } }`, dependencies `"zod": "^4.3.6"`, `"uuid": "^13.0.0"`, devDependencies `"typescript": "^5.9.3"`, `"tsup": "^8.5.1"`, `"vitest": "^4.1.4"`, `"@types/node": "^22.0.0"`, scripts `"build": "tsup"`, `"test": "vitest run"`.

    Create `packages/protocol/tsconfig.json` extending `../../tsconfig.base.json` with `"compilerOptions": { "rootDir": "src", "outDir": "dist" }`, `"include": ["src"]`.

    Create `packages/protocol/tsup.config.ts` exporting default `{ entry: ['src/index.ts'], format: ['esm', 'cjs'], dts: true, sourcemap: true, clean: true }`.

    Create placeholder `packages/protocol/src/index.ts` with `export const PROTOCOL_PACKAGE_MARKER = 'agent-talkie-protocol'` so Task 1 build succeeds before envelope exports land in Task 2.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm install && npm run build -w @agent-talkie/protocol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"workspaces"' package.json` exits 0 and workspace list includes `packages/*`.
    - `grep -q '@agent-talkie/protocol' packages/protocol/package.json` exits 0.
    - `grep -q '"zod": "\\^4.3.6"' packages/protocol/package.json` exits 0.
    - `test -f packages/protocol/tsup.config.ts` exits 0.
  </acceptance_criteria>
  <done>Monorepo installs; protocol package builds (empty entry until Task 2 adds exports).</done>
</task>

<task type="auto">
  <name>Task 2: Zod envelope schema and parse helpers</name>
  <files>packages/protocol/src/envelope.ts, packages/protocol/src/index.ts</files>
  <read_first>
    - packages/protocol/package.json
    - packages/protocol/tsconfig.json
    - .planning/phases/01-protocol-persistence-foundation/01-CONTEXT.md (D-01, D-02, D-03, D-11)
  </read_first>
  <action>
    In `packages/protocol/src/envelope.ts`:

    1. Import `z` from `zod` and `version as uuidVersion` from `uuid`.

    2. Define `envelopeSchema = z.object({ version: z.number().int().positive(), id: z.string().uuid(), sessionId: z.string().uuid().superRefine((val, ctx) => { if (uuidVersion(val) !== 7) ctx.addIssue({ code: z.ZodIssueCode.custom, message: 'sessionId must be UUID v7' }); }), kind: z.enum(['control', 'conversation']), type: z.string().min(1).max(256), payload: z.record(z.string(), z.unknown()), idempotencyKey: z.string().uuid().optional(), seq: z.number().int().nonnegative().optional(), to: z.string().uuid().optional(), spaceId: z.string().uuid().optional() })`.

    3. Export type `Envelope = z.infer<typeof envelopeSchema>`.

    4. Export `function safeParseEnvelope(input: unknown): z.SafeParseReturnType<unknown, Envelope>` delegating to `envelopeSchema.safeParse(input)`.

    5. Export `function parseEnvelope(input: unknown): Envelope` calling `envelopeSchema.parse(input)`.

    6. Export `function formatEnvelopeIssues(result: z.SafeParseReturnType<unknown, Envelope>): { issues: Array<{ path: string; message: string }> }` mapping `result.error.issues` to `path` joined with `.` and `message`.

    Replace placeholder in `packages/protocol/src/index.ts` to re-export `envelopeSchema`, `Envelope`, `safeParseEnvelope`, `parseEnvelope`, `formatEnvelopeIssues` (remove `PROTOCOL_PACKAGE_MARKER`).
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run build -w @agent-talkie/protocol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'kind: z.enum' packages/protocol/src/envelope.ts` exits 0.
    - `grep -q 'idempotencyKey' packages/protocol/src/envelope.ts` exits 0.
    - `grep -q 'spaceId' packages/protocol/src/envelope.ts` exits 0.
    - `grep -q 'uuidVersion' packages/protocol/src/envelope.ts` exits 0.
    - `grep -q 'formatEnvelopeIssues' packages/protocol/src/index.ts` exits 0.
  </acceptance_criteria>
  <done>Envelope schema encodes PROTO-01, PROTO-04, PROTO-05, and optional idempotency key per PROTO-03/D-11.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vitest table-driven envelope validation</name>
  <files>packages/protocol/src/envelope.test.ts, packages/protocol/vitest.config.ts</files>
  <read_first>
    - packages/protocol/src/envelope.ts
    - packages/protocol/src/index.ts
  </read_first>
  <behavior>
    - Valid control envelope: version 1, random UUID for id, UUID v7 for sessionId (use `import { v7 as uuidv7 } from 'uuid'`), kind control, type `join`, payload `{}`, no idempotencyKey — expect success.
    - Reject: missing `version` — safeParse fails with path containing `version`.
    - Reject: `sessionId` as UUID v4 string — safeParse fails with message containing `v7`.
    - Reject: invalid `kind` string — safeParse fails.
    - Optional: envelope with `idempotencyKey` valid UUID and `seq: 0` — success.
  </behavior>
  <action>
    Add `packages/protocol/vitest.config.ts` with `import { defineConfig } from 'vitest/config'` and `export default defineConfig({ test: { environment: 'node', include: ['src/**/*.test.ts'] } })`.

    Add to `packages/protocol/package.json` inside the `test` script: `vitest run --config vitest.config.ts` (or equivalent) so `npm run test -w @agent-talkie/protocol` discovers tests.

    Implement `packages/protocol/src/envelope.test.ts` with the cases in `<behavior>` using `describe`/`it` and `expect`.
  </action>
  <verify>
    <automated>cd /Users/ruihao/Documents/github/agent-talkie && npm run test -w @agent-talkie/protocol</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -w @agent-talkie/protocol` exits 0.
    - `grep -q 'uuidv7' packages/protocol/src/envelope.test.ts` exits 0.
    - `grep -q 'safeParseEnvelope' packages/protocol/src/envelope.test.ts` exits 0.
  </acceptance_criteria>
  <done>Automated tests prove valid envelopes parse and invalid ones fail with structured paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted JSON → Zod | Any future wire input crosses here; must not crash the process on malformed data when using safeParse. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | envelope.ts parse | mitigate | Use `safeParseEnvelope` for untrusted input; document `parseEnvelope` for internal trusted paths only. |
| T-01-02 | D | payload object | mitigate | `z.record(z.string(), z.unknown())` only; relay applies size limits in Phase 2. |
</threat_model>

<verification>
`npm run build -w @agent-talkie/protocol` and `npm run test -w @agent-talkie/protocol` pass from repo root.
</verification>

<success_criteria>
Protocol package builds; envelope matches locked D-01/D-03 shape; tests cover success and representative failures.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-persistence-foundation/01-01-SUMMARY.md`
</output>
